暫定保持：0件（docs/HOLD.md）／最終棚卸し 2026-09-01

# 作業引き継ぎ（最終更新: 2026-08-31）

このファイルは「今どうなっているか」だけを書く。
恒久的な制約は CLAUDE.md（実装向け）と docs/OPERATIONS.md（運用向け）に書き、
ここには重複させない。更新は GitHub Web エディタで main 直コミット。

進行中の案件には必ず PR 番号を書く。番号が無いものは未投入。
「指示文作成済み」だけの記述は、投入済みかどうかが読めないため使わない。

## 現在の課題

- 8/31 朝の実測は7項目すべて通過。PR #19（120件上限の解消）・PR #20＋#56
  （予想生成のサーバー側移行）はいずれも初回本番で意図どおり動いた。
  「端末を開かなくても毎朝158銘柄の寄り予想が自動で貯まる」状態が完成した
- PM_Q_MIN_TICKS=5（有効観測5回未満は予想を作らない）が機能していないことが確定した。
  validCount の最小値が60のため、5未満は原理的に発生しない。
  8/31 の見送りは0件。別の基準に置き換えるか撤去するかの判断が要る
- noOpen が16件に増えた（8/25 は4件）。大きく下げて寄った日は特別気配で
  寄り付きが遅れる銘柄が増えるためと見られる。異常ではないが、
  「答え合わせできたデータは動きの小さい銘柄に偏る」という歪みが構造的に入る。
  係数の作り直しではこの点を意識する必要がある
- 較正係数が2箇所に分かれている。サーバー側は環境変数（既定値）、ブラウザ側は直書き。
  移行期間中の一時的な状態として受け入れた。当面は環境変数を設定しないこと
- 段A の係数（傾き0.058・切片−0.105）は先頭120銘柄由来。158件全件が取れ始めたため
  母集団が変わった。数日貯まった時点で mode=calib による確認が必要
- 8/31 の予想は概ね ±1% に収まったが、実測ギャップには −2〜−4% が並んだ。
  傾き0.058 が控えめすぎる可能性があるが、大きく動いた1日だけの印象のため断定できない
- Vercel 側の環境変数一覧表が CLAUDE.md に存在しない（PR #57 で判明）。
  本文中に散在しているだけで、PM_Q_SLOPE / PM_Q_INTERCEPT はどこにも一覧化されていない
- PR #50 の3点のうち、ネガティブキャッシュと2分停止は 429 が発生しなかったため未発火。
  再連射が止まったことは確認できたが、停止機能そのものの作動は未確認のまま
- parts.ranking の調査は完了。原因はほぼ特定できたが、修正は見送りとした
- PR #50・#51・#52・#53・#54・#55・#56・#57（daytrade-simulator）と
  PR #18・#19・#20（tachibana-server）はいずれもマージ済み

## 進行中の案件

### 気配収集の120件上限の解消（PR #19・完了）

- 状態: PR #19 マージ済み・Railway 再デプロイ済み・8/31 朝の実測で全項目通過。完了
- 触るファイル: なし
- 確定していた原因（8/28 の生ログ 2.4MB を直接解析。推測ではない）:
  - 84ティック全てが例外なく120行ちょうど。ばらつきが1つも無い
  - 1ティック目の120行は要求158件の先頭120番目までと順序も含めて完全一致
  - 集計に現れない38件は要求リストの121番目以降と完全一致
  - 板の厚さや銘柄の性質は無関係。リストの何番目にいるかだけで決まっていた
  - 同リポジトリのランキング取得は同じAPIを120件ずつに割って呼んでいた。
    寄り前ロガーだけが割らずに158件を丸投げしていた
- 実施した内容:
  - 環境変数 PREMARKET_CHUNK_SIZE を新設（既定80・起動時に一度だけ確定）
  - 取得を80件ずつの直列分割へ変更。応答行を連結して1ティック＝1レコードにまとめる
  - レコードの形（ts と raw）は不変。Vercel 側の集計は無変更で動く
  - 1チャンクが失敗しても残りは続行し、取れた分は捨てない。
    失敗はチャンク単位で tickErrorCount に加算
  - 要求件数と応答件数の突き合わせを追加。不一致はセッション内で最初の1回だけ詳細ログ、
    以降はカウントのみ。収集終了時に「件数不一致 Nティック」を0件でも出力
  - 起動ログに「分割 N件」を追加
- Claude Code の実装前確認で判明した事実:
  - 立花の応答順が要求順と一致する保証はコード上に無い。
    webapi.js も sIssueCode をキーにした辞書で受けている。
    このため連結は応答のまま、突き合わせは銘柄コードの集合で行う実装にした
  - webapi.js の getRankingData は chunk(codes,120) を並列5で投げている。
    寄り前ロガーだけ直列にしたため方式が異なる。これは意図的
  - errorCount は加算式ではなく、収集終了時に records から導出している
- 実測結果（8/31）: 件数不一致0ティック / tick失敗0件 / 84ティック全てが158銘柄 /
  missing 0件。38銘柄の取りこぼしは解消した
- 次の一手: なし。完了

### 寄り予想のサーバー側移行・Vercel 側（PR #56・完了）

- 状態: PR #56 マージ済み・8/31 朝の初回自動生成が成功。完了
- 触るファイル: なし
- 移行の理由: 予想の生成も答え合わせもブラウザ側にあり、
  8:45〜9:00 と 9:00以降の2回タブを開かないと記録が作られなかった。
  自動スキャンを導入した動機（決まった時刻に端末を開けない）と矛盾していた
- 実施した内容:
  - api/sync.js に resource「premarket-prediction」を新設（156行追加・既存行の変更なし）
  - POST は認証必須で生成・保存、GET は無認証の読み取り専用
  - 保存先は premarket:pred:<日付>・TTL30日・日付ごとに1キー
  - 予想の計算は src/App.js の pmPredictGapByQuote を移植。計算内容は変更なし
  - 較正係数のみ環境変数化（PM_Q_SLOPE 既定0.058 / PM_Q_INTERCEPT 既定 -0.105）
  - 実測ギャップは summarizePremarketDate の gapPct を流用。計算し直していない
- 移植の検証: 移植元と移植後を同じ定数で実行し、11,983ケース（総当たり＋null/欠損）で
  出力が完全一致。不一致0件、うち非nullの予想4,841件
- 実装前確認で判明した事実:
  - 「観測5回未満は予想を作らない」判定は validCount を見ている。
    quoteCount ではない。しきい値は PM_Q_MIN_TICKS=5
  - packForRedis は閾値なしで常に gzip。呼び出しは PR #56 時点で2箇所、現在は3箇所
  - 生ログが1件も無い日にPOSTしても保存しない（count:0 を返す）。
    空の辞書で上書きして過去の予想を消さないため
- 実測結果（8/31）: GET で found:true / count:158 を確認
- 次の一手: なし。完了

### 寄り予想のサーバー側移行・Railway 側（PR #20・完了）

- 状態: PR #20 マージ済み・8/31 朝の自動呼び出しが成功。完了
- 触るファイル: なし
- 実施した内容:
  - 生ログの保存が成功したときのみ、
    POST /api/sync?resource=premarket-prediction&date=<当日> を1回叩く
  - タイムアウト30秒・リトライなし・ボディなし・日付はクエリ
  - 認証は既存の authHeaders() を流用
  - 失敗しても例外を投げず warn 1行のみ。収集の成否に影響させない
  - errorCount にも tickErrorCount にも加算しない
- 確定した事実:
  - 保存失敗日は Vercel 側に材料が無いため呼ばない（空振り防止）
  - 収集セッションの終了が最大30秒延びる。9:06以降のため取得側への影響なし
- 実測結果（8/31）: 保存完了 9:06:09 の2.5秒後（9:06:12）に
  「予想を生成しました: 2026-08-31 158件（見送り0件）」を確認
- 次の一手: なし。完了

### 寄り予想のサーバー側移行・残る決定事項（移行設計として確定済み）

- 状態: 6点すべて決定済み。PR #56・#20 に反映済み。未投入の残件なし
- 決定内容:
  - 対象銘柄は PREMARKET_CODES のまま。158件全部の予想を作り、画面側で絞る
  - 既存のローカル記録（pm_<ticker>）とは当面併存。的中率集計は当面ローカルを見続け、
    サーバー側が十分貯まってから切り替える
  - 保存は日付ごとに1キー・TTL30日
  - 較正係数の置き場所は Vercel の環境変数
  - 読み出しは無認証、書き込みは認証必須
  - 答え合わせは収集終了直後の1箇所で完結。9:06 時点の始値で確定してよい
- 次の一手: なし。完了

### CLAUDE.md への新 resource 追記（PR #57・完了）

- 状態: PR #57 マージ済み。CLAUDE.md のみ 4 insertions / 2 deletions
- 触るファイル: なし
- 実施した内容:
  - resource 一覧の表に premarket-prediction（POST・GET）の行を追加
  - ファイル早見表の「7種に分岐」を「8種に分岐」へ修正
  - Redis キーと TTL 一覧に premarket:pred:<YYYY-MM-DD> / 30日 /
    PREMARKET_PRED_TTL / api/sync.js の行を追加
  - gzip 節の「gz: 付きで保存されるのは2つだけ」を、
    premarket:pred:<日付> を含めた「3つだけ」へ修正
- 実装前確認7項目すべて想定どおり。食い違いなし
- 変更しなかった判断: 同じ文中の「TTL は用途ごとに5種類」は据え置き。
  premarket:pred の30日は premarket:log と同値のため値の種類は増えないため
- Claude Code からの指摘3点（本文に記載のみ・未対応）:
  - Vercel 側の環境変数一覧表が CLAUDE.md に存在しない
  - date 必須の補足が premarket-summary のみを主語にしている
  - api/premarket.js と premarket-prediction の名前が紛らわしい
- 次の一手: なし。完了

### Phase 2B 段B・段C（PR #54・マージ済み・動作確認の位置づけを変更）

- 状態: PR #54 マージ済み。src/App.js のみの変更（110行追加・6行削除）
- 触るファイル: なし
- 位置づけの変更: 当初は 8/31 朝に動作確認する予定だったが、
  サーバー側移行（PR #56・#20）により毎朝の運用として背負う必要が無くなった。
  「PR #54 が想定通り動くかの一回きりの確認」に格下げ。
  8:45〜9:00 に端末を開けたときに見れば足り、開けなければ後日でよい
- 実施した内容:
  - 気配ベースの予想を src="quote" として記録し、既存のベータ推定（src="beta"）と並走
  - 追加した関数は4つ（窓判定 / 集計取得 / 予想生成 / 組み込み）
  - PM_HIST_MAX を 90 から 180 へ
- 確定した設計:
  - 予想ギャップ% ＝ 0.058 ×（買い比率 − 50）− 0.105。上限は ±3%
  - 確信度は基準45・片側偏りで+12・振れ幅で減点・25〜60%に丸め
  - 有効観測回数が5回未満の銘柄は予想を作らない
    （8/31 の実測でこの判定が機能していないことが判明。下記「未決の判断」参照）
- 次の一手: 一回きりの確認。急がない

### Phase 2B 段D・画面表示

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: src/App.js
- 内容: 予想パネルに2手法を並べて出す。的中率パネルの bySrc の見せ方。
  答え合わせ表示がベータ側を隠してしまう問題もここで解消する
- 前提の変更: サーバー側移行により読み出し元が変わった。
  ローカルの pm_<ticker> ではなく premarket-prediction の GET を見る設計になる
- 次の一手: サーバー側の記録が数日ぶん貯まってから着手。
  8/31 が1日目のため、木曜あたりが目安

### premarket-summary の mode=coverage 追加（PR #55・マージ済み）

- 状態: PR #55 マージ済み。api/sync.js のみ108行追加
- 触るファイル: なし
- 実施した内容: date 指定の単一日について、target / missing / noOpen / noBuyRatio を
  コード一覧で返す読み取り専用モードを追加
- 確定した事実:
  - missing と noOpen・noBuyRatio は排他。母集団が異なる
  - target の count = appearedCount + missing の count が成立する
- 次の一手: なし。完了

### Phase 2B 段A・係数決めの実測分析（PR #53・完了）

- 状態: PR #53 マージ済み。8/29 に4つのURL全ての動作確認が完了
- 触るファイル: なし
- 実施した内容: api/sync.js の premarket-summary に mode=calib を追加。
  from〜to（最大10日）で買い比率5変種の n / r / rRank / slope / intercept / dirRate を返す
- 訂正: 段A の457サンプルは全て上位120銘柄から出たもの。
  「158銘柄体制の実測」は「上位120銘柄の実測」に訂正する。
  データ自体は正しいため係数を捨てる必要は無い
- 次の一手: なし。段A は完了

### Upstash 月間リクエスト上限への対処（完了・コード変更なし）

- 状態: 完了。未投入（PR番号なし）。コード変更を伴わない運用対処のため PR は無い
- 触るファイル: なし
- 実施した内容:
  - 8/29 未明に無料枠50万コマンドへ到達し、Redis の読み書きが停止
  - Vercel Dashboard 経由で Pay As You Go へ移行。Max Budget 20ドル
  - 移行後、mode=calib のURL2本で復旧を確認
- 確定した事実:
  - 内訳は Reads 808,406 / Writes 76,390。読み取りが91%で主因
  - 相場が閉まっている土曜未明も 0.15〜0.2 コマンド/秒が平坦に継続
  - Upstash 側の Choose Plan は常時無効。導線は Vercel 側にしかない
  - 移行でエンドポイント・トークンは変わらず、環境変数の変更は不要だった
- 次の一手: なし。月に一度 Usage を見て金額を確認する

### tachibana-server README の全面改訂（PR #18・完了）

- 状態: PR #18 マージ済み。Railway 再デプロイは 8/28 夜に実施
- 触るファイル: なし
- 実施した内容:
  - 「このサーバーの4つの役割」節を新設（watcher / webapi / scanner / premarketLogger）
  - ファイル一覧を新設（実在17ファイルのみ）
  - 環境変数の3列表を新設（変数名 / 必須・任意 / 用途）。値は非記載
  - VPS 前提の記述を全削除
- PR #18 で確定した事実:
  - コードが実際に読む環境変数は21個（PR #19・#56 で3個増えたため現在は24個）
  - リポジトリ直下の実在ファイルは17個
  - index.js が起動するのは4つのみ
- 次の一手: なし。完了

### parts.ranking の原因調査（調査完了・修正は見送り）

- 状態: 8/28 に Claude Code で調査実施（変更なし）。未投入（PR番号なし）
- 触るファイル: なし（修正に着手する場合は api/ranking.js と api/_scan.js が候補）
- 調査で確定した内容:
  - 6016ms の主因は tachibana-server の getRankingData。全銘柄価格を
    120件ずつ・並列5で取得する処理。キャッシュ保持3分でスキャン間隔より短い
  - 上記は 8/28 に webapi.js を目視確認済み。推測ではない
- 見送りの理由: 組み立て回はスキャンせず即返す仕様のため、6秒は体感に出ない
- 次の一手: 予算超過が実際に観測された時点で再評価する

### ENV_CHANGELOG.md の可読性改善（PR #52・完了）

- 状態: PR #52 マージ済み。記録行追記も完了
- 触るファイル: なし
- 実施した内容: 表外にこぼれていた5行を表内へ移動し日付昇順に整列。
  伏せ字の穴埋め。内容列の書き方ルール4項目と記入例セクションを追加
- 次の一手: なし。完了

### /api/daily 連打対策（PR #50・完了・停止機能のみ作動未確認）

- 状態: PR #50 マージ済み・ブランチ削除済み・ENV_CHANGELOG.md 追記済み
- 触るファイル: なし
- 次の一手: 場中（9:00〜15:30）に同じ手順で再確認できれば、
  429 検知時の一時停止の作動まで見られる。機会があれば見る程度でよい

### /api/daily の呼び出し量そのものの削減（PR2）

- 状態: 未着手。未投入（PR番号なし）。8/27 の実測を受けて優先度を引き下げた
- 触るファイル: src/App.js の fillDayNightFor と visibilitychange のリスナ登録部分
- 次の一手: 当面は観測のみ。表示銘柄数が増えて 429 が再発したときに着手する。
  案D（直列ループに400ms程度の待機＋失敗銘柄への再試行抑止マーク）と
  案E（visibilitychange 経由の再開に最小間隔）。
  案F（cache no-store を force 時のみに限定）は効果の切り分けのため単独PR。
  案C（daily 専用キュー）は二重制御になるため最終手段

優先度を下げた理由:

- 429 が0件で、転送量も最大14.1kB と軽い。今すぐ直さないと壊れる状態ではない
- ただし「表示銘柄数ぶんのリクエストが一度に飛ぶ」構造は変わっていない

## 次にやること

上から順に:

1. 係数の再確認（mode=calib）。158件全件の母集団で数日貯まってから。
   8/31 が1日目のため木曜あたりが目安。傾き0.058 が控えめすぎないかも同時に見る
2. PM_Q_MIN_TICKS の扱いを決める。別の基準に置き換えるか撤去するか。
   validCount の意味が固まったので判断材料は揃っている
3. Phase 2B 段D（画面表示）。読み出し元がサーバー側に変わった前提で設計
4. ブラウザ側の予想生成をいつ外すか。サーバー側の記録が十分貯まってから
5. docs/ENV_AUDIT.md の棚卸しと、CLAUDE.md への環境変数一覧表の新設。
   PR #19・#56 で増えた3個（PREMARKET_CHUNK_SIZE / PM_Q_SLOPE / PM_Q_INTERCEPT）を
   1行ずつ足すのではなく、まとめて見直す
6. PR2 の着手判断。429 が再発するか、表示銘柄数が大きく増えた時点で再評価
7. parts.ranking の修正着手判断。予算超過が観測された時点で再評価

## 未決の判断

- PM_Q_MIN_TICKS=5 をどうするか。validCount の最小値が60のため機能していない。
  撤去するか、寄り付きの遅さ（validCount=84＝9:06 までに寄らない）を
  除外条件として使い直すかの2案が考えられる
- 158件という収集規模を維持するか。120件上限が解消され全件取得が実現したが、
  そもそも何件を定点観測するのが適切かという問いは残っている。
  ペイロードの余裕が401KB（約20銘柄ぶん）しかないため、増やす方向には制約がある
- 段A の係数を158件の母集団で作り直すかどうか。
  新しく入る38銘柄が同じ傾向を持つ保証はない。数日貯まってから mode=calib で確認する
- 較正データに入る偏りをどう扱うか。9:06 までに寄らない銘柄は答え合わせから外れるため、
  大きく動いた銘柄ほどサンプルから抜け落ちる。noOpen が16件出た 8/31 で顕在化した
- ブラウザ側の予想生成（src/App.js）をいつ外すか。
  外すときに較正係数の直書きも削除し、2箇所管理を解消する
- 既存のローカル記録 pm_<ticker> をサーバー側へ統合するのか、いったん切り捨てるのか。
  的中率集計がローカルを見ているため、切り替え方を決めないと過去の集計が断絶する
- Redis の読み取り回数を削減するかどうか。常時ポーリングが1日1.5万コマンド
  （月45万相当）を占めるが、8/29 時点では削減しない判断とした。
  Phase 2B で書き込みが増えて金額が跳ねた場合は再評価する
- quote 予想の確信度の上限60%をいつ引き上げるか。
  根拠が実質4営業日457サンプルのため控えめに置いた。
  ベータ推定側の上限は95%のままなので、当面は横並びで比較できない
- CLAUDE.md の「TTL は用途ごとに5種類」という文言。値の種類としては5で正しいが、
  用途の数と読める曖昧さがある。次に CLAUDE.md を触るときに
  「値としては5種類」と書き換えるのが本筋
- .env.example を作るかどうか。git log --all が空で、一度もコミットされたことがない。
  .gitignore の .env.* により追跡できないため、作るなら例外行が必要
- TACHIBANA_ENV が現在デモと本番のどちらで稼働しているか。
  コードからは判別できず README にも書いていない。Railway の Variables で確認できる
- docs/ENV_AUDIT.md の更新。PR #19・#56 で環境変数が3個増えた（21個→24個）。
  1行ずつ足すより次の棚卸しでまとめて見直す方が整合が取りやすい
- CLAUDE.md への Vercel 側環境変数一覧表の新設。PR #57 で不在が確定した。
  ENV_AUDIT.md の棚卸しと同時にやる方が整合が取りやすい
- Claude Code への「食い違えば実装せず報告のみで止める」指示の書き方。
  PR #53 以降はこの文言を入れており、前提確認は毎回正しく報告されている。
  恒久ルールとして CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- tick失敗の判定基準の読み替え（従来10件以上でNG → チャンク単位で20件以上でNG）を
  docs/OPERATIONS.md へ記載するかどうか
- 暫定保持に残る項目の移設先。docs/OPERATIONS.md が候補だが未着手
- ENV_CHANGELOG.md の記録範囲は決着済み。通常のドキュメント変更は記録しない。
  ただし ENV_CHANGELOG.md 自身の書式・ルール変更だけは例外として1行記録する。
  恒久ルールなので記載先を CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- ENV_CHANGELOG.md の書き方ルール本体の移設先。CLAUDE.md と OPERATIONS.md のどちらか
- daytrade-simulator 側の README を作るかどうか。作るなら10〜15行の入口に徹する。
  優先度は低い
- Mac mini で DevTools が使えることが判明した。
  開発端末は iPad という前提で書かれている docs/OPERATIONS.md を更新するかどうか
- api/premarket.js（今朝の地合い）と premarket-prediction（銘柄ごとの寄り予想）の
  名前の紛らわしさ。実害は出ていないが PR #57 で指摘として挙がった

## 未確認の仮説

- 立花APIの120件という上限が、仕様上の固定値なのか、
  応答サイズやカラム数に依存する可変の値なのかは未確認。
  80件分割にしたのは、この不確かさに余裕を持たせるため
- validCount は「気配が出続けていたティック数」＝寄り付きの遅さを表す指標で
  ほぼ確定した。8/31 の実測値は 60 / 72 / 84 の3値で、12刻み＝3分刻み。
  60が15分ぶん（9:00 に寄った）、84が21分ぶん（9:06 までに寄らなかった）に対応する。
  ただしコード上での裏取りはしていない
- 予想ギャップの幅が実測に対して小さい可能性。8/31 の予想は概ね ±1% に収まったが
  実測ギャップは −2〜−4% が並んだ。傾き0.058 が控えめすぎるのかもしれないが、
  大きく動いた1日だけの印象のため判断できない
- 始値が取れない銘柄に共通する事情。8/31 の noOpen 16件は
  268A・285A・3436・3851・5016・5801・6227・6323・6324・6525・6834・6920・
  6963・7743・8035・9984。大型株を含むため板の薄さでは説明がつかず、
  下げ幅の大きい日の特別気配と見られるが未確認
- Upstash の常時ぶん1日1.5万コマンドの発生源が Railway の watcher による
  ポーリングであるという推測。0.2コマンド/秒は5秒に1回の GET と一致するが未確認
- 買い比率の日平均が「その日の地合い」の代理指標になり得る可能性。
  8/25〜8/28 の4営業日とも順序が崩れていない。8/18〜8/24 でも同傾向。
  ただし後者はサンプルが少なく偶然でも説明がつくため仮説にとどめる
- 気配ベース予想がベータ推定より当たるかどうか。段A の相関は成立しているが、
  実際の予想としての優劣は quote の記録が貯まるまで判断できない
- PR #18 で README に載せた起動ログのサンプル4行が実際の文言と一致しているかは未検証
- PR #18 後の README で見出しの番号付けが混在している可能性がある。未確認
- 組み立ての時間予算 BUILD_BUDGET_MS 8000ms に対し、8/24 の totalMs は 6754ms で84%。
  立花側が普段より遅い日は fetchJsonWithin が先に切れる可能性がある
- 分岐C（/api/ranking へのフォールバック）の fetchJsonWithin には try/catch が無く、
  例外が buildUniverse の外へ出る。コード上の事実だが発生した観測は無い
- 組み立て失敗時、scan:universe:built のマークは組み立ての前に立つため、
  そのスロットは再試行されない可能性がある。未検証
- 内側のタイムアウト（/api/ipo が8秒、/ranking-data が15秒）より外側の予算8秒が
  先に切れる逆転構造になっている。内側の値は実質的に効いていないとみられる
- tachibana-server の getRankingData には topix にある inflight が無い。
  同時に2本来ると全銘柄取得が二重に走る可能性がある。発生の観測は無い
- scan:universe:meta の source が「ranking(業種で取れなかったため代替)」の場合、
  /api/sector が例外だったのか200で0件だったのかは meta からは区別できない
- ENV_CHANGELOG.md の「### 記入例」の表は本表と同じ形式のため、将来スクリプトで
  縦棒区切り行を機械的に数えると 2026-01-01 の1行が混入する
- 8/18 の気配データが 3.2KB と小さいのは初日の部分取得と見られるが未確認。
  8/19・8/20 が 27KB で一致しているため、12件時の定常値は 27KB
- api/premarket.js の cache 変数直上のコメントは内容は正確だが「Vercel側」の語がない。
  PR #49 では対応不要と判断

## 直近の実測値

### 158件全件収集の初回実測（8/31朝・7項目すべて通過）

| 項目 | 基準 | 実測 | 判定 |
|---|---|---|---|
| 件数不一致 | 0ティック | 0ティック | 通過 |
| tick失敗 | 20件未満 | 0件（84ティック全てが158銘柄） | 通過 |
| POST サイズ | 3096KB前後 | 3099KB | 通過 |
| レコード件数 | 84件 | 84件 | 通過 |
| 予想生成 | 成功ログ | 158件（見送り0件） | 通過 |
| mode=coverage | missing 0件 | target 158 / appeared 158 / missing 0 | 通過 |
| premarket-prediction GET | found:true | found:true / count:158 | 通過 |

- tick 所要 889〜1108ms / 平均972ms / 中央値964ms。120件時代は平均493ms。
  15秒間隔に対して使用は1秒弱で、余裕は93%
- 収集 1260秒ちょうど。84ティック × 15秒と完全一致。窓の取りこぼしなし
- 保存完了 9:06:09 → 予想生成完了 9:06:12。タイムアウト30秒に対し2.5秒
- ログ336行すべて [inf]。[err] も [warn] も0行
- POST 3099KB は推定3096KBとの差3KB。19.6KB/銘柄の見積もりがほぼ完全に的中。
  WARN 3500KB までの余裕は401KB（約20銘柄ぶん）
- noOpen 16件 / noBuyRatio 0件 / sessionCount 1
- 同日のスキャンも正常: 8:50 が 203件・4分8秒・失敗0件、
  9:30 が 192件・2分53秒・失敗0件。8:35 の日次再ログインも成功

### 気配収集のカバレッジ（8/28・mode=coverage および生ログ直接解析）

| 項目 | 実測 |
|---|---|
| 収集対象（target） | 158件 |
| 集計に現れた（appeared） | 120件 |
| 一度も現れない（missing） | 38件 |
| 始値が取れない（noOpen） | 4件（3436・3907・5242・6723） |
| 買い比率が取れない（noBuyRatio） | 3件（7707・7749・7769） |
| セッション数 | 1（codes を持つセッションも1） |

生ログ（2.4MB）を直接解析した結果:

- 84ティック全てが例外なく raw 120行ちょうど。ばらつきゼロ
- 1ティック目の120行は codes[0:120] と順序も含めて完全一致
- missing 38件は codes[120:158] と完全一致

missing の38件:
4307 4543 4578 4684 5187 5201 5541 5901 6301 6302 6465 6473 6503 6702
7012 7013 7701 7702 7716 7722 7730 7741 7743 7744 7747 7775 7776 7779
7780 7781 7782 8001 8031 8053 8308 8411 9434 9962

- お気に入り銘柄は全てこの38件に含まれていた
- 8001・8411・9434・6301 など大型株を含む。板の薄さでは説明がつかない
- PR #19 で対処済み。8/31 に missing 0件を確認した

### ペイロード線形性（訂正・8/30、8/31 に確定）

- 従来「2352KB ÷ 158銘柄 ＝ 14.89KB/銘柄」と解釈していたが、これは誤りだった
- 正しくは 2352KB ÷ 120銘柄 ＝ 19.6KB/銘柄。
  8/24 の100銘柄時点の 19.58KB/銘柄とほぼ完全に一致する
- 線形性は最初から崩れていなかった。19.58KB/銘柄は引き続き有効な見積もり値
- 8/31 の実測 3099KB ÷ 158銘柄 ＝ 19.61KB/銘柄。推定どおりで確定

### 移植の同値検証（PR #56・8/30）

- 移植元 src/App.js の pmPredictGapByQuote と移植後 api/sync.js の predictGapByQuote を
  同じ定数で実行し、11,983ケースで出力が完全一致
- 不一致0件。うち非nullの予想が4,841件
- 差分は空白・クォート記号・較正係数の読み元のみ

### Upstash 使用量の内訳（8/29・無料枠到達時点）

| 項目 | 実測 |
|---|---|
| 合計コマンド | 89万 / 無料枠50万（178%） |
| Reads | 817,306（91%） |
| Writes | 76,390（9%） |
| Bandwidth | 556MB |
| Storage | 6MB |

- 平日1日あたり約3万コマンド（月〜木。金は約2.2万）
- 土曜未明の Throughput は 0.15〜0.2 コマンド/秒で平坦。相場が閉まっていても止まらない。
  24時間換算で1.3万〜1.7万コマンド/日、月45万相当
- Hits/Misses の Misses はほぼ0。リトライによる水増しではない

### Phase 2B 段A・買い比率とギャップの関係（8/25〜8/28・4営業日・有効457行）

| 変種 | n | r | rRank | slope | intercept | dirRate |
|---|---|---|---|---|---|---|
| first | 457 | 0.166 | 0.339 | 0.039 | -0.008 | 0.608 |
| last | 457 | 0.211 | 0.494 | 0.058 | -0.105 | 0.643 |
| min | 457 | 0.196 | 0.420 | 0.050 | 0.335 | 0.717 |
| max | 457 | 0.206 | 0.433 | 0.053 | -0.443 | 0.568 |
| avg | 457 | 0.200 | 0.427 | 0.051 | -0.046 | 0.626 |

日別内訳:

| 日付 | n | 買い比率avgの平均 | 実測ギャップの平均 | その日の相関 |
|---|---|---|---|---|
| 8/25 | 119 | 48.924 | -0.251% | 0.458 |
| 8/26 | 116 | 53.435 | +0.056% | 0.313 |
| 8/27 | 109 | 58.500 | +0.893% | 0.411 |
| 8/28 | 113 | 48.335 | -0.384% | 0.159 |

除外内訳: noGap 16 / noRatio 8 / totalRows 480

- 段B 着手の水準（rRank 0.3以上）を満たした。最良は buyRatioLast の rRank 0.494
- buyRatioMin は dirRate 0.717 と方向の的中率が突出
- last の 0.058 なら買い比率75%で予想ギャップ +1.35% 前後
- 8/28 だけ相関 0.159 と低い。4日のうち1日は当たらない日がある
- 訂正（8/30）: このサンプルは上位120銘柄のみから成る。
  母集団は「158銘柄」ではなく「PREMARKET_CODES の先頭120件」

### Phase 2B 段A・8/18〜8/24（5営業日・有効154行・参考値）

| 変種 | n | r | rRank | slope | intercept | dirRate |
|---|---|---|---|---|---|---|
| first | 154 | 0.451 | 0.318 | 0.045 | 0.046 | 0.741 |
| last | 154 | 0.564 | 0.515 | 0.065 | -0.178 | 0.688 |
| min | 154 | 0.482 | 0.398 | 0.055 | 0.414 | 0.833 |
| max | 154 | 0.578 | 0.509 | 0.060 | -0.496 | 0.628 |
| avg | 154 | 0.563 | 0.494 | 0.063 | 0.036 | 0.735 |

日別内訳:

| 日付 | n | 買い比率avgの平均 | 実測ギャップの平均 | その日の相関 |
|---|---|---|---|---|
| 8/18 | 1 | 42.100 | -0.23% | 算出不可 |
| 8/19 | 10 | 36.730 | -1.327% | 0.533 |
| 8/20 | 9 | 54.444 | +1.51% | -0.043 |
| 8/21 | 39 | 37.733 | -1.659% | 0.515 |
| 8/24 | 95 | 51.265 | +0.433% | 0.436 |

除外内訳: noGap 7 / noRatio 5 / totalRows 165

- r が 8/25〜8/28 の2〜3倍高いが信頼できる数字ではない。
  154行のうち95行が 8/24 に集中している
- rRank で見れば last 0.515 / avg 0.494 で 8/25〜8/28 とほぼ同水準。
  順位ベースでは両期間が一致しており、こちらが信頼できる読み方
- 最良の変種が last、dirRate 最良が min である点も両期間で一致
- 係数の作り直しには使わない。段A の結論を別期間が緩やかに支持した位置づけ
- この期間は収集件数が12〜100件で120件を下回っていたため、120件上限の影響を受けていない

### /api/daily 連打対策の効果確認（8/27 夜・Mac の Chrome DevTools）

| 項目 | 実測 | 判定 |
|---|---|---|
| daily リクエスト件数 | 157件（表示銘柄 157件と一致） | 通過（重複取得なし） |
| ステータス | 200 と 304 のみ。429 は0件 | 通過 |
| 裏表の往復による追加リクエスト | 0件 | 通過（再連射なし） |
| 転送量 | 2.0KB / 12.1KB / 14.1KB（3回分） | 通過 |

- 429 が発生しなかったため、ネガティブキャッシュと2分停止は未発火
- 確認したURLはプレビュー用ドメイン。CORS ブロックと ERR_FAILED は
  プレビュー認証の副作用でアプリとは無関係

### PREMARKET_MAX=158（8/25朝・訂正あり）

| 項目 | 実測 | 判定 |
|---|---|---|
| tick失敗 | 0件 | 通過 |
| エラー | 0件 | 通過 |
| POST サイズ | 2352KB | 通過 |
| レコード件数 | 84件 | 通過 |
| tick 所要 | 439〜645ms / 平均493ms | 通過 |
| 起動ログ 対象 | 158件 / 158件 / 158件 | 通過 |

訂正（8/30）:

- 「全項目通過」は誤り。実際には要求158件に対し120件しか返っておらず、
  38銘柄が一度も収集されていなかった
- tick失敗0件・エラー0件だったのは、収集側に突き合わせ処理が無かったため
- POST サイズ 2352KB・tick 所要 平均493ms は、いずれも120銘柄での値
- 収集時間 1260秒＝21分ちょうど。84ティック × 15秒と完全一致。窓の取りこぼしなし

### 気配データの日次サイズ（Upstash premarket:log:<日付>、gzip 後）

| 日付 | Size | 銘柄数 | 1銘柄あたり |
|---|---|---|---|
| 8/18 | 3.2KB | 12 | — |
| 8/19 | 27KB | 12 | — |
| 8/20 | 26.7KB | 12 | — |
| 8/21 | 91.5KB | 40 | 2.29KB |
| 8/24 | 201KB | 100 | 2.01KB |
