暫定保持：0件（docs/HOLD.md）／最終棚卸し 2026-09-01

# 作業引き継ぎ（最終更新: 2026-09-10）

このファイルは「今どうなっているか」だけを書く。
恒久的な制約は CLAUDE.md（実装向け）と docs/OPERATIONS.md（運用向け）に書き、
ここには重複させない。更新は GitHub Web エディタで main 直コミット。

進行中の案件には必ず PR 番号を書く。番号が無いものは未投入。
「指示文作成済み」だけの記述は、投入済みかどうかが読めないため使わない。

## 現在の課題

- quote 予想の実力を13営業日1656件で初めて測定した（9/10・コード変更なし）。
  対象は 8/24〜9/9 の premarket-summary 全件。段A の係数を作った
  8/25〜8/28 をイン期間、8/31 以降をアウト期間として分けた。
  アウト期間1199件で R² +0.19、方向一致率 71.5%、平均絶対誤差 0.963%。
  R² は「全銘柄を前日終値どおりと予想した場合を0とし、そこから誤差を
  何割減らせたか」を表す指標で、0以下なら予想する意味が無いことを意味する。
  8通りの切り口（全件／極端値トリム／低位株除外／共通銘柄 × 実測0.00の含む除く）
  すべてで R² がプラスとなり、符号は一度も反転しなかった
- ただし勝率で見ると差は小さい。ゼロ予想との銘柄単位の勝ち負けは
  1199件中 勝ち639・負け553・引き分け7 で 53.3%。
  日単位（その日の平均絶対誤差で比較）ではアウト期間8営業日で4勝4敗。
  R² が高く出るのは、大きく動いた銘柄でゼロ予想より遥かに小さい誤差を
  出しているためで、「小さい勝ちが多い」のではなく
  「大きく動く日に勝ち、動かない日に負ける」性質を持つ
- イン期間（8/25〜8/28・457件）の R² は +0.045 で、アウト期間より低い。
  通常は係数を作った期間で最も当てはまりが良くなるため、この逆転は
  イン期間が荒れた相場だった（ゼロ予想の平均絶対誤差も 1.23% と大きい）か、
  段A の4営業日が 8/25〜8/28 ではない可能性のどちらかを示す。
  ただしアウト期間で通用している事実は変わらないため判定には影響しない
- 前日ギャップをそのまま今日の予想に使う案は無価値と確定した。
  アウト期間の R² が −0.93 でゼロ予想より大幅に悪い。
  銘柄単位でも quote 予想に 63.3% で負けている。株価の寄り付きギャップに
  日をまたぐ慣性は無い。この方向は今後追わない
- 「今朝の地合い」パネルの数値はどこにも保存されていないことが 9/10 の調査で確定した。
  Redis にも localStorage にも無く、画面を開くたびに Yahoo から取り直して
  その場で計算し捨てている。キャッシュはサーバー側3分とブラウザ変数のみ。
  したがって過去日ぶんは0日で、地合いの項の材料には現状使えない。
  元データを Yahoo から遡って再構成する道はあるが、重みの過半（0.53）を占める
  日経225先物とドル円は24時間動くため日足では 8:45 断面を再現できず、
  分足・時間足が要る
- 買い比率の日平均が地合いの代理指標になるかを13営業日で検証した。
  日ごとの買い比率平均と実測ギャップ平均の相関は 0.886、中央値なら 0.904。
  13営業日すべてで方向が一致した（最低は 9/2 の 44.07 で実測 −1.331、
  最高は 8/27 の 56.05 で実測 +0.893）。
  ただし予想式に地合い項として組み込むと精度は悪化する。
  イン期間で推定した係数（残差 = 0.180 × 日平均 − 9.620）を適用すると、
  アウト期間の R² が +0.19 から +0.07 へ落ちた。低位株を除いた条件でも
  +0.23 が +0.14 へ低下。イン期間では +0.045 → +0.053 とわずかに改善するため、
  4日ぶん（日平均としては4点）から推定した係数の過学習と判断する。
  日平均は 44〜56 の12ポイント振れるため、係数 0.18 では予想全体が
  2.2% 上下し、個別銘柄の買い比率による予想幅と同等の大きさになってしまう
- 一方、方向判定だけに絞れば地合いも予想も精度が大きく上がる（9/10 の検証）。
  アウト期間の方向一致率は、全銘柄 71.4% に対し、
  買い比率が50から10以上離れた銘柄で 81.6%（523件）、
  15以上で 85.0%（354件）、20以上で 90.2%（205件）、
  20以上かつ前日終値500円以上で 91.5%（177件）。
  安全に言えるのは10以上まで。20以上は205件、25以上は111件と数が減り、
  25以上ではむしろ 87.4% に下がるため、絞りすぎは数字が暴れる
- 板の一貫性という別の切り口も効く。収集中ずっと同じ側に偏っていた銘柄
  （buyRatioMin と buyRatioMax がともに50超、またはともに50未満）に絞ると
  方向一致率 79.7%（636件・全体の53%）。50から3以上離れた条件で 81.8%、
  5以上で 83.9%。現行式は buyRatioLast しか見ておらず、
  途中でひっくり返った銘柄と一貫していた銘柄を区別していない
- 方向判定の材料として日平均を足すのは有効。アウト期間で
  現行式 71.4% に対し、Last + 日平均で 73.4%、Last + 日平均×2 で 76.2%、
  Avg + 日平均で 74.8%。ただし1100件で26件の差は誤差に埋もれうる水準のため、
  この 2.4〜4.8ポイントは本物と断定できない
- 画面の「寄与」表示に不具合がある（9/10 の調査で判明）。
  contribution は changePct × coef だけで weight を掛けておらず、
  Σ(weight) での正規化も入っていない。したがって7指標の「寄与」を
  足しても総合値（想定ギャップ）にならない。実効寄与は
  weight × changePct × coef ÷ Σweight で、例えば日経225先物の表示が
  「寄与 +1.00」でも総合値への効きは 0.45 倍の +0.45 になる
- 8:46 と 8:58 に premarket-summary が送出されるという前提が誤りだったことが
  9/10 の調査で確定した。そのような周期送出の設計はコード上に存在しない。
  送出の引き金は4つだけで、いずれも時刻ではない。パネルのマウント、
  お気に入り（JP分）の変更、9:00 をまたぐこと、手動更新ボタン。
  唯一の呼び出し元は pmBuildPredictions 内の1箇所で、その呼び出し口は
  依存配列 [favKey, afterOpen] の useEffect と refresh() の2つのみ。
  8:45〜9:00 の間はお気に入りが変わらず afterOpen も false のまま固定のため、
  effect はマウント時の1回しか走らない。8:45 より前からタブを開いていると
  その唯一の実行が窓の外に落ち、9:00 の再実行時は今度は窓を過ぎている。
  つまり 8:45 より前に開いているとかえって一度も送出されない構造だった。
  段4 の確認項目「8:58 で partial が true」は現行コードでは到達不能である
- 観測ログは premarket-summary を記録対象に含んでいる。obsNetLog に
  resource 名のフィルタや許可リストは無く、fetch に到達すれば必ず1件残る。
  記録されずに戻る経路は fetch より手前の2つだけで、時間外の早期 return と
  60秒キャッシュのヒットのみ。したがって「記録漏れ」ではなく
  「通信自体が発生していない」が結論
- 寄り前気配ベースの予想（src="quote"）が作られない問題は解決した。
  原因は2層に分かれていた。1層目はサーバー側のタイミングで、
  端末が問い合わせる 8:45〜9:00 には元データの premarket:log:<日付> が
  まだ書かれていないというもの（段1〜段4・PR #69・#25・#72）。
  2層目はフロント側で、寄り前タブを選択したときにしか寄り前パネルを
  画面へ置かない作りのため通信そのものが発生しないというもの（PR #71）。
  9/8 朝に quote 予想が初めて画面へ到達したことを実測で確認した
- PR #74（applied ログ）を 9/9 にマージし、Vercel のビルド通過を確認した。
  9/10 朝に実運用で動作し、狙いどおり反映の有無を判定できた
- 9/10 朝の applied は6件。via の内訳は init が1件（09:00:46）、
  retry が5件（09:02:02 / 09:03:17 / 09:04:32 / 09:05:56 / 09:07:02）。
  srv は5件目まで0で、6件目の 09:07:02 に 152 へ切り替わった。
  rows は6件とも157で不変。通信ログの found が 09:07:01 で初めて true に
  なったのと完全に一致しており、applied は設計どおり機能している
- 9/9 に持ち越していた「155件と157本の2件差」は、9/10 の miss で決着した。
  最終 applied の miss は 4424 / 7713 / 7749 / 7760 / 7781 の5件で、
  157 − 152 = 5 と一致する。上限10件に対し5件のため途中で切れた一部ではなく全件。
  5件中4件が77xx台の小型株で、7713 は 9/4 に validCount が 1 だった銘柄。
  「板が薄い銘柄が落ちる」という見立てと矛盾しないが、1営業日ぶんのため
  銘柄側かタイミング側かの断定はまだできない
- 9/10 の通信ログの predCount は153だが applied の srv は152で1件合わない。
  9/10 の調査でこれは仕様どおりと確定した。predCount はサーバーが返した
  辞書の全キー数でお気に入りかどうかを見ていない。一方 srvCount は
  お気に入り側の行しか回らない。サーバー予想の母集団は Railway 側の
  PREMARKET_CODES（158銘柄）で端末のお気に入りとは無関係のため、
  お気に入りに入っていない銘柄が1件あれば差が出る。バグではない
- 突き合わせキーの正規化に非対称がある（9/10 の調査で判明）。
  サーバー側は String(row.sIssueCode).trim() で trim 済みだが、
  フロント側は serverPreds[ticker.replace(".T","")] で trim も大文字化もしない。
  お気に入りの ticker に前後空白が混ざる、または 278a.T のように小文字で
  保存されていると突き合わせに失敗する。型はどちらも文字列のため型違いは無い。
  9/10 の miss 5件はすべて数字4桁のためこの件には該当しないが、
  429A のような英字を含むコードが miss に現れた場合は真っ先に疑う箇所
- applied の rows 157 は「お気に入りのうち日本株（.T 付き）だけ」の件数と確定した。
  pmIsJP（/\.T$/）で二重に絞られており米国株は入らない。
  重複登録があればその分は水増しされる。お気に入り総数ではない
- お気に入り総件数の確認手段は既に画面上にある。同期タブの「◯◯銘柄」の表示が
  localStorage の fav_tickers の生の件数で、米国株も含む絞り込み前の値。
  同期タブの数字と rows 157 の差が米国株ぶんになる。観測ログ側には総件数が無い
- 寄り予想の再取得タイマーの打ち切りは「回数」ではなく「時刻 9:12」のみと確定した
  （9/10 の調査）。PM_PRED_RETRY_UNTIL_MIN（9*60+12）を、タイマーを張る時と
  ループ内の2箇所で判定する。最大回数の定数も回数を数える変数も存在しない。
  間隔は PM_PRED_RETRY_MS（75秒）で、setInterval のため初回発火は75秒後。
  成功が 9:08・9:09 にずれ込んでも取りこぼさない。危ないのは 9:11 台以降
- 再取得タイマーの起動条件は afterOpen && favKey && results && serverOk===false。
  つまり 9:00 後の初回 pmBuildResults が空振りして初めて張られる。
  実測の初回リクエスト（9/10 は 09:00:36）はこの「初回」であって
  再取得タイマーの1回目ではない
- 観測ログ（PR #73）が 9/9 朝に初めて実運用で動いた。在席せずに
  朝の挙動を判定できることが確認できた。9/9 の心拍は 08:39〜09:39 の
  121回、9/10 は 08:29〜09:29 の121回がいずれも1つも欠けず、
  9/7 のタイマー凍結は2営業日連続で再発していない
- 9/10 朝の premarket-prediction は6回。09:00:36 から75秒間隔で叩き、
  6回目の 09:07:01 に found:true / predCount 153 で成功。status は6回とも200、
  応答は325〜372ms。9/9 は5回目の 09:06:21 に155件で成功しており、
  成功までの試行が1回増え、時刻も40秒遅くなった。ただし打ち切りが 9:12 のため
  余裕は残っている
- 9/10 朝のリクエスト date と応答 resDate は6回とも 2026-09-10 で一致した。
  9/9 と同じ結果で、premarket-prediction 側の日付は2営業日連続で正しい
- 「今朝の地合い」パネルの予想対象日が翌営業日へ進む現象が 9/10 に再現した。
  更新 9:07:02 の時点で表示は 2026-09-11。9/8 に続き2回目。
  今回は同じ画面内で premarket-prediction の date=2026-09-10（正しい）と
  地合いパネルの 2026-09-11（進んでいる）が同時に見えており、
  CLAUDE.md にある「日付関数が fcTodayJST と pmTargetDate の2系統ある」という
  前提どおり、地合いパネルだけが別系統を使っているとほぼ確定した
- 9/10 朝の daily の経路別回数は fetchDaily が158回、
  pmFetchRecentDaily が942回。942 は 157 × 6 と一致し、再取得6回ぶんの
  日足取得がすべて走ったことを示す。9/9 の785（157 × 5）と同じ構造で、
  PR #71 の副作用は2営業日連続で再現した
- 描画件数ログでは「サーバー予想が画面へ反映されたか」を判定できないことが
  9/9 の調査で確定した。答え合わせモードの行数はお気に入りのうち
  日本株と判定された銘柄数で決まり、サーバー予想の件数に影響されない。
  サーバー予想は各行の中身を差し替えるだけで行を増減させないため、
  件数が変わったときだけ記録する既存の作りでは何も残らない。
  この判定不能を埋めるため applied ログを追加した（PR #74・マージ済み）
- 観測ログにはアプリ内タブの切替を記録する仕組みが無い（9/10 の調査で確認）。
  obsEvent の呼び出しは applied / render / error / reject / visibility の5種のみで、
  visibility はブラウザの前面・背面であってアプリ内タブではない。
  パネルが何時にマウントされたかを知る唯一の手がかりは mode:"pred" の
  render イベントの時刻だが、OBS_LAST_ROWS が件数の変化時しか記録しないため
  同じ件数での再マウントは残らない
- 9/8 朝 9:06 時点および 9/10 朝 9:07 時点で「予想 vs 実際の始値」が
  全銘柄「待機中」だった。2回とも再現しており、たまたまではない。
  daily は 9/9 に785回、9/10 に942回走っているため「daily が回っていない」説は弱い。
  返り値に当日の始値がまだ乗っていない可能性が高いが、
  始値の有無を記録していないため未確定
- 9/8 朝 9:06 時点および 9/10 朝 9:07 時点で「気配スナップショット」が
  0日分 / 0件だった。Phase 2B の校正用データが空のまま。2回とも再現。
  9/10 は daytrade-simulator-git-main-... のURLで観測しており、
  端末内保存（localStorage）はURLごとに別領域になるため、
  普段と違うURLを開いていた場合は0件が正常な結果である可能性がある。
  お気に入りはアプリ内ログインで同期されるため、同期対象外のデータ
  （気配スナップショット・ベータ推定の過去実績・pm_<ticker> の履歴）だけが
  URL依存で失われる形になる。普段の朝と同じURLかどうかは未確認
- 寄り前収集の安定稼働が確定した。8/31・9/1・9/2・9/3・9/4・9/7 の6営業日連続で、
  tick失敗0 / 件数不一致0 / 全ティック158銘柄 を通過。
  「端末を開かなくても毎朝158銘柄の寄り予想が自動で貯まる」状態は再現性を持って動いている
- 予想誤差の主因は「地合い」だと 9/3 に確定した。同日の予想の平均は −0.02% だったが
  実測の平均は +0.58% で、152銘柄そろって +0.60% ぶん上に外している。
  quote 予想の式には地合いを表す項が無く、切片（−0.105）が固定値でその役目を代行している。
  実測の日別平均ギャップは −1.33%〜+0.58% と1.9ポイント振れており、
  固定の切片では追随できない。ベータ推定側には marketBias があるが quote 側には無い
- 9/4 も同方向の偏りが出た。平均予想 −0.04% に対し平均実測 +0.27% で、
  平均誤差は +0.31%。9/3 の +0.60% より小さいが符号は同じで、
  地合いが誤差の主因という見立ては2営業日連続で再現した。
  ただし補正の効き目は限定的で、全銘柄に一律 +0.31% を足しても
  平均絶対誤差は 0.87% から 0.82% にしか下がらない。
  地合いは全体を上下にずらすだけで、銘柄ごとのバラつきは別の要因が支配している
- 買い比率が極端な銘柄を大きく外している。9/3 に予想の絶対値が1.5%以上だった13件は
  平均絶対誤差 1.79% で、残り139件の 0.80% の2倍以上。
  内訳は 7707（予想 −2.92 / 実測 −0.47）、7717（−2.76 / +0.75）、
  7743（−2.54 / 0.00）、7776（−2.22 / 0.00）、256A（−1.98 / +0.51）など、
  売り気配に偏っていた銘柄がほとんど下げずに寄る形。
  板が薄く気配数量が数枚しか出ない銘柄で比率が振り切れることが原因と見られる
- 9/4 も極端な買い比率で同じ傾向が出た。予想の絶対値1.5%以上の13件は
  平均絶対誤差 1.57% で、残り142件の 0.80% のちょうど2倍。
  7776（比率3.7・予想−2.79 / 実測0.00）、7777（5.4・−2.69 / −0.28）、
  7743（9.3・−2.47 / 0.00）、7722（20.0・−1.84 / 0.00）。
  ただし 7709 は比率1.2 で予想 −2.94 に対し実測が −4.69 で不足側に外した
- 9/4 の外れの正体が見えた。「予想が大きすぎた」のではなく
  「寄り値が前日終値から動かない銘柄を予想対象にしている」ことによる。
  実測ギャップがちょうど 0.00 の銘柄は155件中17件あり、
  うち5件は予想の絶対値が1.0%以上だった。
  外れの大きい13件のうち10件が77xx台に集中し、株価も 7776 が214円、
  7743 が792円、7779 が218円と低位・小型が中心。
  したがって上限で丸める方向（PM_Q_EXP_LIMIT を下げる）は 0.00 組に効かず、
  唯一の不足側である 7709 を悪化させるだけになる
- 低位株を外す効果は13営業日の集計でも確認できた（9/10）。
  前日終値500円以上に絞るとアウト期間の R² が +0.192 から +0.228 へ上がる。
  「板の薄さ」の判定材料として株価水準で代用できることを示す
- validCount による群の差は 9/3（上げ相場）では再現しなかった。
  60の群が平均誤差 +0.60%、61の群が +0.66%、61以上をまとめても +0.61% で差は0.01%。
  下げ相場3営業日で見えていた「約1%の開き」は地合いの影響だったと考えられる
- 群の境界を61で引いていたのが粗すぎた。61ちょうどは60よりティック1回ぶん長く
  両側の気配が取れただけで、寄りが遅れたとは呼べない。
  9/3 は61ちょうどが77件・62以上が7件で、84（9:06まで寄らず）は0件だった。
  下げた3日の「61以上」は84付近を多く含んでおり、日によって別の集団を指していた。
  区切るなら62以上にすべき
- 9/4 の実測で「62以上」の区切りに別の裏付けが取れた。validCount 62以上の12銘柄が
  例外なく絶対値1.94%以上のギャップで寄っている。一方 61ちょうどの5銘柄は
  +0.78 / −0.41 / −1.59 / +2.34 / +2.06 とばらついた。
  誤差の大小ではなく「大きく動いたかどうか」という別の切り口で 62 が効いている
- validCount が60未満の銘柄が出る日と出ない日がある。9/2 は 11・33・34・49 の4件、
  9/3 は0件、9/4 は 1（7713）・33（7976）の2件。
  quoteCount は揃っているため収集の取りこぼしではない
- validCount 1 が実際に発生した（9/4 の 7713）。買い比率6.7 の1点だけから
  予想を作っており、実測は −0.21 で大きく外した。
  PM_Q_MIN_TICKS 撤去時（PR #63）の「5未満で弾かれる行は存在しない」という結論は誤り。
  ただし下限を5に戻しても 7713 は通るため、必要なのは下限の復活ではなく別の水準の判断
- noOpen の件数は日によって大きく振れる。8/25 が4件、8/31 が16件、
  9/1 が5件、9/2 が4件、9/4 が1件（5242）。大きく下げて寄った日に増える。
  「答え合わせできたデータは動きの小さい銘柄に偏る」という歪みは構造的に残る
- 低位株が較正データを壊す。7709 は 9/1 に +15.87%、9/2 に +14.71%。
  前日終値が63円・68円の価格帯で、1円の動きが1.6%になる。
  ただし 9/3 の 7709 は予想 −2.23 に対し実測 −2.94、9/4 も予想 −2.94 に対し
  実測 −4.69 で不足側に外した。問題の本体は価格帯ではなく板の薄さの可能性がある
- 較正係数が2箇所に分かれている。サーバー側は環境変数（既定値）、ブラウザ側は直書き。
  移行期間中の一時的な状態として受け入れた。当面は環境変数を設定しないこと。
  PR #64 で CLAUDE.md の環境変数表にもこの注意を明記した。
  9/8 にサーバー予想が画面へ出ることを確認できたため、
  ブラウザ側の予想生成を外す前提条件は満たされた
- 段A の係数（傾き0.058・切片−0.105）は先頭120銘柄由来。158件全件の4営業日で
  mode=calib を取り直したところ buyRatioLast は傾き0.045・切片−0.405 だった。
  ただし切片は期間の地合いを吸い込むため、この値をそのまま採用してはいけない。
  係数の見直しは2箇所管理を解消してからにする
- 段A の4営業日が 8/25〜8/28 であることは実データで裏づけられた（9/10）。
  この4日の premarket-summary は count が4日とも120ちょうどで、
  8/24 は100、8/31 以降は158。立花の120件上限を回避するチャンク分割が
  入ったのが 8/30（tachibana-server PR #19）であることと整合する。
  ただしイン期間とアウト期間で銘柄母集団が違う（120 対 158）ため、
  期間間の単純比較には注意が要る。共通銘柄だけに絞った切り口でも
  アウト期間 R² は +0.226 で結論は変わらなかった
- 環境変数の未設定が黙って進む経路が2種類ある（PR #64 の調査で判明）。
  UPSTASH_REDIS_REST_URL / _TOKEN は依存が 1.38.2 になった結果、未設定でも起動は続き
  最初の Redis 呼び出しで初めて失敗する。PUSHOVER_TOKEN / PUSHOVER_USER は
  未設定チェックが無く、API は 200 を返すが通知だけが届かない。
  どちらも「設定漏れにすぐ気付ける」という前提が成立しない
- PR #50 の3点のうち、ネガティブキャッシュと2分停止は 429 が発生しなかったため未発火。
  再連射が止まったことは確認できたが、停止機能そのものの作動は未確認のまま
- parts.ranking の調査は完了。原因はほぼ特定できたが、修正は見送りとした
- PR #50〜#57・#60〜#74（daytrade-simulator）と PR #18〜#25（tachibana-server）は
  すべてマージ済み。未マージの PR は現在無い

## 進行中の案件

### 方向判定への特化（未着手・前半と後半に分割）

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: 前半は src/App.js の PremarketPanel の表示部分のみ。
  後半は api/sync.js の predictGapByQuote 周辺（2箇所管理の解消が先）
- 背景: 9/10 の13営業日1656件の検証で、「どれだけ動くか」の精度は
  ゼロ予想比で53.3%の勝率にとどまる一方、「どちらに動くか」は
  対象を絞ると大きく精度が上がることが分かった。
  全銘柄 71.4% に対し、買い比率が50から10以上離れた銘柄で 81.6%、
  20以上で 90.2%、20以上かつ前日終値500円以上で 91.5%
- 前半（表示の絞り込みのみ・予想式に触らない）:
  - 買い比率の偏り（|buyRatioLast − 50|）が一定以上の銘柄を画面上で区別する
  - しきい値は10を基準とする。20以上は205件と数が減り、25以上ではむしろ
    87.4% に下がるため、絞りすぎない
  - 前日終値500円未満を別扱いにするかは実装時に判断
  - 予想式・通信内容・既存の表示は変更しない
- 後半（確信度の見直し・2箇所管理の解消後）:
  - 板の一貫性を確信度に反映する。buyRatioMin と buyRatioMax がともに
    50超、またはともに50未満の銘柄は方向一致率 79.7%（636件・全体の53%）。
    50から3以上離れた条件で 81.8%、5以上で 83.9%。
    現行式は buyRatioLast しか見ておらず、途中でひっくり返った銘柄と
    一貫していた銘柄を区別していない
  - 方向の確度を、動く幅の予想とは別に表示するかを検討する
- 次の一手: 前半に着手する。まず調査タスクを1本挟み、
  PremarketPanel の表示部分で買い比率とその最小・最大にどこからアクセスできるか、
  絞り込み表示を足すとして既存のどの表示と競合するかを洗い出す

### 反映判定ログ（applied）の追加（PR #74・マージ済み・実運用確認済み）

- 状態: PR #74 マージ済み・Vercel ビルド通過確認済み。9/10 朝に実運用で動作を確認した
- 触るファイル: src/App.js（実装済み・以後は不要）
- 背景: 9/9 の調査で、既存の描画件数ログでは「サーバー予想が画面へ反映されたか」を
  判定できないことが確定した。行数はお気に入りの日本株銘柄数で決まり、
  サーバー予想の件数に影響されないため、件数が変わったときだけ記録する
  既存の作りでは何も残らない
- 実装内容:
  - pmBuildResults の戻り値に srvCount（fromServer が true の要素を持つ行の本数）と
    missCodes（サーバー予想が乗らなかった銘柄コード・先頭10件）を追加
  - pmObsApplied を新設。type:"applied" / via / srv / rows / miss の5項目を1件記録する
  - 結果を渡している3経路で、setResults の直後かつ alive 判定を通ったあとに呼び出す。
    via は init（お気に入り変更・9:00またぎの useEffect）／
    retry（予想の再取得タイマー）／manual（更新ボタンからの refresh()）の3値
  - PM_MISS_CODES_MAX（10）を新設
- 実装前確認で判明した訂正: setResults の呼び出しは想定の2箇所ではなく6箇所。
  うち予想結果を渡しているのは3箇所で、3つ目は更新ボタンからの手動再取得だった。
  残り3箇所は null を渡すだけのため記録対象外とした
- 9/10 の実運用で分かったこと:
  - applied は6件記録され、via の内訳は init 1件・retry 5件で想定どおりだった
  - srv が 0 から152へ切り替わる時刻が、通信ログの found:true と完全に一致した
  - miss は5件で上限10件に届かず、差分の全件が特定できた
  - rows はお気に入りの日本株数のため、お気に入り総数の確認には使えなかった
- 検証: npm run build 終了コード0・Compiled successfully。
  CI=true でも同じ結果で警告0件。eslint-disable-next-line は生成していない
- 次の一手: なし。運用に入った。miss の顔ぶれが日替わりかどうかを数営業日ぶん観測する

### 観測ログ機能（PR #73・マージ済み・実運用確認済み）

- 状態: PR #73 マージ済み。9/9・9/10 の2営業日で実運用の動作を確認した
- 触るファイル: src/App.js（実装済み・以後は不要）
- 背景: 朝の 8:45〜9:12 は在席できる日とできない日があり、
  DevTools を開けない日は何も残らなかった。9/8 も 8:46・8:58 を計測できず、
  Console の確認もできなかった。人の在席を前提にした観測は成立しないと判断した
- 実装内容: localStorage に3キーを新設し、あとから読み出せる形で記録する
  - obslog_net … 通信ログ。リングバッファ100件。
    時刻 / resource / リクエストの date / HTTP ステータス / 所要ms / rows件数 /
    partial / open が null の件数 / buyRatioAvg 保有件数。
    premarket-prediction は rows を持たないため、代わりに found /
    predictions のキー数 / 応答の date（resDate）を記録する
  - obslog_event … イベントログ。リングバッファ100件。
    JavaScript エラー（error と unhandledrejection の両方）、
    visibilitychange、予想の描画件数（件数が変わったときのみ）、
    9:00〜9:12 の daily 取得回数（fetchDaily 経由と pmFetchRecentDaily 経由を別々に）、
    applied（PR #74 で追加）
  - obslog_beat … 心拍。30秒おきの時刻のみ。直近60分ぶん
- 画面への出口: 寄り前タブの「気配スナップショット」の直下に
  「観測ログ」の折りたたみ帯を新設。件数表示とコピー・クリアボタン。
  コピーは既存 copyPqAll と同じ3段構え（clipboard API → textarea+execCommand → prompt）
- 実運用で分かった限界:
  - 描画件数ログでは反映の有無を判定できない（PR #74 で対処済み）
  - イベントログの並び順が時刻順になっていない。
    daily の上書きレコードが元の位置に残るためと見られる。目視で追うときに混乱する
  - premarket-summary の行が出ないのは記録側の問題ではなく、
    そもそも通信が発生しない設計のためと 9/10 の調査で確定した
  - アプリ内タブの切替を記録する仕組みが無いため、パネルが何時にマウントされたかを
    観測ログから読めない
- 副作用:
  - localStorage の使用量が数十KB増える。容量が逼迫した端末では既存データの保存が
    先に失敗しうる（観測ログ側は握りつぶすため画面は壊れない）
  - JSエラーが連続する状況ではイベントログ100件がエラーで埋まり、
    描画件数や visibilitychange の記録が押し出される
  - 心拍はアプリを開いている間だけ記録されるため、途切れている＝異常とは限らない
  - daily カウンタは 9:00〜9:12 にアプリを開いていた場合のみ記録される
- 検証: npm run build 終了コード0・警告なし。変更は src/App.js のみ
- 次の一手: なし。運用に入った

### quote 予想の実力測定（9/10 実施・コード変更なし・完了）

- 状態: 完了。未投入（PR番号なし）。読み取りと集計のみのため PR は無い
- 触るファイル: なし
- 実施した内容: premarket-summary（mode なし）を 8/24〜9/9 の13営業日ぶん
  ブラウザで取得し、1844行から予想生成可能かつ実測ギャップのある1656件を抽出。
  quote 予想を再現式で計算し、3つのベースラインと比較した
- 再現式: 予想 = ROUND(MIN(3, MAX(-3, 0.058 × (buyRatioLast − 50) − 0.105)), 2)。
  生成条件は buyRatioLast と validCount がともに非 null
- ベースライン3種: 全銘柄ゼロ予想 / 当日の実測平均を全銘柄に当てる（カンニング系・
  地合いを完璧に当てた場合の上限を表す） / 前日ギャップをそのまま当てる
- 結論:
  - quote 予想には予測力がある。アウト期間 R² +0.19、8通りの切り口すべてでプラス
  - ただし勝率では 53.3%（銘柄単位）、日単位では8営業日で4勝4敗
  - 当日平均（カンニング系）はアウト期間 R² +0.205 で quote とほぼ互角。
    地合いを当てるだけで板情報と同等の精度が出ることを示す
  - 前日ギャップは R² −0.93 で無価値
  - 方向判定に絞れば大きく精度が上がる（別項目へ）
- 手順の再現方法: premarket-log&date=list で対象日を確認し、
  日付ごとに premarket-summary を叩いて rows を取る。
  mode=calib は銘柄コードも予想値も返さないため使えない。
  生ログ（premarket-log&date=<日付>）は3MB前後のため使わない
- 次の一手: なし。営業日30日ぶん貯まった時点で同じ手順を再実行する

### 寄り前気配の予想が作られない問題・サーバー側（段1〜段4・PR #72 マージ済み・実装完了）

- 状態: 段1 PR #69 マージ済み・ブランチ削除済み。段2 PR #25 マージ済み・
  Railway 再デプロイ済み。段3 は 9/7 朝に実施し全項目通過。
  段4 PR #72 マージ済み。サーバー側の実装としては完了。
  ただし段4 の成果を端末から確認する手段が現状のフロント側に無いことが
  9/10 の調査で判明した（下記「次の一手」）
- 触るファイル: なし（着手する場合は src/App.js の PremarketPanel）
- 対策の方針: 収集の途中で1回だけ、その時点までの records を暫定キーへPOSTする。
  途中経過も本番と同じ records 形式でそのまま送り、買い比率などの集計を
  サーバー側に新規実装しない。集計が2箇所に分かれると 9:00 前と 9:06 以降で
  数字が食い違ったまま画面上は正常に見えるため
- 送信タイミングは 8:57 の1回。当初案の 8:52・8:56・8:59 の3回から減らした
- 段1 の実施内容（PR #69）: handlePremarketLog の POST に body.partial === true の
  分岐を追加。保存先を premarket:log:partial:<日付> へ切り替え、追記ではなく上書き、
  TTL は6時間（PREMARKET_LOG_PARTIAL_TTL）。本番キーへ流れる経路は無い。
  併せて listPremarketDates に isDateString のフィルタを追加した
- 段2 の実施内容（PR #25）: postPartialLog を新設。判定は
  jstMinuteOfDay(nowJst()) >= PARTIAL_MINUTE（8*60+57）。
  ローカル変数 partialPosted で1セッション1回に制限。リトライなし・タイムアウト10秒
- 段3 の実測結果（9/7・全項目通過）:
  - Railway ログ 8:57:07 に「途中経過を保存しました: 2026-09-07 48レコード / 1749KB」
  - 48レコードは 8:45〜8:57 のティック数48と完全一致。1レコード＝1ティック（158銘柄ぶん）
  - 最終は 9:06:07 に 84レコード / 3095KB。48/84 の比率から換算すると 1768KB となり
    暫定の実測 1749KB とほぼ一致。途中経過は本番と同じ密度で欠けなく送られている
  - Upstash に premarket:log:partial:2026-09-07 が存在。Size 321KB / TTL 5h49m。
    ログの1749KB と数字が違うのは packForRedis による gzip 圧縮のため（約5.4倍）
  - 本番側も正常。収集終了 84件（エラー0件）1261秒 / 件数不一致 0ティック /
    POST 158銘柄 3095KB 84レコード / 保存 84件 / 予想生成 154件（見送り4件）
  - 段2 が本番の収集に影響していないことを確認した
- 段4 の実施内容（PR #72）: handlePremarketSummary が、
  本番キーの読み取り結果が配列でなかったときに限り
  premarket:log:partial:<日付> を redis.get し、要素1件の配列に包んで
  summarizePremarketDate へ渡す。応答に partial ブール値を追加。
  dates.forEach を for ループへ書き換え（forEach 内では await が効かないため）。
  展開は本番・暫定とも unpackFromRedis を通す。
  CLAUDE.md の Redis キー一覧へ premarket:log:partial:<日付> を追記
- 段4 の実装前確認で判明した訂正: mode=calib と mode=coverage は
  handlePremarketSummary を経由しない。ルーティング側（resource === 'premarket-summary'
  のブロック）で handlePremarketCalib / handlePremarketCoverage へ分岐しており、
  退避経路が流れ込む経路自体が存在しなかった
- 段4 の副作用: 8:45〜9:06 に取得したサマリーは途中経過のため、
  寄り値と前日比ギャップが null のままの行が出る。これは正常（まだ寄っていないため）。
  9:06 以降は本番キーが優先され確定値に戻る。
  暫定キーの TTL は6時間で当日15:00前後に消えるため、午後の確認は時間切れに注意
- 9/10 の調査で判明した重大な前提の誤り: 端末側は 8:45〜9:00 に
  premarket-summary を自発的に叩く作りになっていない。周期タイマーは存在せず、
  パネル内の setInterval は「9:00 をまたいだかの判定用（60秒）」と
  「9:00以降の予想再取得用」の2本のみ。前者は 8:45〜9:00 の間ずっと同じ false を
  セットするため依存値が変わらず effect を再実行しない。
  したがって段4 の実装は正しく入っているが、その成果を受け取る側が存在しない
- 次の一手: 8:45〜9:00 に途中経過を取りに行く仕組みを端末側へ足すかどうかの判断。
  足すなら周期タイマーを1本追加することになるが、これは「寄り前の取得を
  タブから切り離す（C案）」と同じ箇所を触るため、
  ブラウザ側の予想生成の撤去を先に済ませる方針を守る。
  当面は手動更新ボタンで 8:45〜9:00 に1回叩けば partial の値そのものは確認できる

### 寄り前気配の予想が作られない問題・フロント側（PR #71・マージ済み・完了）

- 状態: PR #71 マージ済み。9/8・9/9・9/10 の3営業日で効果を実測で確認した
- 触るファイル: なし
- 背景: 9/7 の観測で、8:45〜9:08 に寄り前系の通信が1本も出ないことが判明した。
  寄り前パネルは activeTab==="premarket" の条件付きレンダリングで、
  タブを選ばない限りマウントされず useEffect もタイマーも存在しなかった。
  activeTab の初期値は "fav" の直書きで localStorage 復元が無かった
- 実施した内容（変更1・タブの記憶）:
  - ACTIVE_TAB_KEY（値は "active_tab"）と ACTIVE_TAB_IDS を新設
  - loadActiveTab() は保存値が ACTIVE_TAB_IDS に含まれるときだけ返し、それ以外は "fav"
  - useState("fav") を useState(loadActiveTab) へ変更し、保存用の useEffect を追加
  - 既存の usePersistedState を使わなかった理由は、保存値をそのまま採用する作りで
    「想定外の値なら fav に落とす」要件を満たせないため
  - ACTIVE_TAB_IDS を別に持った理由は、判定に使いたい TABS が App() 内のローカル変数で、
    モジュール直下の loadActiveTab() から参照できないため。同期が必要な旨をコメントに明記
  - 生の文字列で保存したのは、既存の getLayoutMode / setLayoutMode と流儀を揃えたため
- 実施した内容（変更2・9:00〜9:12 の再取得）:
  - PM_PRED_RETRY_MS（75秒）と PM_PRED_RETRY_UNTIL_MIN（JST 9:12）を新設
  - pmBuildResults の戻り値に serverOk を追加。既存の date / rows は無変更
  - PremarketPanel に再取得用の useEffect を追加。成功時点で停止、9:12 で打ち切り
  - force を付けて呼ぶのは、再取得間隔75秒が PM_PRED_TTL（3分）より短く、
    force なしではキャッシュに阻まれて実質再取得にならないため
  - 既存の1分タイマーに相乗りさせなかったのは、あれが「9:00 をまたいだか」の
    判定専用で間隔60秒固定のため、条件を混ぜると本来の役割まで読みづらくなるため
- 9/8 朝の実測: premarket-prediction が5本発生し、0.3kB の空振り4本のあと
  4.7kB の成功1本。見積もり「9:06 前後で成功し5回程度」と一致した。
  画面には寄り前気配とベータ推定の2行が並んだ
- 9/9 朝の実測: 09:01:11 から5回、5回目の 09:06:21 に found:true / 155件で成功。
  日足取得は785回（157 × 5）
- 9/10 朝の実測: 09:00:36 から6回、6回目の 09:07:01 に found:true / 153件で成功。
  日足取得は942回（157 × 6）。成功が1回ぶん遅くなったが打ち切り 9:12 には余裕がある
- 副作用:
  - 前回のタブが「デバイス同期」や「使い方」だと次回もそこから始まる
  - 9:00〜9:12 の間だけ通信量が増える。1回ごとにお気に入り銘柄の日足取得を伴う。
    PM_TODAY_TTL（5分）のキャッシュは効く
  - 「最終更新」の時刻表示が手動操作なしに変わる
  - タブ名を増減したとき ACTIVE_TAB_IDS の更新を忘れると、そのタブから復元されない
- 次の一手: なし。完了

### package-lock.json の typescript 不整合の解消（PR #70・完了）

- 状態: PR #70 マージ済み・Vercel ビルド通過確認済み。完了
- 触るファイル: なし
- 経緯: 当初 HANDOFF.md に「記録側が 4.9.5、宣言側が 7.0.2」と記載していたが、
  9/5 の調査で事実と異なることが判明した。正しくは記録側が 7.0.2、宣言側は不在。
  4.9.5 はリポジトリ内に存在せず、npm が実行時にレジストリから計算する値だった
- 実測で確定した内容: クリーンな環境からの npm ci は終了コード1・EUSAGE で失敗する。
  停止理由は、記録側の typescript 7.0.2 が react-scripts 5.0.1 の peer 範囲
  （^3.2.1 || ^4）を満たさないこと。package.json の直接依存6件と lock の root
  エントリは一致していたが、npm ci は lock 内の peer 依存の解決結果まで検証する
- 訂正: 「Vercel のビルドは通っているため実害は出ていない」も誤り。
  Vercel が npm install を使っていたため通っていただけで、npm ci は再現性をもって失敗していた
- 実施した内容: package.json に devDependencies を新設し typescript ^4.9.5 を1件追加。
  npm install で package-lock.json を再生成した。+12行 / −368行
- 検証: npm ci・npm run build ともに終了コード0。ビルド警告0件。
  Vercel の本番ビルドも通過を確認した
- 次の一手: なし。完了

### validCount 62以上を「大きく動く銘柄」の先行指標として使う（未着手）

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: 着手時は src/App.js の表示側が候補。予想式には手を入れない
- 内容: 9/4 に validCount 62以上の12銘柄が例外なく絶対値1.94%以上で寄った。
  62以上は 9:00 時点で寄れなかったことを表し、寄れないのは注文が大きく偏っているため。
  結果として大きく動くという解釈が成り立つ
- ただし収集終了（9:06）まで確定しない値のため、9:00 の寄り付きには間に合わない。
  使えるとすれば寄り後の銘柄選定になる。この点は要検討
- 次の一手: 数営業日ぶん貯めて、62以上が毎日同様に大きく動くかを確認する。
  「方向判定への特化」と目的が近いため、着手するならそちらと併せて検討する

### Phase 2B 段D-1・画面表示（PR #67・完了）

- 状態: PR #67 マージ済み（9/4 01:08）。完了
- 触るファイル: なし
- 実施した内容:
  - 答え合わせ表示（9:00以降）を1銘柄1行から、出どころごとの複数行へ変更
  - 予想一覧（9:00前）にも寄り前気配とベータ推定の2手法を並べた
  - 答え合わせの寄り前気配はサーバー（premarket-prediction の GET）を優先
  - 新設: PM_PRED_API / PM_PRED_TTL（3分）/ pmFetchServerPredictions / pmQuoteReasons
- 9/4 朝の実測: 2手法は並ばなかった。原因は本 PR の実装ではなく
  quote 予想そのものが作られていないことによる。
  9/8 朝に PR #71・#72 が入った状態で2手法が並ぶことを確認し、本 PR の実装も正しかった
- 次の一手: なし。完了

### Phase 2B 段D-2・的中率パネルの読み出し元の移行（未着手）

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: 着手時は api/sync.js と src/App.js
- 内容: 的中率パネルの集計を、端末内の記録からサーバー側へ移すかどうか
- 先行して必要なサーバー修正: premarket-prediction の GET に
  「保存済み日付の一覧を返す手段」が無い。date は必須で、
  premarket-log にある date=list に相当するものが存在しない
- 推奨（必須ではない）: predictions の各銘柄に reasons を含める
- 次の一手: 移行するかどうかの判断が先。
  9/8 に quote 予想が画面へ到達したため、「移行しても集計対象が無い」という
  従来の障害は解消に向かう。数営業日ぶん貯まってから判断する

### 地合いの項を予想式に入れる（保留・着手しない）

- 状態: 保留。未投入（PR番号なし）。9/10 の検証により当面は着手しない判断
- 触るファイル: 着手時は api/sync.js の predictGapByQuote と
  src/App.js の pmPredictGapByQuote が候補。ただし2箇所管理の解消が先
- 内容: quote 予想には地合いを表す項が無く、切片が固定値でその役目を代行している
- 9/10 の検証結果:
  - 既存の「今朝の地合い」パネルの値は保存されておらず過去0日ぶんのため使えない
  - 買い比率の日平均は代理指標としての相関が 0.886（中央値なら 0.904）と高いが、
    予想式に組み込むとアウト期間の R² が +0.19 から +0.07 へ悪化する
  - イン期間では改善しアウト期間で悪化するという、過学習の典型的な形
  - 上限の見積もりも小さい。当日の実測平均を全銘柄に当てるカンニング系の
    ベースラインでも R² は +0.205 で、現行の +0.192 とほぼ変わらない
- 保留の理由: 日平均としては13点しかなく係数を決めるには足りない。
  実際に組み込むと悪化するという結果が出た以上、今のデータでの実装は危険
- 次の一手: 営業日30日ぶん貯まった時点で係数を推定し直して再評価する。
  それまで着手しない。データは自動で貯まるため待つコストは無い

### 極端な買い比率の扱いの見直し（未着手・判定材料は判明）

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: 着手時は api/sync.js と src/App.js の PM_Q_EXP_LIMIT 周辺が候補。
  ただし2箇所管理の解消が先
- 内容: 買い比率が極端な銘柄で予想が過剰になる
- 9/4 で分かったこと: 外れの実体は「寄り値が前日終値から動かない銘柄を
  予想対象にしている」ことだった
- 9/10 で分かったこと: 「板の薄さ」の判定材料は株価水準で代用できる。
  前日終値500円以上に絞るとアウト期間の R² が +0.192 から +0.228 へ上がる。
  気配数量の絶対値を records に足す必要は当面ない
- 次の一手: 「方向判定への特化」と実質的に同じ話になったため、
  そちらと統合して進めるかを判断する。上限で丸める案は引き続き筋が悪い

### CLAUDE.md への追記（PR #68・完了）

- 状態: PR #68 マージ済み。完了
- 触るファイル: なし
- 実施した内容: 段D-1 の調査で判明した恒久的な前提3点を CLAUDE.md へ追記。
  サーバー予想が 9:06 前後にしか保存されないこと、日付関数が
  fcTodayJST と pmTargetDate の2系統あること、サーバー取得ぶんを
  localStorage の pm_<ticker> へ書き戻さないこと
- 次の一手: なし。完了

### /api/daily 連打対策（PR #50・完了・停止機能のみ作動未確認）

- 状態: PR #50 マージ済み・ブランチ削除済み・ENV_CHANGELOG.md 追記済み
- 触るファイル: なし
- 次の一手: 場中（9:00〜15:30）に同じ手順で再確認できれば、
  429 検知時の一時停止の作動まで見られる。機会があれば見る程度でよい

### /api/daily の呼び出し量そのものの削減（PR2）

- 状態: 未着手。未投入（PR番号なし）。8/27 の実測を受けて優先度を引き下げた
- 触るファイル: src/App.js の fillDayNightFor と visibilitychange のリスナ登録部分
- 次の一手: 当面は観測のみ。表示銘柄数が増えて 429 が再発したときに着手する。
  pmFetchRecentDaily は 9/9 に785回（157 × 5）、9/10 に942回（157 × 6）走った。
  429 は発生していないが、お気に入りが増えると比例して増える点は把握しておく
- 優先度を下げた理由: 429 が0件で転送量も軽い。今すぐ直さないと壊れる状態ではない

### parts.ranking の原因調査（調査完了・修正は見送り）

- 状態: 8/28 に Claude Code で調査実施（変更なし）。未投入（PR番号なし）
- 触るファイル: なし
- 調査で確定した内容: 6016ms の主因は tachibana-server の getRankingData
- 見送りの理由: 組み立て回はスキャンせず即返す仕様のため、6秒は体感に出ない
- 次の一手: 予算超過が実際に観測された時点で再評価する

### validCount の定義訂正（PR #65・#66・完了）

- 状態: PR #65 マージ済み（内容に誤りあり）。PR #66 マージ済み。完了
- 触るファイル: なし
- 調査で確定した内容:
  - validCount は ratios.length。加算処理は存在せず配列長を取るだけ
  - カウント条件は3つ。pAAV が数値化できること、pABV が数値化できること、
    その合計が0より大きいこと。片側が空文字なら除外、片側が数値の0なら算入される
  - 上限は quoteCount
  - 寄り付き後を除外する明示的な判定は無い
  - noOpen は row.open == null で判定しており validCount を見ていない
- 次の一手: なし。完了

### 較正係数の再確認と群別の検証（完了・コード変更なし）

- 状態: 9/3 に実施。未投入（PR番号なし）。読み取りのみのため PR は無い
- 触るファイル: なし
- 結論:
  - 傾き・切片とも変更しない
  - 本番が使っている buyRatioLast は、5変種のうち rRank が最も高かった
  - buyRatioMin が50超の群の dirRate が 0.602（全体は 0.517）
  - validCount の群差は上げ相場では消えた。確信度への反映は見送る
- 注意: mode=calib の dirRate は「買い比率が50超の行のうち実測がプラスだった割合」で、
  「予想の符号と実測の符号の一致率」とは別の指標。9/10 の方向一致率と並べて読まないこと
- 次の一手: なし。完了

### 寄り前収集の安定確認（完了・コード変更なし）

- 状態: 完了。未投入（PR番号なし）。確認作業のみのため PR は無い
- 触るファイル: なし
- 結果: 8/31・9/1・9/2 の3営業日とも全項目通過。9/7 も同様に通過した
- 次の一手: なし。完了

### 環境変数の棚卸しと一覧表の新設（PR #64・完了）

- 状態: PR #64 マージ済み。完了
- 触るファイル: なし
- 実施した内容: CLAUDE.md に「環境変数」の節を新設。Vercel Functions（15件）と
  GitHub Actions（3件）の2表・3列構成。docs/ENV_AUDIT.md は調査記録として凍結
- 調査で確定した内容:
  - 較正係数は PM_Q_SLOPE と PM_Q_INTERCEPT の2つのみ。他の PM_Q_ 系は直書き定数
  - PM_HIST_MAX は環境変数ではない（src/App.js の直書き定数・値180）
  - TACHIBANA_RELAY_SECRET は実質必須。未設定だと認証が要る5経路すべてが 401
- 次の一手: なし。完了

### 暫定保持28件の移設（PR #21・#60・#61・#62・完了）

- 状態: 4本ともマージ済み。docs/HOLD.md は0件の受け皿として存置。完了
- 触るファイル: なし
- 調査で判明し記述を訂正した点:
  - 組み立ての条件は「マークの値が 日付:スロット と一致しないとき」だった
  - summarizePremarketDate の依存は本体を含めて4箇所だった
  - api/sector.js の AI 呼び出しは ?sectors= が付かない経路では現役で呼ばれる
  - PREMARKET_CODES の158件は Railway の環境変数側の値
- 次の一手: なし。完了

### 気配収集の120件上限の解消（PR #19・完了）

- 状態: PR #19 マージ済み・Railway 再デプロイ済み。完了
- 触るファイル: なし
- 実測結果: 8/31・9/1・9/2 とも件数不一致0ティック / tick失敗0件 /
  84ティック全てが158銘柄 / missing 0件。38銘柄の取りこぼしは解消した
- 補足: 9/10 の実力測定で、8/25〜8/28 の count が4日とも120ちょうど、
  8/24 が100、8/31 以降が158であることを確認した。
  この PR の投入時期（8/30）と完全に整合する
- 次の一手: なし。完了

### 寄り予想のサーバー側移行（PR #56・#20・#57・完了）

- 状態: 3本ともマージ済み。7営業日連続で自動生成が成功。完了
- 触るファイル: なし
- 実施した内容:
  - PR #56 … api/sync.js に resource「premarket-prediction」を新設
  - PR #20 … 生ログの保存成功時のみ Railway 側から生成APIを1回叩く
  - PR #57 … CLAUDE.md に新 resource を追記
- 移植の検証: 11,983ケースで移植元と出力が完全一致。不一致0件
- 実測結果: 8/31 は158件、9/1 は154件、9/2 は155件、9/3 は152件、
  9/4 は155件、9/7 は154件（見送り4件）、9/9 は155件、9/10 は153件
- 次の一手: なし。完了

### tachibana-server README の実装追随（PR #22・#23・#24・完了）

- 状態: 3本ともマージ済み。完了
- 触るファイル: なし
- 調査で確定した内容:
  - 予想生成は生ログの保存が成功したときだけ1回呼ばれる
  - 起動ログの実出力順は premarket件数 → watcher → scan → premarket起動 →
    premarket窓外 → webapi
- 次の一手: なし。完了

### PM_Q_MIN_TICKS の撤去（PR #63・完了・結論に誤りあり）

- 状態: PR #63 マージ済み。実装は完了しているが、撤去時の根拠に誤りが判明した
- 触るファイル: なし
- 訂正: PM_Q_MIN_TICKS は環境変数ではなく直書き定数だった。
  「実測の最小値は60」も誤り（8/31 に 50、9/1 に 21、9/2 に 11）。
  「5未満で弾かれる行は存在しない」も 9/4 に覆った（7713 の validCount が 1）
- 次の一手: なし。水準の見直しは「極端な買い比率の扱い」と一体で検討する

### Upstash 月間リクエスト上限への対処（完了・コード変更なし）

- 状態: 完了。未投入（PR番号なし）
- 触るファイル: なし
- 実施した内容: 8/29 未明に無料枠50万コマンドへ到達し停止。
  Vercel Dashboard 経由で Pay As You Go へ移行。Max Budget 20ドル
- 次の一手: なし。月に一度 Usage を見て金額を確認する

## 次にやること

上から順に:

1. 同期タブの「◯◯銘柄」の表示を見て、rows 157 との差を確認する。
   差が米国株ぶんで説明が付けば「157 がお気に入り総数か」の論点は閉じる。
   次にアプリを開いたときで足りる
2. 9/10 の朝が普段と同じURLだったかを確認する。
   daytrade-simulator-git-main-... で観測したが、端末内保存はURLごとに別領域のため、
   普段と違うURLなら「気配スナップショット 0日分」は正常な結果になる。
   これが確認できるまで項目7 の調査には入らない
3. 翌朝の applied の miss を見る。9/10 は 4424 / 7713 / 7749 / 7760 / 7781 の5件。
   同じ顔ぶれなら銘柄側の事情、日替わりなら収集タイミング側と切り分けられる。
   判定の目安は次のとおり
   - applied が出て srv と rows が近い数字なら正常
   - applied が出て srv が0なら、取得はできたが行に流し込めていない
   - applied が出ないなら、取得後の日足取得の途中で落ちたか中断した
   - srv と rows の差が常時5本以内なら許容範囲、10本超なら収集側を疑う
4. 方向判定への特化・前半（表示の絞り込みのみ）。
   買い比率の偏りが10以上の銘柄を画面上で区別する。予想式には触らないため
   項目5 を待たずに着手できる。まず調査タスクを1本挟み、
   PremarketPanel の表示部分から買い比率とその最小・最大にどうアクセスするか、
   既存の表示と競合しないかを洗い出す。実際の朝の画面で使えるかを
   早めに確かめてから後半へ進む
5. ブラウザ側の予想生成を外す。較正係数の2箇所管理を解消しないと
   確信度の見直しも極端気配の扱いも途中経過の取得も手を付けられないため、優先度が高い。
   9/8 にサーバー予想が画面へ出ることを確認したため前提条件は満たされた。
   着手時はまず調査タスクを1本挟み、ブラウザ側の予想生成がどこから呼ばれ、
   撤去したとき何が画面から消えるのかを先に洗い出す
6. 方向判定への特化・後半（確信度の見直し）。
   板の一貫性（buyRatioMin と buyRatioMax が同じ側か）を確信度に反映する。
   項目5 の完了が前提
7. 「予想 vs 実際の始値」が全銘柄「待機中」だった件の切り分け。
   9/8 と 9/10 の2回とも再現している。daily は 9/9 に785回、9/10 に942回
   走っているため、返り値に当日の始値が乗っているかどうかを見る方向へ絞る
8. 「気配スナップショット 0日分 / 0件」の原因調査。項目2 の確認が先
9. 観測ログのイベント配列を時刻順に並べ替えるかどうか。
   9/9・9/10 とも並び順が時刻順になっておらず、目視で追うときに混乱した
10. 「今朝の地合い」の予想対象日が翌営業日へ進む件の修正判断。
    9/8 と 9/10 の2回再現し、9/10 は同じ画面内で premarket-prediction 側が
    当日で正しいことも同時に確認できた。日付関数の2系統のうち
    地合いパネル側だけを揃えれば済む見込み。調査タスク1本で着手できる
11. 「今朝の地合い」の寄与表示の修正判断。weight を掛けていないため
    7指標の寄与を足しても総合値にならない。表示の掛け算を1つ足すだけで直る
12. 8:45〜9:00 に途中経過（partial）を取りに行く仕組みを端末側へ足すかどうか。
    段4 のサーバー側実装は完了しているが受け取る側が無い。
    項目5 と項目13（C案）と同じ箇所を触るため順序を守る
13. 寄り前の取得をタブから切り離すか（C案）。タブを開かなくても記録が貯まる形。
    ただし項目5（ブラウザ側の予想生成の撤去）を先に済ませないと、
    2箇所管理のまま範囲だけ広がるため順序を守る
14. 段D-2 の判断。的中率パネルをサーバー由来にするかどうか。
    quote の記録が数営業日ぶん貯まってから
15. 群別の誤差の継続観測。62以上の群は 9/3 が7件、9/4 が12件。
    営業日10日ぶんを目安に貯めてから精度指標としての採用を再評価する
16. validCount 62以上を大きく動く銘柄の先行指標として使えるかの確認。
    項目4・6 と目的が近いため併せて検討する
17. 極端な買い比率の扱いの検討。判定材料は株価水準（前日終値500円）で
    代用できると 9/10 に判明したため、項目4・6 と統合するかを判断する
18. 営業日30日ぶん貯まった時点で、quote 予想の実力測定を再実行する。
    手順は「quote 予想の実力測定」の項に記載。併せて地合いの項の係数も
    推定し直して再評価する
19. docs/OPERATIONS.md の更新。iPad 前提で書かれているが、
    Mac mini と DevTools が使える PC が加わったため追記が要る。
    9/7 に観測された「ページ内のタイマーが止まりリロードするまで復帰しない」現象と、
    PR #73 の観測ログを使った朝の運用手順（在席不要・夜に帯を開く）も含める
20. PR2 の着手判断。429 が再発するか、表示銘柄数が大きく増えた時点で再評価
21. parts.ranking の修正着手判断。予算超過が観測された時点で再評価

## 未決の判断

- 方向判定に特化する場合のしきい値をどこに置くか。買い比率の偏り10以上なら
  523件で 81.6%、20以上なら205件で 90.2% だが、25以上では111件で 87.4% と下がる。
  数が減ると数字が暴れるため、安全に言えるのは10以上まで。
  実運用で何件残れば使えるかは、実際の画面を見てから判断する
- 方向判定に地合い（買い比率の日平均）を足すかどうか。
  方向一致率は 71.4% から 73.4%（日平均を1倍）または 76.2%（2倍）へ上がるが、
  1100件で26件の差は誤差に埋もれうる水準。動く幅の予想には悪影響があるため、
  方向表示にだけ使う形になる。営業日30日ぶん貯まってから判断する
- 「今朝の地合い」の寄与表示を直すかどうか。weight を掛けるだけの1行の変更だが、
  放置すると内訳の数字を読み違える
- 8:45〜9:00 に途中経過を取りに行く仕組みを端末側へ足すかどうか。
  9/10 の調査で、現行コードには時刻起点の周期送出が一切無いことが確定した。
  段4 のサーバー側実装（PR #69・#25・#72）は完了しているが、
  それを叩く側が存在しないため成果が使われていない。
  足すなら周期タイマーを1本追加することになるが、C案と同じ箇所を触るため
  ブラウザ側の予想生成の撤去を先に済ませる。当面は手動更新ボタンで代替できる
- 途中経過の送信回数を 8:57 の1回のままにするか、増やすか。
  3回にすると1朝あたりの送信量が約2.3倍になるため1回に絞った。
  8:58 の rows が使い物になるかを見てから判断するとしていたが、
  そもそも 8:58 に取りに行く仕組みが無いため、上の項目が先
- 9:00 時点の買い比率と 9:06 時点の買い比率は別の数字である点の扱い。
  現行の較正係数（傾き0.058）は 9:06 までのデータで求めたものなので、
  9:00 で打ち切った値に当てはめると系統的にずれる可能性がある
- 突き合わせキーの正規化を揃えるかどうか。サーバー側は trim 済み、
  フロント側は trim も大文字化もしていない。現状の miss には該当例が無いが、
  429A のような英字を含むコードで問題が出うる
- 寄り前の取得をタブから切り離す（C案）か、タブ記憶（PR #71・A案）で当面しのぐか。
  最終的には C案へ向かう方針で合意済み。ただしブラウザ側の予想生成の撤去が先。
  なお C案へ移ってもタブ記憶は残す（使い勝手として妥当なため）。
  不要になるのは B案（時間帯で初期タブを強制的に切り替える）のほう
- ACTIVE_TAB_IDS から「デバイス同期」「使い方」を除外するかどうか。
  それらのタブを開いたまま閉じると次回もそこから始まる。実害は小さいため保留
- 観測ログにアプリ内タブの切替を記録するかどうか。
  9/10 の調査で、パネルが何時にマウントされたかを観測ログから読めないことが分かった。
  render イベントは件数が変わったときしか残らないため代替にならない
- 観測ログの applied にお気に入り総数を足すかどうか。
  現状の rows は JP 絞り込み後の値のため総数が分からない。
  足すならパネル入口の (p.favs||[]).length を1項目追加するのが最小の変更で、
  新しい取得処理も追加の通信も要らない。ただし同期タブの表示で代替できる
- 観測ログのリングバッファ100件が足りるかどうか。
  9/9 は通信5件・イベント10件・心拍121件、9/10 は通信11件・イベント28件・心拍121件で、
  1朝ぶんは余裕があった。ただし心拍は60分ぶんで枠を使い切る設計のため、
  複数日を遡ることはできない。実際 9/10 のログには 9/9 の記録が混在して残っていた
- 観測ログをサーバー側へ送るかどうか。現状は端末内のみで、
  端末を替えると見られない。当面は端末内で足りる想定
- 観測ログの例外・中断時にログを残すか（C案）。PR #74 で applied を入れたため
  「到達したか否か」は分かるようになったが、到達しなかった理由までは分からない。
  applied を数日運用してから必要性を判断する
- validCount 62以上を先行指標として使う場合、値が確定するのは収集終了（9:06）のため
  9:00 の寄り付きには間に合わない。寄り後の銘柄選定に使う形になるが、
  それが実用に足るかは未判断
- 予想を作ってよい validCount の下限をどこに置くか。PM_Q_MIN_TICKS（5）は撤去済みだが、
  9/4 に 1 が出て「1点だけから予想する」ことの是非が現実の問題になった
- 完了した案件を別ファイル（docs/DONE.md など）へ移す方向で検討する。
  進行中の案件に完了済みが17件たまり、読む量が増えている。
  論点は3つ。移す基準、DONE.md に何を残すか、
  docs/OPERATIONS.md の「ドキュメントの更新方法」表に DONE.md の行を足すこと
- 段D-2 で的中率パネルをサーバー由来に切り替えるかどうか。
  切り替えると失うものが3つある。ベータ推定の過去実績が全損すること、
  保持期間が端末側の約90営業日からサーバー側の30日へ縮むこと、
  9:06 までに寄らなかった銘柄の実測が永久に空のままで集計から落ちること。
  マージするなら出どころ名を分けるか取得元のフラグを持たせる必要がある
- 低位株を較正データから除外するか、重み付けで扱うか。
  9/10 の検証では前日終値500円以上に絞ると R² が上がったため、
  除外する方向に一定の裏づけがある
- 158件という収集規模を維持するか。ペイロードの余裕が約400KB しかない
- 段A の係数を158件の母集団で作り直すかどうか。当面は据え置き
- 較正データに入る偏りをどう扱うか。9:06 までに寄らない銘柄は答え合わせから外れる。
  9/10 の集計でも 1844行のうち実測ギャップが取れたのは1656件で、
  除外はランダムではなく荒れた日に集中する（8/31 の noOpen 16件など）
- ブラウザ側の予想生成（src/App.js）をいつ外すか。
  外すときに較正係数の直書きも削除し、2箇所管理を解消する
- Redis の読み取り回数を削減するかどうか。常時ポーリングが1日1.5万コマンド
- quote 予想の確信度の上限60%をいつ引き上げるか
- サーバー側の predictions に reasons を含めるかどうか
- 9:06 までに寄らなかった銘柄の実測をサーバー側で後追い補完するかどうか
- CLAUDE.md の「TTL は用途ごとに5種類」という文言。値の種類としては5で正しいが
  用途の数と読める曖昧さがある。PR #72 で PREMARKET_LOG_PARTIAL_TTL を
  キー一覧へ追記したため、件数そのものの見直しも次に CLAUDE.md を触るときに行う
- .env.example を作るかどうか。PR #64 で CLAUDE.md に一覧表ができたため必要性は下がった
- TACHIBANA_ENV が現在デモと本番のどちらで稼働しているか
- Claude Code への「食い違えば実装せず報告のみで止める」指示の書き方。
  9/7 は調査タスク・PR #71・PR #72 の3本すべてで、9/8 は PR #73 で、
  9/9 は PR #74 で実装前確認が正しく報告された。
  9/10 の調査タスクでは「premarket-summary が 8:46 と 8:58 に送出される設計」という
  前提そのものが誤りであると報告され、しかもその食い違い自体が
  調査の答えになっていた。指示文で「食い違えば止める」と書いていたが、
  今回は止めずに残りの3項目も報告してきた点は結果的に妥当だった。
  恒久ルールとして CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- 調査タスクを実装の前に必ず1本挟むという進め方の位置づけ。
  9/7・9/9・9/10 とも調査タスクを先に投げたことで、実装せずに済む可能性や
  想定と違う構造が事前に判明した。9/10 は「3営業日連続で未確認」としていた項目が
  そもそも到達不能だったと判明し、無駄な観測を続けずに済んだ。
  同じく 9/10 の地合いの項も、実装前に手元のデータで測ったことで
  「入れると悪化する」と分かり2〜3週間の作業を避けられた。
  恒久ルールとして書き残すかどうか
- Claude Code が完了報告に添える「CI・レビューコメントを監視して自動対応する」という
  申し出をどう扱うか。マージ判断は人間が行う方針のため受け入れていない
- ENV_CHANGELOG.md の記録範囲は決着済み。通常のドキュメント変更は記録しない。
  ただし ENV_CHANGELOG.md 自身の書式・ルール変更だけは例外として1行記録する
- ENV_CHANGELOG.md の書き方ルール本体の移設先。CLAUDE.md と OPERATIONS.md のどちらか
- daytrade-simulator 側の README を作るかどうか。優先度は低い
- api/premarket.js（今朝の地合い）と premarket-prediction（銘柄ごとの寄り予想）の
  名前の紛らわしさ

## 未確認の仮説

- 9/10 の朝に開いていた daytrade-simulator-git-main-... のURLが、
  普段の朝と同じものかどうか。違う場合、端末内保存が別領域になるため
  「気配スナップショット 0日分 / 0件」は正常な結果になる
- miss の5件（4424 / 7713 / 7749 / 7760 / 7781）が毎朝同じ顔ぶれかどうか。
  同じなら銘柄側の事情、日替わりなら収集タイミング側。1営業日ぶんのため未確定
- 157 という行数とお気に入り総数の差が、米国株ぶんで説明が付くかどうか。
  同期タブの表示と突き合わせれば確認できる
- 方向判定を絞り込んだときの精度が、実運用でも再現するかどうか。
  13営業日の過去データ上の話であり、実際の朝の画面で何件残るか、
  その銘柄が使えるものかは未確認
- 買い比率の偏り20以上で 90.2%、25以上で 87.4% と下がる現象が、
  サンプル数の減少による揺らぎなのか、極端に振り切れた銘柄が
  別の性質を持つのかは未確認。9/4 の「板が薄い銘柄で比率が振り切れる」
  という見立てとは整合するが、方向判定に限れば裏づけが足りない
- イン期間（8/25〜8/28）の R² がアウト期間より低い理由。
  荒れた相場だったためか、段A の4営業日が実は別の日付なのかは未確定。
  count が4日とも120であることから 8/25〜8/28 でほぼ確実だが、
  段A の較正がどの日付範囲で行われたかを直接示す記録は残っていない
- 「今朝の地合い」の予想対象日が翌営業日へ進む原因が、
  日付関数の2系統のうち地合いパネル側だけが pmTargetDate ではない別系統を
  使っていることによるかどうか。9/8・9/10 の2回再現し状況証拠は揃ったが、
  地合いパネル側のコードそのものは未確認
- 9:06 前後の時点で答え合わせが全銘柄「待機中」になる原因。
  9/8・9/10 の2回とも再現。daily は走っているため
  返り値に当日の始値がまだ乗っていない可能性が高いが、
  始値の有無を記録していないため未確定
- 9/4 の 9:00 前の画面で「寄り前気配 ・データなし」の行が描かれていたかどうか。
  9/7・9/8 とも確認できず未決着
- 9/7 にページ内のタイマーが止まった原因。8:23 のスクショと 8:51 のスクショで
  requests 数・転送量・アプリの時計表示が完全に一致しており、リロードで復帰した。
  PC のスリープ設定は解除済みのため、システムスリープ説は否定された。
  残る候補は3つ。ディスプレイスリープ（画面だけ消える設定。システムは動くが
  ブラウザが非表示と判断してタイマーを間引くことがある）、
  App Nap（ウインドウが隠れている、または操作のないアプリの処理速度を
  macOS が落とす機能。スリープ設定とは無関係に働く）、
  ブラウザのバックグラウンドタブ間引き（Chrome は非表示タブのタイマーを
  1分に1回程度まで落とし、5分以上非表示だとさらに強く絞る）。
  9/9・9/10 とも心拍が121回すべて欠けずに記録され、2営業日連続で再発しなかった。
  ただし原因が特定できたわけではない
- 段4 が入った状態で 8:58 の rows が実用に足るか。48ティックぶんの買い比率から
  作る予想が、84ティックぶんの確定値とどれだけ違うかは未確認。
  取りに行く仕組みが無いため現状は手動更新でしか確かめられない
- validCount が60未満になる原因。売り一色・買い一色で片側の気配数量が
  空になるためと考えているが、立花APIの仕様は未確認
- 62以上の群が地合いに対して下振れするかどうか
- validCount 62以上が「大きく動く銘柄」を表すという解釈。1営業日ぶんの観測にとどまる
- 買い比率が極端になる銘柄は板が薄いという推測。
  気配数量の絶対値そのものは記録していないため因果は未確認。
  ただし 9/10 の検証で株価水準（前日終値500円）が代理になることは確認できた
- 立花APIの120件という上限が仕様上の固定値なのか可変の値なのかは未確認
- 始値が取れない銘柄に共通する事情。8/31 の noOpen 16件は大型株を含むため
  板の薄さでは説明がつかず、特別気配と見られるが未確認
- Upstash の常時ぶん1日1.5万コマンドの発生源が Railway の watcher による
  ポーリングであるという推測
- 気配ベース予想がベータ推定より当たるかどうか。9/8 に2手法が同じ画面へ並んだため
  目視比較の準備は整った。ただし当日は寄り値が「待機中」のままで
  答え合わせができなかったため、比較そのものは未着手
- 立花の sIssueCode が常に無加工の4文字であるかどうか
- Claude Code のセッションが認識する日付が JST と1日ずれる場合がある
- UPSTASH_REDIS_REST_URL / _TOKEN が未設定のときの挙動が変わった理由
- KV_REST_API_URL / KV_REST_API_TOKEN が Vercel 側に実際に注入されているかは未確認
- Vercel が今後 npm ci を選ぶかどうか
- typescript 7.0.2 が package-lock.json に入った時期

## 直近の実測値

- quote 予想の実力測定（9/10・8/24〜9/9 の13営業日・全1844行のうち有効1656件）:
  - アウト期間（8/31〜9/9・1199件）: R² +0.1917 / 方向一致率 0.715 /
    平均絶対誤差 0.963%
  - イン期間（8/25〜8/28・457件）: R² +0.0448 / 方向一致率 0.701 /
    平均絶対誤差 1.186%
  - 切り口別のアウト期間 R²: 全件 +0.192 / |gapPct|≤10 +0.202 /
    prevClose≥500 +0.228 / 共通銘柄 +0.226。8通りすべてでプラス
  - ベースライン（アウト期間）: ゼロ予想 R² 0（定義上）/ 平均絶対誤差 1.05%、
    当日実測平均（カンニング系）R² +0.205、前日ギャップ R² −0.93
  - ゼロ予想との勝敗: 銘柄単位 1199件中 勝ち639 / 負け553 / 引き分け7（53.3%）。
    日単位では8営業日で4勝4敗
  - 前日ギャップとの勝敗: 1123件中 勝ち711 / 負け407 / 引き分け5（63.3%）。
    日単位では7勝1敗
- 方向一致率の絞り込み別（アウト期間・9/10）:
  - 全件 71.4%（1089件）
  - |買い比率−50|≥5 で 76.8%（802件）／≥10 で 81.6%（523件）／
    ≥15 で 85.0%（354件）／≥20 で 90.2%（205件）／≥25 で 87.4%（111件）
  - ≥20 かつ prevClose≥500 で 91.5%（177件）／≥10 かつ同条件で 81.4%（484件）
  - |予想|≥0.5% で 82.0%（595件）／≥1.0% で 88.4%（284件）／≥2.0% で 93.8%（32件）
  - buyRatioMin と Max がともに50から0以上同じ側で 79.7%（636件）／
    3以上で 81.8%（521件）／5以上で 83.9%（440件）
  - 材料の比較: buyRatioLast 71.4% / buyRatioAvg 72.1% / buyRatioMin 70.6% /
    buyRatioMax 64.5% / Last+日平均 73.4% / Last+日平均×2 76.2% /
    Avg+日平均 74.8% / 日平均のみ 68.4%
- 日別の買い比率平均と実測ギャップ平均（9/10・13営業日）:
  8/24 は 53.82 / +0.433、8/25 は 52.10 / −0.251、8/26 は 52.58 / +0.056、
  8/27 は 56.05 / +0.893、8/28 は 51.31 / −0.384、8/31 は 45.25 / −0.798、
  9/1 は 50.40 / −0.489、9/2 は 44.07 / −1.331、9/3 は 51.52 / +0.584、
  9/4 は 51.11 / +0.271、9/7 は 54.88 / +0.557、9/8 は 47.43 / −0.675、
  9/9 は 48.68 / −0.027。
  相関は buyRatioLast の日平均で +0.886（傾き +0.1599 / 切片 −8.196）、
  buyRatioAvg の日平均で +0.868、buyRatioLast の中央値で +0.904
- 地合い項を組み込んだ場合（9/10・イン期間で係数推定）:
  アウト期間 R² は +0.1917 → +0.0690（案A・残差回帰）または +0.0745（案B・同時推定）。
  prevClose≥500 でも +0.2275 → +0.1423 / +0.1502。
  イン期間は +0.0448 → +0.0531 / +0.0533 とわずかに改善。方向一致率のみ
  アウト期間で 0.715 → 0.739 と上昇
- 各日の premarket-summary の count（9/10 取得）: 8/24 が100、
  8/25〜8/28 が各120、8/31 以降が各158。partial は13日ともすべて false
- 9/10 朝の applied（6件）: 09:00:46 via init srv 0 / rows 157、
  09:02:02 via retry srv 0 / rows 157、09:03:17 via retry srv 0 / rows 157、
  09:04:32 via retry srv 0 / rows 157、09:05:56 via retry srv 0 / rows 157、
  09:07:02 via retry srv 152 / rows 157。
  最後の miss は 4424 / 7713 / 7749 / 7760 / 7781 の5件（157 − 152 と一致）。
  それ以前の5件の miss はいずれも 7203 / 5242 / 7013 / 8411 / 429A /
  7003 / 6141 / 1662 / 4733 / 4684 の10件（上限で切られた先頭10件）
- 9/10 朝の premarket-prediction（通信ログ6件）: 09:00:36 / 09:02:01 / 09:03:16 /
  09:04:31 / 09:05:46 / 09:07:01 の75秒間隔。status は6件とも200、
  所要は 325 / 333 / 339 / 342 / 333 / 372 ms。found は6件目のみ true で predCount 153。
  リクエストの date と応答の resDate は6件とも 2026-09-10 で一致
- 9/10 朝の daily 経路別回数: fetchDaily 158回 / pmFetchRecentDaily 942回。
  942 は 157 × 6 と一致する
- 9/10 朝の心拍: 08:29:22〜09:29:52 の30秒刻み121回がすべて記録され、欠けは0
- 9/10 朝の描画件数ログ: 09:00:23 に result モードで 0、
  09:00:46 に result モードで 157。以降は変化なし
- 9/10 朝の画面（9:07 時点）: 7203 が寄り前気配 −0.56%（買い比率 42.1% /
  最小31.1% / 最大46.7% / 観測61回）、ベータ推定 −0.59%。
  5242 が −0.10%（50.0% / 最小50.0% / 最大50.0% / 60回）、ベータ推定 −0.40%。
  7013 が −0.38%（45.3% / 最小31.4% / 最大46.7% / 61回）。実際の始値は全銘柄「待機中」
- 9/10 朝の「今朝の地合い」: 総合（想定ギャップ）−0.63%。
  日経225先物 −1.25% / SOX +0.37% / S&P500 −0.48% / NASDAQ −0.64% /
  ドル円 +0.06% / VIX +4.71% / NYダウ −0.77%。更新 9:07:02。
  予想対象日の表示は 2026-09-11（当日は 9/10 のため翌営業日へ進んでいる）
- 「今朝の地合い」の重みと感応度（9/10 の調査）: 日経225先物 weight 0.45 / coef 1.00、
  SOX 0.15 / 0.35、S&P500 0.12 / 0.50、NASDAQ 0.10 / 0.50、ドル円 0.08 / 0.30、
  VIX 0.07 / −0.05、NYダウ 0.03 / 0.30。weight の合計は 1.00。
  総合値 = Σ(weight × changePct × coef) ÷ Σ(weight) を ±3% でクリップ
- 9/10 朝の気配スナップショット: 0日分 / 0件（Phase 2B の校正用）
- 9/10 朝の premarket-summary: 通信ログに1件も現れなかった。
  9/10 の調査により、これは記録漏れではなく通信が発生しない設計のためと確定
- 9/9 朝の観測ログ（心拍）: 08:39:01〜09:39:31 の30秒刻み121回がすべて記録され、
  欠けは0。9/7 のタイマー凍結は再発しなかった
- 9/9 朝の premarket-prediction（通信ログ5件）: 09:01:11 / 09:02:36 / 09:03:51 /
  09:05:06 / 09:06:21 の75秒間隔。status は5件とも200、
  所要は 324 / 425 / 336 / 335 / 343 ms。found は5件目のみ true で predCount 155。
  リクエストの date と応答の resDate は5件とも 2026-09-09 で一致
- 9/9 朝の daily 経路別回数: fetchDaily 158回 / pmFetchRecentDaily 785回。
  785 は 157 × 5 と一致する
- 9/9 朝の描画件数ログ: 02:51〜02:52 に pred モードで 0 と 157 が交互に3組、
  09:00:58 に result モードで 0、09:01:20 に result モードで 157。
  09:06:21 の予想到達以降は1件も無い（行数が変わらないため。異常ではない）
- 9/8 朝の画面（9:06:34 時点）: 7203・5242・7013 のいずれも
  「寄り前気配 ・サーバー保存ぶん」と「ベータ推定」の2行が並んだ。
  7203 は寄り前気配 −1.71% / ベータ推定 +0.06%（買い比率 22.4% / 観測60回）、
  5242 は −1.46% / +0.06%（26.7% / 60回）、
  7013 は −1.05% / +0.12%（33.7% / 72回）。実際の始値は全銘柄「待機中」
- 9/8 朝の premarket-prediction（Network・絞り込み sync）: 5本。
  0.3kB が4本（347ms / 339ms / 335ms / 344ms）、4.7kB が1本（357ms）。
  すべて 200。Initiator は App.js:6234。
  空振り4本のあと成功1本という形で、見積もりどおり
- 9/8 朝の「今朝の地合い」: 総合（想定ギャップ）+0.15%。
  日経225先物 +0.15% / SOX +3.38% / S&P500 −0.38% / NASDAQ −0.29% /
  ドル円 −1.51% / VIX +5.30% / NYダウ −0.51%。更新 9:06:34。
  予想対象日の表示は 2026-09-09
- 9/7 の寄り前収集: 84ティック / 全ティック158銘柄 / 件数不一致0 / エラー0件 /
  収集時間 1261秒（21分1秒）/ POST 158銘柄 3095KB 84レコード / 保存 84件 /
  予想生成 154件（見送り4件）
- 9/7 の途中経過POST（段3）: 8:57:07 に 48レコード / 1749KB。
  48は 8:45〜8:57 のティック数と一致。1レコード＝1ティック（158銘柄ぶん）で約36KB。
  84ティック換算で約3.06MB となり本番の3095KB と整合
- 9/7 の暫定キー: premarket:log:partial:2026-09-07 が Upstash に存在。
  Size 321KB / TTL 5h49m。ログの1749KB との差は gzip 圧縮によるもの（約5.4倍）
- 9/7 のフロント側観測: 8:45〜9:08 に発生した sync は sync?userId=... と
  sync?resource=scan-result&date=2026-09-07 の2本のみ。
  premarket-summary と premarket-prediction は0本。
  Console の赤2件は Vercel プレビューURLの SSO によるもの（CORS と 307）で無関係
- 寄り前収集（8/31・9/1・9/2）: いずれも84ティック / 全ティック158銘柄 /
  件数不一致0 / tick失敗0 / missing 0件 / appearedCount 158
- 予想生成: 8/31 は158件・見送り0件、9/1 は154件・見送り4件、
  9/2 は155件・見送り3件、9/3 は152件、9/4 は155件・見送り3件、
  9/7 は154件・見送り4件、9/9 は155件、9/10 は153件
- 的中率パネル（9/4 時点）: 方向一致率 63% / 平均誤差 1.06% / 件数 546 /
  平均確信度 31%。内訳は「ベータ推定 546件」の1枠のみ
- 9/4 の quote 予想の全体成績（155件）: 平均予想 −0.04% / 平均実測 +0.27% /
  平均誤差 +0.31% / 平均絶対誤差 0.87% / 方向一致率 72.2%（133件中96件）/ 相関 0.531
- 9/4 の地合い一律補正の効き目: 平均絶対誤差 0.87% → 0.82%、
  方向一致率 72.2% → 72.5%
- 9/4 の実測ギャップ 0.00: 155件中17件。
  6227・6758・7702・7715・7719・7722・7725・7729・7734・7743・7760・7769・
  7776・7779・8058・9434・368A。うち予想の絶対値が1.0%以上は5件
- 9/4 の予想の絶対値1.5%以上（13件）: 平均絶対誤差 1.57%。残り142件は 0.80%。
  13件のうち10件が77xx台。誤差の大きい順は 7776（+2.79）・7743（+2.47）・
  7713（+2.41）・7777（+2.41）・7722（+1.84）・7709（−1.75）・7711（+1.64）・
  7782（−1.30）・7774（+1.22）・9308（−0.90）・7762（+0.68）・7267（−0.53）・9101（+0.43）
- 9/4 の premarket-summary: count 158 / 全銘柄 quoteCount 84 /
  startedAt と finishedAt の差 1,245秒
- 9/4 の欠測: validCount 0 が3件（5187・5242・7781）。noOpen は1件（5242）
- 9/4 の validCount 62以上（12件）と実測ギャップ: 278A(72) +2.81 / 3697(72) +2.81 /
  3907(72) −3.30 / 4475(72) +4.70 / 6702(72) +2.74 / 7716(72) +3.12 /
  8031(72) −2.91 / 9984(72) +3.92 / 7747(80) −3.88 / 5801(64) +1.94 /
  6920(64) −2.08 / 5713(62) +2.95。12件すべてが絶対値1.94%以上
- 9/4 の validCount 61（5件）: 3110 +0.78 / 3436 −0.41 / 6361 −1.59 /
  6504 +2.34 / 9101 +2.06
- 9/4 の極端な買い比率: 7776（3.7 / −2.79 / 0.00）、7777（5.4 / −2.69 / −0.28）、
  7743（9.3 / −2.47 / 0.00）、7722（20.0 / −1.84 / 0.00）は過剰側。
  7709（1.2 / −2.94 / −4.69）は不足側
- noOpen: 8/25 は4件、8/31 は16件、9/1 は5件、9/2 は4件、9/4 は1件。9/3・9/7 は未確認
- noBuyRatio: 8/31 は0件、9/1 は4件、9/2 は3件、9/4 は3件。9/3・9/7 は未確認
- validCount の実測値: 8/31 は 50・60・61・62・64・66・69・72・77・82・84、
  9/1 は 21・60・61・62・64・66・72・75・79・80・84、
  9/2 は 11・33・34・49・60・61・62・65・71・72・73・76・84、
  9/3 は 60・61・62・65・71・72、9/4 は 0・1・33・60・61・62・64・72・80
- mode=calib（from 2026-08-31・to 2026-09-03・有効595件）:
  buyRatioLast は r 0.419 / rRank 0.466 / 傾き0.045 / 切片−0.405 / dirRate 0.517。
  buyRatioAvg は r 0.483 / rRank 0.433 / 傾き0.051 / 切片−0.255 / dirRate 0.557。
  buyRatioMin は dirRate 0.602 で5変種中最高。
  なお dirRate は「買い比率50超の行のうち実測がプラスだった割合」であり、
  予想の符号と実測の符号の一致率とは別の指標
- mode=calib の日別（avg変種）: 8/31 は142件・45.70・−0.798・r 0.622、
  9/1 は150件・46.34・−0.489・r 0.251、9/2 は151件・38.14・−1.331・r 0.445、
  9/3 は152件・50.46・+0.584・r 0.468
- 9/3（上げ相場）の群別: 全152件は平均予想 −0.02% / 平均実測 +0.58% /
  平均誤差 +0.60% / 平均絶対誤差 0.89% / 方向一致 0.671
- 段A の較正（上位120銘柄・457サンプル）: buyRatioLast の rRank 0.494〜0.515、
  buyRatioMin の dirRate 0.717〜0.833。採用係数は傾き0.058・切片−0.105
- 寄り予想の再取得タイマー（9/10 の調査で確定）: 間隔 PM_PRED_RETRY_MS 75秒、
  打ち切り PM_PRED_RETRY_UNTIL_MIN 9:12（時刻のみ・回数上限なし）。
  setInterval のため初回発火は張った75秒後
- npm run build（PR #74 後・9/9）: 終了コード0・Compiled successfully。
  CI=true でも警告0件
- npm run build（PR #73 後・9/8）: 終了コード0・警告なし
- npm ci / npm run build（PR #70 後・9/5）: いずれも終了コード0。
  ビルド警告0件。成果物は gzip 後 137.74 kB
- Upstash 8月分: Reads 808,406 / Writes 76,390。常時 0.15〜0.2 コマンド/秒
- parts.ranking: 6016ms（totalMs 6754 の89%）
- 気配ログのペイロード: 9/1・9/7 とも 158銘柄で 3095KB。Vercel 上限 4.5MB
- 環境変数の件数（PR #64 時点）: Vercel Functions 15件 / GitHub Actions 3件
