# 作業引き継ぎ（最終更新: 2026-08-30）

このファイルは「今どうなっているか」だけを書く。
恒久的な制約は CLAUDE.md（実装向け）と docs/OPERATIONS.md（運用向け）に書き、
ここには重複させない。更新は GitHub Web エディタで main 直コミット。

進行中の案件には必ず PR 番号を書く。番号が無いものは未投入。
「指示文作成済み」だけの記述は、投入済みかどうかが読めないため使わない。

## 現在の課題

- 最優先は気配収集の120件上限問題。立花APIが要求158件のうち先頭120件しか返しておらず、
  121番目以降の38銘柄は一度もデータが取れていない。お気に入り銘柄が全てこの38件側にあり、
  気配ベースの予想が1件も作られていない状態が続いていた（PR #55 の mode=coverage で判明）
- 上記により、Phase 2B 段B の「8/31 朝の動作確認」は位置づけが変わった。
  お気に入り銘柄の quote 予想は現状では作られないため、確認しても「作られない」ことしか分からない
- 寄り予想の生成と答え合わせがブラウザ側にあり、8:45〜9:00 に端末を開かないと記録が作られない。
  自動スキャンを導入した動機（決まった時刻に端末を開けない）と矛盾しているため、
  サーバー側移行を段Dより先に検討することにした
- PR #50 の3点のうち、ネガティブキャッシュと2分停止は 429 が発生しなかったため未発火。
  再連射が止まったことは確認できたが、停止機能そのものの作動は未確認のまま
- parts.ranking の調査は完了。原因はほぼ特定できたが、修正は見送りとした（下記参照）
- PR #50・#51・#52・#53・#54・#55（daytrade-simulator）と PR #18（tachibana-server）は
  いずれもマージ済み

## 進行中の案件

### 気配収集の120件上限（最優先・未投入（PR番号なし））

- 状態: 原因確定。修正方針は未決。未投入（PR番号なし）
- 触るファイル: tachibana-server/premarketLogger.js（および webapi.js の可能性）
- 確定した事実（8/28 の生ログ 2.4MB を直接解析して確認。推測ではない）:
  - 84ティック全てが例外なく120行ちょうど。ばらつきが1つも無い
  - 1ティック目の120行は、要求した158件の先頭から120番目までと順序も含めて完全一致
  - 集計に現れない38件は、要求リストの121番目以降と完全一致
  - 板の厚さや銘柄の性質は無関係。リストの何番目にいるかだけで決まっている
  - 同リポジトリのランキング取得は同じAPIを120件ずつに割って呼んでいる。
    寄り前ロガーだけが割らずに158件を丸投げしていた
- 検知できなかった理由: 収集側に要求件数と応答件数を突き合わせる処理が無い。
  38件が黙って欠けても tick失敗0件・エラー0件・「対象158件」のログのまま正常終了に見える
- 修正の選択肢:
  - 案1（根本解決）: 120件ずつのチャンク分割に変更。Railway 再デプロイと ティック所要・
    ペイロードサイズの再実測が必要。1ティックの所要時間が延びる点と、
    ペイロードが3096KB前後になり WARN 3500KB に近づく点の確認が要る
  - 案2（暫定）: PREMARKET_CODES の並び順を変え、お気に入り銘柄を先頭120件側へ移す。
    環境変数の変更のみで、当日から収集を開始できる。ただし158件全件の収集は諦める形になる
- 併せて必要: 要求件数と応答件数の不一致を検知する警告の追加。
  入れないと同じ欠落がまた静かに起きる
- 次の一手: 案1・案2 のどちらで進めるかを決める

### 寄り予想のサーバー側移行（調査完了・未投入（PR番号なし））

- 状態: Claude Code による調査完了。移す範囲は src="quote" のみで確定。未投入（PR番号なし）
- 触るファイル: tachibana-server/premarketLogger.js、daytrade-simulator/api/sync.js、
  daytrade-simulator/src/App.js
- 移行を検討する理由: 予想の生成も答え合わせもブラウザ側にあり、
  8:45〜9:00 と 9:00以降の2回、同じ端末でタブを開かないと記録が作られない。
  予想レコード（localStorage の pm_<ticker>）は端末間同期の対象外のため、
  iPad と Mac で別々に貯まる
- 調査で確定した事実:
  - 予想の材料はサーバー側に既に全て揃っている。買い比率の元（pAAV・pABV）、
    前日終値（pPRP）、当日始値（pDOP）は全て DEFAULT_COLS に含まれ毎ティック取得済み
  - pmPredictGapByQuote は純関数でブラウザ固有APIを使っていない。コピーで移植可能
  - 処理を足す場所は runSession() の postLog 直後（premarketLogger.js:374）。
    その場の records と date をローカル変数として使えるため追加の読み出し不要
  - Railway から Upstash への直接書き込み手段は無い（@upstash/redis 未導入）。
    設計方針として api/sync 経由に統一されており、既存の前例が3本ある
  - 買い比率の算出は Vercel 側の summarizePremarketDate（約90行）にのみ存在。
    移すならこの移植か、サーバーから premarket-summary を GET するかの二択
  - premarketLogger には祝日判定が無い（isWeekend のみ）。scanner.js は holidays.js を使用
- 答え合わせの始値について（8/28 の mode=coverage で確定）:
  - 9:06 時点で始値が取れなかったのは120件中4件のみ（3436・3907・5242・6723）
  - 9:06 の値で確定してよく、追撃取得も窓の延長も不要。
    予想と答え合わせを収集終了直後の1箇所で完結できる
- 移行前に決める必要がある事項:
  - 対象銘柄の決め方（PREMARKET_CODES のままか、user:<SCAN_SYNC_USER_ID> の favs を読むか。
    api/_scan.js:199 に前例あり）
  - 既存の pm_<ticker>（端末ローカル・最大180件）との関係。
    的中率集計がここを見ているため、切り替え方を決めないと過去の集計が断絶する
  - 保存キーと TTL、保存単位
  - 較正係数 PM_Q_SLOPE・PM_Q_INTERCEPT の置き場所（両側に持つと2箇所管理になる）
  - 新 resource の GET を無認証にするか
- 次の一手: 120件上限の修正方針が決まってから着手する。
  対象銘柄がどう変わるかが移行設計の前提になるため

### premarket-summary の mode=coverage 追加（PR #55・マージ済み）

- 状態: PR #55 マージ済み。api/sync.js のみ108行追加。
  ENV_CHANGELOG.md への記録は不要（環境変数の変更ではないため対象外）
- 触るファイル: なし
- 実施した内容:
  - date 指定の単一日について、収集対象（target）・集計に1行も現れなかった銘柄（missing）・
    始値が取れなかった銘柄（noOpen）・買い比率が取れなかった銘柄（noBuyRatio）を
    それぞれコード一覧で返す読み取り専用モードを追加
  - 収集対象は同一日の全セッションの codes の和集合。途中で対象が入れ替わっても取りこぼさない
  - 判定は summarizePremarketDate() の戻り値のみを見ており、計算を書き直していないため
    mode 未指定の明細と数字が食い違わない
  - 各一覧は昇順・最大200件、超過時 truncated: true。count は切る前の実件数
  - コードは大文字に正規化して突き合わせる（278a と 278A を別銘柄と数えないため）
- 確定した事実:
  - missing と noOpen・noBuyRatio は排他。missing は targetSeen から appeared を引いたもので、
    noOpen・noBuyRatio は「集計に現れた銘柄の中での欠測」。母集団が異なる
  - target の count = appearedCount + missing の count が成立する
- 次の一手: なし。完了

### Phase 2B 段B・段C（PR #54・マージ済み・動作確認未実施）

- 状態: PR #54 マージ済み。src/App.js のみの変更（110行追加・6行削除）。
  ENV_CHANGELOG.md への記録は不要。動作確認は未実施
- 触るファイル: なし（確認のみ）
- 実施した内容:
  - 気配ベースの予想を src="quote" として記録し、既存のベータ推定（src="beta"）と並走
  - 買い比率の取得元は既存の /api/sync?resource=premarket-summary&date=当日。
    サーバー側は無変更で、段A の較正に使った summarizePremarketDate を経由するため
    較正時の数字と本番の数字が食い違わない
  - 追加した関数は4つ。pmQuoteWindowOpen（8:45〜9:00判定）/
    pmFetchQuoteSummary（60秒キャッシュ・no-store・8秒タイムアウト・失敗時はnull）/
    pmPredictGapByQuote（予想と確信度の算出）/ pmBuildPredictions への組み込み
  - PM_HIST_MAX を 90 から 180 へ（段C）。pmSaveHist のコメントも定数参照に修正
- 確定した設計:
  - 予想ギャップ% ＝ 0.058 ×（買い比率 − 50）− 0.105。上限は ±3%
  - 予想の大きさは buyRatioLast、確信度の加点は buyRatioMin / buyRatioMax で判定
  - 確信度は基準45・片側偏りで+12・振れ幅で減点・25〜60%に丸め
  - その朝の有効観測回数が5回未満の銘柄は予想を作らない
  - 気配が取れなくてもベータ推定は必ず動く（例外を投げずnullを返す作り）
- 確定した事実:
  - SYNC_API は App コンポーネント内の変数のため、モジュール直下の関数から参照できない。
    PM_QSUM_API を新設して対応。本番URLの直書きのため、
    プレビュー環境で開いても気配データは本番から読む
  - PM_SRC_PRIORITY が ["quote","beta"] のため、quote が記録された日は
    答え合わせ表示が quote 側に切り替わる。ベータの予想値は画面から見えなくなるが、
    記録と答え合わせは裏で継続し、的中率パネルの出どころ別の行で両方比較できる
  - 8:45〜9:00 にアプリを開かなかった日は quote レコードが作られない
  - 予想レコードは localStorage の pm_<ticker> に保存され、端末間同期の対象外。
    答え合わせも同じ端末で 9:00 以降にタブを開かないと走らない
- 次の一手: 動作確認は 120件上限の修正後に行う。
  現状ではお気に入り銘柄の気配データが存在せず、確認しても quote 予想は作られない

### Phase 2B 段A・係数決めの実測分析（PR #53・完了）

- 状態: PR #53 マージ済み。8/29 に4つのURL全ての動作確認が完了。
  ENV_CHANGELOG.md への記録は不要
- 触るファイル: なし
- 実施した内容:
  - api/sync.js の premarket-summary に mode=calib を追加。
    from〜to（最大10日）を指定すると、買い比率5変種それぞれについて
    n / r / rRank / slope / intercept / dirRate を返す
  - 日別内訳と除外内訳（noGap / noRatio / totalRows）も返す
  - 既存の handlePremarketSummary() 本体は未変更。handler() に mode 分岐を1つ足す形
  - 買い比率とギャップの算出は既存の summarizePremarketDate() をそのまま使用
  - 1日ずつ読んで積算値だけ残し生ログは都度捨てる。読み取り専用
- 動作確認の結果:
  - date 指定の明細（既存挙動）: 正常
  - mode=calib で 8/25〜8/28: 正常
  - mode=calib で 8/18〜8/24: 8/29 の有料化後に取得成功
  - mode=calib で 8/18〜8/28（11日）: range too wide (max 10 days) を返した。仕様どおり
- 訂正: 段A の457サンプルは全て上位120銘柄から出たもの。
  「158銘柄体制の実測」という位置づけは「上位120銘柄の実測」に訂正する。
  データ自体は正しいため係数を捨てる必要は無い
- 次の一手: なし。段A は完了

### Upstash 月間リクエスト上限への対処（完了・コード変更なし）

- 状態: 完了。未投入（PR番号なし）。コード変更を伴わない運用対処のため PR は無い
- 触るファイル: なし
- 実施した内容:
  - 8/29 未明に無料枠50万コマンドへ到達し、Redis の読み書きが停止
  - Upstash 管理画面の Usage タブで内訳を確認し、案1（有料化）を採用
  - Vercel Dashboard → Integrations → Upstash → 該当DB → Settings →
    Update Configuration → Change Configuration から Pay As You Go へ移行
  - Max Budget を20ドルに設定（5ドルは設定不可。最低20ドルの制約あり）
  - 移行後、mode=calib のURL2本で復旧を確認。8/25〜8/28 は前回と完全一致
- 確定した事実:
  - 内訳は Reads 808,406 / Writes 76,390。読み取りが91%で主因
  - 相場が閉まっている土曜未明も 0.15〜0.2 コマンド/秒が平坦に継続。
    Hits/Misses の Misses はほぼ0で、リトライ地獄ではなく正常な読み取りが続いている
  - Upstash 側の Choose Plan は常時無効。Vercel 経由で作成したDBのため
    プラン変更の導線は Vercel 側にしかない
  - 移行でエンドポイント・トークンは変わらなかった。
    Vercel・Railway の環境変数は変更不要だった
  - Read Regions は追加しなかった。追加すると複製もコマンド数として課金されるため
  - Data Size 上限は 256MB → 10GB。現在6MB。ただし容量も従量課金の対象になった
- 次の一手: なし。完了。月に一度 Usage を見て金額を確認する

### Phase 2B 段D・画面表示

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: src/App.js
- 内容: 予想パネルに2手法を並べて出す。的中率パネルの bySrc の見せ方。
  段B で quotePred を各行に持たせてあるため、表示側で参照するだけで足りる。
  答え合わせ表示がベータ側を隠してしまう問題もここで解消する
- 次の一手: 120件上限の修正と、サーバー側移行の判断が先。
  移行する場合は表示側の読み出し元が変わるため、段D の設計も変わる

### tachibana-server README の全面改訂（PR #18・完了）

- 状態: PR #18 マージ済み。Railway 再デプロイは 8/28 夜（安全帯・金曜）に実施。
  ENV_CHANGELOG.md への記録は不要（ドキュメントのみの変更のため対象外）
- 触るファイル: なし
- 実施した内容:
  - 冒頭の位置づけを「1銘柄を中継する常駐サーバー」から
    「4つの役割を常時実行する Railway 上の常駐サーバー」へ変更
  - 「このサーバーの4つの役割」節を新設（watcher / webapi / scanner / premarketLogger）
  - アスキー図の中継先を api/tachibana-quote.js から api/sync.js へ修正
  - ファイル一覧を新設（実在17ファイルのみ）。verify-*.js 3件には
    「常駐処理からは呼ばれない検証用スクリプト」の注記を付与
  - 環境変数の3列表を新設（変数名 / 必須・任意 / 用途）。値は非記載
  - VPS 前提の記述を全削除
  - 「GitHubへの追加方法」節を削除
  - Vercel 側のファイル構成は api/sync.js への言及までに留める方針を採用
- PR #18 で確定した事実:
  - コードが実際に読む環境変数は21個。daytrade-simulator/docs/ENV_AUDIT.md の
    一覧と完全に一致（config.js 12 / auth.js 2 / webapi.js 1 / scanner.js 4 /
    premarketLogger.js 3、VERCEL_API_BASE は2ファイルで重複）
  - リポジトリ直下の実在ファイルは17個
  - index.js が起動するのは watcher / webapi / scanner / premarketLogger の4つのみ
- 次の一手: なし。完了

### parts.ranking の原因調査（調査完了・修正は見送り）

- 状態: 8/28 に Claude Code で調査実施（変更なし）。未投入（PR番号なし）。
  修正は当面着手しない
- 触るファイル: なし（修正に着手する場合は api/ranking.js と api/_scan.js が候補）
- 調査で確定した内容:
  - parts.ranking は buildUniverse の tSync〜tRanking 区間。内訳は
    /api/sector または /api/ranking への内部HTTP1〜2本と、その内側の
    /ranking-data（Railway）・/api/ipo 取得
  - 6016ms の主因は tachibana-server の getRankingData。全銘柄価格を
    120件ずつ・並列5で取得する処理で、コード上に「約4秒で完了する」と明記がある。
    キャッシュ保持は3分でスキャン間隔（最短40分）より短く、毎回ミスが確定する
  - 上記2点は 8/28 に webapi.js を目視確認済み。推測ではない
  - 次に重いのは withFallback の redis.set
- 補足（8/30）: ここでの「120件ずつ」という分割は、
  寄り前ロガーが分割していないこととの対比で意味を持つことが判明した
- 見送りの理由: 組み立て回はスキャンせず即返す仕様のため、6秒は体感に出ない
- 次の一手: 予算超過が実際に観測された時点で再評価する

### ENV_CHANGELOG.md の可読性改善（PR #52・完了）

- 状態: PR #52 マージ済み。ENV_CHANGELOG.md への記録行追記も完了
- 触るファイル: なし
- 実施した内容:
  - 表の外にこぼれていた5行（PR#42〜#45・PR #50）を表内へ移動し日付昇順に整列
  - 伏せ字だった「2026-08-2X」を確定日（8/22・8/23・8/23・8/24）に穴埋め
  - PR #50 行の破損修復
  - 内容列の書き方ルール4項目と記入例セクションを追加
- 次の一手: なし。完了

### /api/daily 連打対策（PR #50・完了・停止機能のみ作動未確認）

- 状態: PR #50 マージ済み・ブランチ削除済み・ENV_CHANGELOG.md 追記済み。
  8/27 夜に Mac の Chrome DevTools で効果確認を実施し、再連射の停止を確認
- 触るファイル: なし
- 次の一手: 場中（9:00〜15:30）に同じ手順で再確認できれば、429 検知時の
  一時停止（DAILY_PAUSED_UNTIL）の作動まで見られる。機会があれば見る程度でよい

### /api/daily の呼び出し量そのものの削減（PR2）

- 状態: 未着手。未投入（PR番号なし）。8/27 の実測を受けて優先度を引き下げた
- 触るファイル: src/App.js の fillDayNightFor と visibilitychange のリスナ登録部分
- 次の一手: 当面は観測のみ。表示銘柄数が増えて 429 が再発したときに着手する。
  案D（fillDayNightFor の直列ループに400ms程度の待機＋失敗銘柄への再試行抑止マーク）
  と案E（visibilitychange 経由の再開に最小間隔）。
  案F（pmFetchRecentDaily の cache no-store を force 時のみに限定）は効果の切り分けのため単独PR。
  案C（daily 専用キュー）は PM_FETCH_CONCURRENCY と二重制御になるため最終手段

優先度を下げた理由:

- 429 が0件で、転送量も最大14.1kB と軽い。今すぐ直さないと壊れる状態ではない
- ただし「表示銘柄数ぶんのリクエストが一度に飛ぶ」構造は変わっていない

## 次にやること

上から順に:

1. 120件上限の修正方針を決める。案1（チャンク分割・根本解決）か
   案2（並べ替えで暫定対応）か。案1なら Railway 再デプロイと実測が必要
2. 要求件数と応答件数の不一致を検知する警告の追加
3. 寄り予想のサーバー側移行。対象銘柄が確定してから着手
4. Phase 2B 段B の動作確認。気配データが取れるようになってから
5. PR2 の着手判断。429 が再発するか、表示銘柄数が大きく増えた時点で再評価
6. parts.ranking の修正着手判断。組み立ての予算超過が観測された時点で再評価
7. Phase 2B 段D（画面表示）。段B の実績が貯まってから

## 未決の判断

- 120件上限への対処を案1（チャンク分割）と案2（並べ替え）のどちらにするか。
  案1は158件全件を取れるが Railway 再デプロイとティック所要・ペイロードの再実測が要る。
  案2は環境変数の変更だけで当日から効くが、158件全件の収集は諦める形になる
- 158件という収集規模を維持するか。120件上限が判明したことで、
  そもそも何件を定点観測するのが適切かを問い直す余地が生まれた
- Redis の読み取り回数を削減するかどうか。常時ポーリングが1日1.5万コマンド
  （月45万相当）を占めることが判明したが、8/29 時点では削減しない判断とした。
  理由は3点。ポーリング間隔の延長は銘柄切り替えの反映が遅れる、
  夜間・土日停止は条件分岐が増えて「静かに止まる」故障の余地が増える、
  有料化後の見込み額が月2ドル程度で削減の労力に見合わない。
  Phase 2B で書き込みが増えて金額が跳ねた場合は再評価する
- quote 予想の確信度の上限60%をいつ引き上げるか。根拠が実質4営業日457サンプルのため
  控えめに置いた。実績が貯まって「確信度が高い予想ほど当たる」関係が確認できたら再評価する。
  なおベータ推定側の上限は95%のままなので、当面は両者の確信度を横並びで比較できない
- .env.example を作るかどうか。PR #18 の調査で、同ファイルが一度もコミットされたことが
  ないと判明した（git log --all が空）。.gitignore の .env.* により追跡できない状態のため、
  作るなら .gitignore に例外行が必要
- TACHIBANA_ENV が現在デモと本番のどちらで稼働しているか。コードからは判別できず、
  README にも書いていない。Railway の Variables 画面を見れば確認できる。
  docs/OPERATIONS.md への記載が候補
- Claude Code への「食い違えば実装せず報告のみで止める」指示の書き方。PR #18 で
  .env.example 不在という前提相違があったが、Claude Code 側が「停止して往復するより
  完成させる方が有益」と判断して続行した。
  「食い違いの内容にかかわらず止めること」への文言変更を検討。
  恒久ルールなので本来の記載先は CLAUDE.md か docs/OPERATIONS.md。
  なお PR #53・#54・#55 ではこの文言を指示文に入れており、前提確認は正しく報告された
- 暫定保持に残る項目（premarketLogger.js の9前提、修正時に巻き込む恐れのある箇所7件、
  組み立ての起動経路、PREMARKET_CODES 158件固定）の移設先。docs/OPERATIONS.md が候補だが未着手
- ENV_CHANGELOG.md の記録範囲は決着済み。CLAUDE.md など通常のドキュメント変更は
  記録しない。8/25 の PR#48、8/26 の CLAUDE.md 修正、8/28 の PR #18、8/29 の PR #53、
  8/30 の PR #54・#55 はこの扱い。
  ただし ENV_CHANGELOG.md 自身の書式・ルール変更だけは例外として1行記録する。
  8/28 の PR #52 がこの例外の初適用。
  恒久ルールなので、記載先を CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- ENV_CHANGELOG.md の書き方ルール本体（内容列4項目・記入例）は現在
  ENV_CHANGELOG.md 内に置いている。CLAUDE.md と docs/OPERATIONS.md の
  どちらへ移すかは未決
- daytrade-simulator 側の README を作るかどうか。現在は存在せず、GitHub のトップが
  ファイル一覧のみになる。作るなら10〜15行の入口に徹する方針。優先度は低い
- Mac mini で DevTools が使えることが判明した。開発端末は iPad という前提で書かれている
  docs/OPERATIONS.md の記述を更新するかどうか
- 予想レコードが端末ごとに分かれている現状（localStorage の pm_<ticker>・同期対象外）を、
  サーバー側移行の際に統合するのか、いったん切り捨てるのか

## 未確認の仮説

- 立花APIの120件という上限が、仕様上の固定値なのか、
  応答サイズやカラム数に依存する可変の値なのかは未確認。
  同リポジトリのランキング取得が120件ずつに割っている事実から
  経験的に120が採用されたと見られるが、根拠のコメントは無い
- 158件をチャンク分割した場合の1ティック所要時間。現在は120件で平均493ms。
  2分割すると単純計算で倍近くになるが、15秒間隔に対する余裕は残ると見られる。実測は未実施
- 158件全件が返った場合のペイロードサイズ。19.6KB/銘柄 × 158 ＝ 3096KB前後と推定。
  WARN 3500KB に対して余裕が404KBまで縮まる。実測は未実施
- 始値が取れなかった4件（3436・3907・5242・6723）に共通する事情。
  9:06 までに寄らない銘柄と見られるが未確認。
  なお 5242 は validCount の仮説でも例外として挙がっている銘柄
- Upstash の常時ぶん1日1.5万コマンドの発生源が Railway の watcher による
  「見る銘柄の問い合わせ」ポーリングであるという推測。0.2コマンド/秒は
  5秒に1回の GET と一致するが、コードでの確認は未実施
- 買い比率の日平均が「その日の地合い」の代理指標になり得る可能性。
  8/25〜8/28 の4営業日とも、買い比率の日平均が高い日ほど実測ギャップの日平均も高く、
  順序が崩れていない（48.9→-0.251 / 53.4→+0.056 / 58.5→+0.893 / 48.3→-0.384）。
  8/18〜8/24 でも同じ傾向が見られる（36.7→-1.327 / 54.4→+1.51 /
  37.7→-1.659 / 51.3→+0.433）。ただし後者は1日あたり9〜95行と
  サンプルが少なく、偶然でも説明がつくため仮説にとどめる
- 気配ベース予想がベータ推定より当たるかどうか。段A の相関は成立しているが、
  実際の予想としての優劣は quote の記録が貯まるまで判断できない
- PR #18 で README に載せた起動ログのサンプル4行が、実際のログ文言と
  一字一句一致しているかは未検証
- PR #18 後の README で、見出しの番号付けが混在している可能性がある。未確認
- 組み立ての時間予算 BUILD_BUDGET_MS 8000ms に対し、8/24 の totalMs は 6754ms で84%。
  立花側が普段より遅い日は fetchJsonWithin が先に切れる可能性がある。実際に切れた観測は無い
- 分岐C（/api/ranking へのフォールバック）の fetchJsonWithin には try/catch が無く、
  例外が buildUniverse の外へ出る。コード上の事実だが、発生した観測は無い
- 組み立て失敗時、scan:universe:built のマークは組み立ての前に立つため、
  そのスロットは再試行されない可能性がある。未検証
- 内側のタイムアウト（/api/ipo が8秒、/ranking-data が15秒）より外側の予算8秒が
  先に切れる逆転構造になっている。内側の値は実質的に効いていないとみられる
- tachibana-server の getRankingData には topix にある取得中Promise共有（inflight）が無い。
  同時に2本来ると全銘柄取得が二重に走る可能性がある。発生の観測は無い
- scan:universe:meta の source が「ranking(業種で取れなかったため代替)」の場合、
  /api/sector が例外だったのか200で0件だったのかは meta からは区別できない
- ENV_CHANGELOG.md の「### 記入例」の表は本表と同じ形式のため、将来スクリプトで
  縦棒区切り行を機械的に数えると 2026-01-01 の1行が混入する
- 8/18 の気配データが 3.2KB と小さいのは初日の部分取得と見られるが、未確認。
  8/19・8/20 が 27KB で一致しているため、12件時の定常値は 27KB
- api/premarket.js の cache 変数直上のコメントは内容は正確だが「Vercel側」の語がない。
  PR #49 では対応不要と判断
- validCount は寄り付き時刻の代理指標の可能性（0/57/69/81 の4値・12刻み）。
  validCount=81 と open=null が完全一致。例外は 5242
- iPad の Web エディタでの貼り付けは、複数行の Markdown で改行が失われることがある。
  8/26 に1回発生（CLAUDE.md）。再発するならドキュメント修正も Claude Code 経由に統一する

## 直近の実測値

### 気配収集のカバレッジ（8/28・mode=coverage および生ログ直接解析）

| 項目 | 実測 |
|---|---|
| 収集対象（target） | 158件 |
| 集計に現れた（appeared） | 120件 |
| 一度も現れない（missing） | 38件 |
| 始値が取れない（noOpen） | 4件（3436・3907・5242・6723） |
| 買い比率が取れない（noBuyRatio） | 3件（7707・7749・7769） |
| セッション数 | 1（codes を持つセッションも1） |

生ログ（2.4MB）を直接解析した結果:

- 84ティック全てが例外なく raw 120行ちょうど。ばらつきゼロ
- 1ティック目の120行は codes[0:120] と順序も含めて完全一致
- missing 38件は codes[120:158] と完全一致
- noOpen・noBuyRatio は appeared 120件の内数。missing とは排他

missing の38件:
4307 4543 4578 4684 5187 5201 5541 5901 6301 6302 6465 6473 6503 6702
7012 7013 7701 7702 7716 7722 7730 7741 7743 7744 7747 7775 7776 7779
7780 7781 7782 8001 8031 8053 8308 8411 9434 9962

- お気に入り銘柄は全てこの38件に含まれていた。
  気配ベースの予想は一度も作られていなかったことになる
- 8001・8411・9434・6301 など大型株を含む。板の薄さでは説明がつかない

### ペイロード線形性（訂正・8/30）

- 従来「実測 2352KB ÷ 158銘柄 ＝ 14.89KB/銘柄。101番目以降は板が薄い」と解釈していたが、
  これは誤りだった
- 正しくは 2352KB ÷ 120銘柄 ＝ 19.6KB/銘柄。
  8/24 の100銘柄時点の 19.58KB/銘柄とほぼ完全に一致する
- 線形性は最初から崩れていなかった。19.58KB/銘柄は引き続き有効な見積もり値
- 158件全件が返った場合の推定は 3096KB前後。
  Vercel 上限4.5MB に対して69%、WARN 3500KB に対して余裕は404KB

### Upstash 使用量の内訳（8/29・無料枠到達時点）

| 項目 | 実測 |
|---|---|
| 合計コマンド | 89万 / 無料枠50万（178%） |
| Reads | 817,306（91%） |
| Writes | 76,390（9%） |
| Bandwidth | 556MB |
| Storage | 6MB |

- 平日1日あたり約3万コマンド（Daily Commands グラフ・月〜木。金は約2.2万）
- 土曜未明の Throughput は 0.15〜0.2 コマンド/秒で平坦。相場が閉まっていても止まらない。
  24時間換算で1.3万〜1.7万コマンド/日、月45万相当
- 平日3万のうち約半分が上記の常時ぶん、残り約1.5万が相場中の実際の仕事
- Hits/Misses の Misses はほぼ0。リトライによる水増しではない
- 「立花のリアルタイム中継の書き込みが主因」という従来の推測は否定された

### Phase 2B 段A・買い比率とギャップの関係（8/25〜8/28・4営業日・有効457行）

| 変種 | n | r | rRank | slope | intercept | dirRate |
|---|---|---|---|---|---|---|
| first | 457 | 0.166 | 0.339 | 0.039 | -0.008 | 0.608 |
| last | 457 | 0.211 | 0.494 | 0.058 | -0.105 | 0.643 |
| min | 457 | 0.196 | 0.420 | 0.050 | 0.335 | 0.717 |
| max | 457 | 0.206 | 0.433 | 0.053 | -0.443 | 0.568 |
| avg | 457 | 0.200 | 0.427 | 0.051 | -0.046 | 0.626 |

日別内訳:

| 日付 | n | 買い比率avgの平均 | 実測ギャップの平均 | その日の相関 |
|---|---|---|---|---|
| 8/25 | 119 | 48.924 | -0.251% | 0.458 |
| 8/26 | 116 | 53.435 | +0.056% | 0.313 |
| 8/27 | 109 | 58.500 | +0.893% | 0.411 |
| 8/28 | 113 | 48.335 | -0.384% | 0.159 |

除外内訳: noGap 16 / noRatio 8 / totalRows 480

- 段B 着手の水準（rRank 0.3以上）を満たした。最良は buyRatioLast の rRank 0.494
- buyRatioMin は dirRate 0.717 と方向の的中率が突出
- slope は現実的な水準。last の 0.058 なら買い比率75%で予想ギャップ +1.35% 前後
- 8/28 だけ相関 0.159 と低い。4日のうち1日は当たらない日がある
- この期間の値を段B の係数として採用した（PR #54 で実装済み）
- 訂正（8/30）: このサンプルは上位120銘柄のみから成る。
  母集団は「158銘柄」ではなく「PREMARKET_CODES の先頭120件」

### Phase 2B 段A・8/18〜8/24（5営業日・有効154行・参考値）

| 変種 | n | r | rRank | slope | intercept | dirRate |
|---|---|---|---|---|---|---|
| first | 154 | 0.451 | 0.318 | 0.045 | 0.046 | 0.741 |
| last | 154 | 0.564 | 0.515 | 0.065 | -0.178 | 0.688 |
| min | 154 | 0.482 | 0.398 | 0.055 | 0.414 | 0.833 |
| max | 154 | 0.578 | 0.509 | 0.060 | -0.496 | 0.628 |
| avg | 154 | 0.563 | 0.494 | 0.063 | 0.036 | 0.735 |

日別内訳:

| 日付 | n | 買い比率avgの平均 | 実測ギャップの平均 | その日の相関 |
|---|---|---|---|---|
| 8/18 | 1 | 42.100 | -0.23% | 算出不可 |
| 8/19 | 10 | 36.730 | -1.327% | 0.533 |
| 8/20 | 9 | 54.444 | +1.51% | -0.043 |
| 8/21 | 39 | 37.733 | -1.659% | 0.515 |
| 8/24 | 95 | 51.265 | +0.433% | 0.436 |

除外内訳: noGap 7 / noRatio 5 / totalRows 165

- 8/29 の Upstash 有料化後に取得
- r が 0.451〜0.578 と 8/25〜8/28 の2〜3倍高いが、これは信頼できる数字ではない。
  154行のうち95行が 8/24 に集中し、他の日は1〜39行しかない
- rRank で見れば last 0.515 / avg 0.494 で 8/25〜8/28 とほぼ同水準。
  順位ベースでは両期間が一致しており、こちらが信頼できる読み方
- 最良の変種が last である点、dirRate 最良が min である点も両期間で一致
- 係数の作り直しには使わない。段A の結論を別期間が緩やかに支持した、という位置づけ
- なお この期間は収集件数が12〜100件で120件を下回っていたため、
  120件上限の影響を受けていない

### /api/daily 連打対策の効果確認（8/27 夜・Mac の Chrome DevTools）

| 項目 | 実測 | 判定 |
|---|---|---|
| daily リクエスト件数 | 157件（表示銘柄 157件と一致） | 通過（重複取得なし） |
| ステータス | 200 と 304 のみ。429 は0件 | 通過 |
| 裏表の往復による追加リクエスト | 0件 | 通過（再連射なし） |
| 転送量 | 2.0KB / 12.1KB / 14.1KB（3回分） | 通過 |

- 429 が発生しなかったため、ネガティブキャッシュと2分停止は未発火。作動は未確認
- 20:24 の回は大半が disk cache（1ms）、20:32 と 21:25 の回は大半が 304（0.1KB）
- 確認したURLはプレビュー用ドメイン。コンソールに出る CORS ブロックと ERR_FAILED は
  プレビュー認証の副作用で、アプリとは無関係
- apple-mobile-web-app-capable の非推奨警告が1件。表示・動作への影響なし

### PREMARKET_MAX=158（8/25朝・訂正あり）

| 項目 | 実測 | 判定 |
|---|---|---|
| tick失敗 | 0件 | 通過 |
| エラー | 0件 | 通過 |
| POST サイズ | 2352KB | 通過（WARN 3500KB まで 1148KB の余裕） |
| レコード件数 | 84件 | 通過 |
| tick 所要 | 439〜645ms / 平均493ms | 通過 |
| 起動ログ 対象 | 158件 / 158件 / 158件 | 通過 |

訂正（8/30）:

- 「全項目通過」は誤り。実際には要求158件に対し120件しか返っておらず、
  38銘柄が一度も収集されていなかった
- tick失敗0件・エラー0件だったのは、収集側に要求件数と応答件数を突き合わせる処理が無く、
  欠落を検知できない構造だったため
- POST サイズ 2352KB・tick 所要 平均493ms は、いずれも120銘柄での値
- 収集時間 1260秒＝21分ちょうど。84ティック × 15秒と完全一致。窓の取りこぼしなし
- 8:35 JST の日次再ログイン成功。メンテ明けの関門を通過
- 8:50 スキャン 203件 / 4分9秒 / 失敗0件
- 4:29・4:39 JST の watcher 読み込み失敗2件はメンテ帯（3:00〜8:30）内で想定内

### 気配データの日次サイズ（Upstash premarket:log:<日付>、gzip 後）

| 日付 | Size | 銘柄数 | 1銘柄あたり |
|---|---|---|---|
| 8/18 | 3.2KB | 12 | — |
| 8/19 | 27KB | 12 | — |
| 8/20 | 26.7KB | 12 | — |
| 8/21 | 91.5KB | 40 | 2.29KB |
| 8/24 | 201KB | 100 | 2.01KB |

## 暫定保持（移設先が決まり次第、順次ここから削除する）

premarketLogger.js の壊してはいけない前提:

1. warn() ヘルパーの内部は console.log。console.warn に戻さない（Railway で [err] 分類される）
2. running の解除は .finally() のみ
3. lastSessionDate ガードは維持
4. errorCount と tickErrorCount を混ぜない
5. PREMARKET_CODES のパースは /^[0-9A-Z]{4}$/
6. PREMARKET_MAX は起動時に一度だけ確定。変更には再起動が必須
7. 1ティック＝立花への POST 1回。ボトルネックは Vercel へのペイロードサイズ
8. 検証窓は平日 8:45〜9:06 の21分のみ。失敗の検知は翌営業日。
   実体は START_MINUTE（8:45）/ END_MINUTE（9:06）
9. POSTサイズ閾値は POST_SIZE_WARN_KB=3500 / POST_SIZE_ERROR_KB=4000 / POST_SIZE_LIMIT_KB=4500。
   error() は console.error のまま維持する
10. 立花への1回の要求で返るのは先頭120件まで。121件目以降は黙って捨てられる。
    要求件数と応答件数を突き合わせる処理は現在存在しない

tick失敗 と エラー は別カウンタ。必ず tick失敗 を先に見る。

修正時に巻き込む恐れのある箇所:

- api/sync.js — lastSectors 未送信時に既存値を維持
- api/_scan.js — 組み立てはスロット先頭の1回だけ。マークが先に立つ
- api/_scan.js の saveUniverse — source が "ranking" 以外だと保存拒否
- src/lib/analyze.js — App.js と api/_scan.js の共有。変更すると自動スキャンの保存スコアも変わる
- src/App.js の PUSH_SYNC — 触るとお気に入りが巻き戻る
- src/App.js の applySyncedData — SyncPanel の ID 切り替え時に last_sectors を上書き
- api/sector.js の sectorCache と分岐 — 触ると AI 呼び出しが復活

組み立ての起動経路:

- 唯一の入口: POST /api/sync?resource=scan-run → runScanBatch → buildUniverse
- フロントの手動スキャンでは走らない（buildStockUniverse はブラウザ内組み立て）
- offset:0 かつ scan:universe:built のマークが無いときだけ組み立て
- 組み立て回はスキャンせず done:0 / nextOffset:0 で即返す

固定事項:

- PREMARKET_CODES は158件固定の定点観測（scan:universe と非連動）。
  PREMARKET_MAX=158 だが、立花の120件上限により実際に収集できているのは先頭120件のみ
- 予想レコード pm_<ticker> は localStorage 保存で端末間同期の対象外。
  最大180件（PM_HIST_MAX）。exportAllData でのみ持ち出せる
- Upstash は Pay As You Go（$0.2 / 100Kコマンド）。Max Budget 20ドル。
  上限到達時はデータベースが停止する。70%・90%でメール通知
