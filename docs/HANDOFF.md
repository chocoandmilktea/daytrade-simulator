暫定保持：0件（docs/HOLD.md）／最終棚卸し 2026-09-24

# 作業引き継ぎ（最終更新: 2026-09-24）

このファイルは「今どうなっているか」だけを書く。
恒久的な制約は CLAUDE.md（実装向け）と docs/OPERATIONS.md（運用向け）に書き、
ここには重複させない。更新は GitHub Web エディタで main 直コミット。

進行中の案件には必ず PR 番号を書く。番号が無いものは未投入。
「指示文作成済み」だけの記述は、投入済みかどうかが読めないため使わない。

寄り予想（寄り前気配・方向判定・集計帯・観測ログ・今朝の地合いを含む）の
9/16 までの全記録は docs/archive/HANDOFF-2026-09-21.md に退避した。
再開するときはそちらを読む。普段は読まない。

## 現在の課題

- 寄り予想の開発を凍結した（9/21）。PR #80 の検証で、寄り前に偏りが大きかった
  銘柄を「始値で建て、終値で手じまい」した場合の損益は、15営業日・1096回で
  コスト0.2%差引後 −0.42%、勝率40.3%。買い・売りとも両方マイナスで、
  15日中12日がマイナス。寄り付きのギャップ（前日終値との差）は当たっても
  寄った時点で値段に織り込み済みで、その後はむしろ戻る。
  逆に売買してもコスト差引後はほぼ0%で、利益は残らない
- 凍結に伴い、寄り予想の機能と寄り前収集を 9/22 に全削除した（PR #82・#83・#26）。
  アプリの寄り予想タブ、Vercel の premarket 系 API、Railway の寄り前収集が
  すべて無くなり、Upstash への毎朝の書き込みは止まった
- 次に注力する対象はまだ決まっていない
- 環境変数の未設定が黙って進む経路が2種類ある（PR #64 の調査で判明）。
  UPSTASH_REDIS_REST_URL / _TOKEN は依存が 1.38.2 になった結果、未設定でも起動は続き
  最初の Redis 呼び出しで初めて失敗する。PUSHOVER_TOKEN / PUSHOVER_USER は
  未設定チェックが無く、API は 200 を返すが通知だけが届かない。
  どちらも「設定漏れにすぐ気付ける」という前提が成立しない
- PR #50 の3点のうち、ネガティブキャッシュと2分停止は 429 が発生しなかったため未発火。
  再連射が止まったことは確認できたが、停止機能そのものの作動は未確認のまま
- 端末内保存（localStorage）の失敗を握りつぶしている箇所が多い（9/16 の調査で8箇所を確認）。
  失敗しても画面にもログにも残らない
- PR #83 までの daytrade-simulator の PR と、PR #26 までの tachibana-server の PR は
  すべてマージ済み。未マージの PR は現在無い

## 進行中の案件

### 寄り予想の凍結と削除（PR #80・#82・#83・#26・すべてマージ済み・完了）

- 状態: PR #80（凍結判断）マージ済み。PR #82（アプリ側の全削除）・
  PR #83（📋コピーボタンの削除）・PR #26（tachibana-server 側の削除）も
  9/22 にすべてマージ済み
- 触るファイル: なし
- 検証の中身: public/backtest.html（削除済み）で、Redis の予測記録（30日保持）と
  /api/daily の終値を突き合わせて集計した。偏り大は買い比率が50から10以上離れた銘柄。
  始値は予測記録の open を使用し、Yahoo の始値と1%以上ずれた行は除外（該当0件）。
  未寄り62件は除外
- 予測記録の保存: 8/31〜9/18 の15営業日ぶんを
  premarket-archive-2026-09-21.json として iPad の「ファイル」に保存済み
- 削除の範囲（PR #82）: src/App.js の寄り予想ブロックとタブ登録、寄り前気配の記録（pq_*）、
  寄り予想専用の観測ログ、api/premarket.js、api/sync.js の premarket 系 resource 3種、
  public/backtest.html
- 削除の範囲（PR #83）: 一覧上部バーの📋コピーボタン（寄り前収集の銘柄設定用だった）
- 削除の範囲（PR #26）: premarketLogger.js、index.js の起動2行、
  webapi.js の /market-price と DEFAULT_COLS
- 環境変数: Vercel の TACHIBANA_MARKET_PRICE_API を削除済み（PM_Q_SLOPE /
  PM_Q_INTERCEPT は元々未設定）。Railway の PREMARKET_CODES / PREMARKET_MAX を
  削除済み（PREMARKET_CHUNK_SIZE は元々未設定）
- 残存物: Upstash の premarket:log:* と premarket:pred:* は TTL 最長30日のため
  10月下旬に自然消滅する。端末の pm_* / dirstats_hist / obslog_net も残置（害なし）
- 次の一手: 朝のログ確認（「次にやること」の2）と、ENV_CHANGELOG.md への実結果の追記
- 再開する場合: docs/archive/HANDOFF-2026-09-21.md と上記 JSON、
  および PR #82 / #26 の差分を読む

### /api/daily 連打対策（PR #50・完了・停止機能のみ作動未確認）

- 状態: PR #50 マージ済み・ブランチ削除済み・ENV_CHANGELOG.md 追記済み
- 触るファイル: なし
- 次の一手: 場中（9:00〜15:30）に同じ手順で再確認できれば、
  429 検知時の一時停止の作動まで見られる。機会があれば見る程度でよい

### /api/daily の呼び出し量そのものの削減（PR2）

- 状態: 未着手。未投入（PR番号なし）。8/27 の実測を受けて優先度を引き下げた
- 触るファイル: src/App.js の fillDayNightFor と visibilitychange のリスナ登録部分
- 優先度を下げた理由: 429 が0件で転送量も軽い。今すぐ直さないと壊れる状態ではない
- 次の一手: 当面は観測のみ。表示銘柄数が増えて 429 が再発したときに着手する

### parts.ranking の原因調査（調査完了・修正は見送り）

- 状態: 8/28 に Claude Code で調査実施（変更なし）。未投入（PR番号なし）
- 触るファイル: なし
- 調査で確定した内容: 6016ms の主因は tachibana-server の getRankingData
- 見送りの理由: 組み立て回はスキャンせず即返す仕様のため、6秒は体感に出ない
- 次の一手: 予算超過が実際に観測された時点で再評価する

## 次にやること

1. 次に注力する対象を決める
2. 寄り前収集の停止確認。Railway のログに premarket の行が出ていないこと（未確認）と、
   Vercel のログに 400 userId required が出ていないこと。
   9/24 朝は Vercel 側を取り逃した（実行ログの保持が短く、後から遡れない）。
   Railway 側は保持が長いため後追いでも確認できる
3. ENV_CHANGELOG.md へ、環境変数削除の実結果を1件追記する。
   次に daytrade-simulator を触るときでよい
4. 月に一度 Upstash の Usage を見て金額を確認する（Max Budget 20ドル）。
   寄り前収集の停止で減っているはずなので、10月分で変化を見る。
   docs/OPERATIONS.md へ移すまではここに置く
5. docs/OPERATIONS.md の更新。iPad 前提で書かれているが、
   Mac mini と DevTools が使える PC が加わったため追記が要る。
   併せて、上の Upstash の月次確認と、docs/archive の扱い
   （凍結した案件の記録置き場で普段は読まない）を書き加える

### 待ち・条件付き

6. PR2 の着手判断。429 が再発するか、表示銘柄数が大きく増えた時点で再評価
7. parts.ranking の修正着手判断。予算超過が観測された時点で再評価

## 未決の判断

- Redis の読み取り回数を削減するかどうか。常時ポーリングが1日1.5万コマンド
- TACHIBANA_ENV が現在デモと本番のどちらで稼働しているか
- CLAUDE.md の「TTL は用途ごとに5種類」という文言。PR #82 で premarket 系の3キーが
  無くなったため、件数そのものが実態と合っていない可能性がある。
  次に CLAUDE.md を触るときに実際のキー一覧と突き合わせる
- .env.example を作るかどうか。PR #64 で CLAUDE.md に一覧表ができたため必要性は下がった
- 端末内保存の失敗の握りつぶしを直すかどうか。少なくとも観測ログへ理由を残す方向。
  なお PR #82 で観測ログのクリアボタン（寄り予想タブ内にあった）が消えたため、
  手動で消す導線は現在無い
- Claude Code への「食い違えば実装せず報告のみで止める」指示の書き方。
  これまでの実装前確認はすべて正しく報告されており、恒久ルールとして
  CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- 調査タスクの指示文で、症状の説明と調査項目の対応をどこまで書くか。
  調査項目を列挙するだけでなく「この症状を説明できる筋道を全て挙げよ」と
  書き添えるほうが漏れにくい可能性がある
- 調査タスクを実装の前に必ず1本挟むという進め方を、恒久ルールとして書き残すかどうか
- Claude Code が完了報告に添える「CI・レビューコメントを監視して自動対応する」という
  申し出をどう扱うか。マージ判断は人間が行う方針のため受け入れていない
- ENV_CHANGELOG.md の記録範囲は決着済み。通常のドキュメント変更は記録しない。
  ただし ENV_CHANGELOG.md 自身の書式・ルール変更だけは例外として1行記録する
- ENV_CHANGELOG.md の書き方ルール本体の移設先。CLAUDE.md と OPERATIONS.md のどちらか
- daytrade-simulator 側の README を作るかどうか。優先度は低い

## 未確認の仮説

- Upstash の常時ぶん1日1.5万コマンドの発生源が Railway の watcher による
  ポーリングであるという推測
- Claude Code のセッションが認識する日付が JST と1日ずれる場合がある
- UPSTASH_REDIS_REST_URL / _TOKEN が未設定のときの挙動が変わった理由
- KV_REST_API_URL / KV_REST_API_TOKEN が Vercel 側に実際に注入されているかは未確認
- Vercel が今後 npm ci を選ぶかどうか
- typescript 7.0.2 が package-lock.json に入った時期

## 直近の実測値

- 寄り→引け検証（9/21・PR #80・8/31〜9/18 の15営業日）:
  全体 −0.42%（コスト0.2%差引後）／ 1096回 ／ 勝率40.3%。
  買い −0.27%（42.6%・577回）／ 売り −0.58%（37.8%・519回）。
  最良日 9/8（+0.17%）を除くと −0.46%（1023回）。
  除外は未寄り62件・終値取得失敗0件・始値不整合0件
- 寄り→引け検証の日別: 8/31 −0.79 / 9/1 −0.25 / 9/2 +0.00 / 9/3 −0.64 /
  9/4 −0.47 / 9/7 −0.14 / 9/8 +0.17 / 9/9 −0.59 / 9/10 −0.85 / 9/11 −0.17 /
  9/14 −0.31 / 9/15 −1.10 / 9/16 −0.15 / 9/17 −0.98 / 9/18 +0.15（単位は%）
- npm run build（PR #82 後・9/22）: CI=true で警告0件・成功。PR #83 後も同じ
- npm run build（PR #77 後・9/15）: 終了コード0・警告0件。CI=true でも同じ
- npm ci / npm run build（PR #70 後・9/5）: いずれも終了コード0。
  ビルド警告0件。成果物は gzip 後 137.74 kB
- Upstash 8月分: Reads 808,406 / Writes 76,390。常時 0.15〜0.2 コマンド/秒
- parts.ranking: 6016ms（totalMs 6754 の89%）
- 気配ログのペイロード: 9/1・9/7 とも 158銘柄で 3095KB。Vercel 上限 4.5MB
- 環境変数の件数（PR #64 時点）: Vercel Functions 15件 / GitHub Actions 3件
