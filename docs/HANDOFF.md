# 作業引き継ぎ（最終更新: 2026-08-29）

このファイルは「今どうなっているか」だけを書く。
恒久的な制約は CLAUDE.md（実装向け）と docs/OPERATIONS.md（運用向け）に書き、
ここには重複させない。更新は GitHub Web エディタで main 直コミット。

進行中の案件には必ず PR 番号を書く。番号が無いものは未投入。
「指示文作成済み」だけの記述は、投入済みかどうかが読めないため使わない。

## 現在の課題

- Upstash の月間リクエスト上限（50万）に到達した。8/29 未明に
  premarket-summary の mode=calib を叩いた際、
  ERR max requests limit exceeded / Limit: 500000, Usage: 500000 が返った。
  実装の不具合ではなくプラン上限。Redis への読み書きが全て止まるため、
  スキャン結果保存・寄り前気配収集・端末間同期・立花中継の全てが影響を受ける。
  土日は稼働しないため実害は出ていないが、月曜 8:45 の気配収集までに手当てが必要
- 上記以外に急ぎの課題はない。PR #50・#51・#52・#53（daytrade-simulator）と
  PR #18（tachibana-server）はいずれもマージ済み
- PR #50 の3点のうち、ネガティブキャッシュと2分停止は 429 が発生しなかったため未発火。
  再連射が止まったことは確認できたが、停止機能そのものの作動は未確認のまま
- parts.ranking の調査は完了。原因はほぼ特定できたが、修正は見送りとした（下記参照）

## 進行中の案件

### Upstash 月間リクエスト上限への対処

- 状態: 未着手。未投入（PR番号なし）。8/29 未明に到達を確認
- 触るファイル: 未定（案3を採る場合は api/sync.js の tachibana-quote 周辺が候補）
- 到達の経緯: 段A の集計URL（3つ目・8/18〜8/24 の範囲）を叩いた時点で上限エラー。
  2つ目（8/25〜8/28）は成功しているため、その間に枠を使い切った形
- 対処の候補:
  - 案1: 有料プランへ移行。最も確実で月曜に間に合う
  - 案2: 枠の月次回復を待つ。ただしリセット日が契約日基準か月初かが未確認で、
    外すと月曜に間に合わない
  - 案3: 書き込み回数そのものを削減。中継間隔の延長、スナップショット書き込みの間引き。
    根本対処だが実装が必要で月曜には間に合わない
- 次の一手: Upstash 管理画面で2点を確認する。
  1) コマンド種別ごとの使用量内訳（主因の確定）
  2) 枠のリセット日
  この2点が分かるまで案1と案2のどちらが妥当かは決められない。
  案3は主因が確定してから設計する

### Phase 2B 段A・係数決めの実測分析（PR #53・完了）

- 状態: PR #53 マージ済み。8/29 未明に4つのURLで動作確認まで完了。
  ENV_CHANGELOG.md への記録は不要（環境変数の変更ではないため対象外）
- 触るファイル: なし
- 実施した内容:
  - api/sync.js の premarket-summary に mode=calib を追加。
    from〜to（最大10日）を指定すると、買い比率5変種それぞれについて
    n / r / rRank / slope / intercept / dirRate を返す
  - 日別内訳（日付・対象行数・買い比率avgの平均・gapPctの平均・その日の相関）と
    除外内訳（noGap / noRatio / totalRows）も返す
  - 既存の handlePremarketSummary() 本体は未変更。handler() に mode 分岐を1つ足し、
    mode=calib のときだけ新ハンドラ handlePremarketCalib() へ回す形
  - 買い比率とギャップの算出は既存の summarizePremarketDate() をそのまま使用。
    再実装していないため現行の明細と数字が食い違わない
  - 1日ずつ読んで積算値だけ残し生ログは都度捨てる。読み取り専用（set・expire を呼ばない）
- 動作確認の結果:
  - date 指定の明細（既存挙動）: 正常
  - mode=calib で 8/25〜8/28: 正常に集計を返した（結果は「直近の実測値」参照）
  - mode=calib で 8/18〜8/24: Upstash 上限エラーで取得できず。未取得のまま
  - mode=calib で 8/18〜8/28（11日）: range too wide (max 10 days) を返した。仕様どおり
- 次の一手: なし。段A は完了。ただし 8/18〜8/24 の範囲は未取得のため、
  Upstash が復旧したら参考値として取り直す

### Phase 2B 段B・気配ベース予想の実装

- 状態: 未着手。未投入（PR番号なし）。段A の結果が段B 着手の水準（rRank 0.3以上）を
  満たしたため、着手可能な状態にある
- 触るファイル: src/App.js（pmPredictGap の隣に気配ベースの予想関数を新設）
- 設計の方針（段A の結果から）:
  - 予想の大きさは buyRatioLast で計算する（rRank 0.494 で最良）
  - 確信度は buyRatioMin で調整する（dirRate 0.717 で方向の的中率が突出）
  - src="quote" として pm_<ticker> に記録し、既存のベータ推定と並走させる。
    器（PM_SRC_LABELS の quote、PM_SRC_PRIORITY の ["quote","beta"]、
    的中率パネルの bySrc 集計）は既に実装済み
  - 確信度の初期値は控えめに置く。4営業日・457サンプルしかないため
- 次の一手: Upstash の件が片付いてから設計に入る。
  段B は書き込みを増やす変更ではないが、動作確認に Redis 読み取りが必要

### Phase 2B 段C・PM_HIST_MAX の引き上げ

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: src/App.js（PM_HIST_MAX の定数のみ）
- 内容: src が beta と quote の2種になると1日2件消費するため、
  90件のままでは実質45営業日に半減する。180 にすれば従来どおり90営業日ぶん
- 次の一手: 段B と同じPRに相乗りさせる

### Phase 2B 段D・画面表示

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: src/App.js
- 内容: 予想パネルに2手法を並べて出す。的中率パネルの bySrc の見せ方
- 次の一手: 段B の実績が貯まってから着手する

### tachibana-server README の全面改訂（PR #18・完了）

- 状態: PR #18 マージ済み。Railway 再デプロイは 8/28 夜（安全帯・金曜）に実施。
  ENV_CHANGELOG.md への記録は不要（ドキュメントのみの変更のため対象外）
- 触るファイル: なし
- 実施した内容:
  - 冒頭の位置づけを「1銘柄を中継する常駐サーバー」から
    「4つの役割を常時実行する Railway 上の常駐サーバー」へ変更
  - 「このサーバーの4つの役割」節を新設（watcher / webapi / scanner / premarketLogger）
  - アスキー図の中継先を api/tachibana-quote.js から api/sync.js へ修正。
    4モジュールそれぞれの流れを1本ずつ図示
  - ファイル一覧を新設（実在17ファイルのみ）。verify-*.js 3件には
    「常駐処理からは呼ばれない検証用スクリプト」の注記を付与
  - 環境変数の3列表を新設（変数名 / 必須・任意 / 用途）。値は非記載。
    詳細は daytrade-simulator の docs/ENV_AUDIT.md へ誘導
  - VPS 前提の記述を全削除（2章のセットアップ手順、方法B の pm2、
    セキュリティ注意の「このVPS以外に」）
  - 「GitHubへの追加方法」節を削除（運用開始済みで役目を終えていたため）
  - Vercel 側のファイル構成は api/sync.js への言及までに留める方針を採用。
    あちらの変更に追従できず再び古くなるのを防ぐため
- PR #18 で確定した事実:
  - コードが実際に読む環境変数は21個。daytrade-simulator/docs/ENV_AUDIT.md の
    一覧と完全に一致（config.js 12 / auth.js 2 / webapi.js 1 / scanner.js 4 /
    premarketLogger.js 3、VERCEL_API_BASE は2ファイルで重複）
  - リポジトリ直下の実在ファイルは17個。旧 README に無かったのは
    holidays.js / premarketLogger.js / scanner.js / webapi.js / verify-*.js 3件
  - index.js が起動するのは watcher / webapi / scanner / premarketLogger の4つのみ
- 次の一手: なし。完了

### parts.ranking の原因調査（調査完了・修正は見送り）

- 状態: 8/28 に Claude Code で調査実施（変更なし）。未投入（PR番号なし）。
  修正は当面着手しない
- 触るファイル: なし（修正に着手する場合は api/ranking.js と api/_scan.js が候補）
- 調査で確定した内容:
  - parts.ranking は buildUniverse の tSync〜tRanking 区間。内訳は
    /api/sector または /api/ranking への内部HTTP1〜2本と、その内側の
    /ranking-data（Railway）・/api/ipo 取得
  - 6016ms の主因は tachibana-server の getRankingData。全銘柄価格を
    120件ずつ・並列5で取得する処理で、コード上に「約4秒で完了する」と明記がある。
    キャッシュ保持は3分でスキャン間隔（最短40分）より短く、毎回ミスが確定する
  - 上記2点は 8/28 に webapi.js を目視確認済み。推測ではない
  - 次に重いのは withFallback の redis.set。全銘柄行データと全銘柄名マップを
    await して保存するため、その時間がまるごと所要msに乗る
- 見送りの理由: 組み立て回はスキャンせず即返す仕様のため、6秒は体感に出ない。
  totalMs 6754ms はタイムアウトではなく正常完了
- 次の一手: 予算超過（下記の未確認の仮説）が実際に観測された時点で再評価する。
  その場合は計測より先に、失敗時の検知と再試行の設計から入る

### ENV_CHANGELOG.md の可読性改善（PR #52・完了）

- 状態: PR #52 マージ済み。ENV_CHANGELOG.md への記録行追記も完了
- 触るファイル: なし
- 実施した内容:
  - 表の外にこぼれていた5行（PR#42〜#45・PR #50）を表内へ移動し日付昇順に整列
  - 伏せ字だった「2026-08-2X」を確定日（8/22・8/23・8/23・8/24）に穴埋め
  - PR #50 行の破損修復（行末の縦棒、データ影響「なし」→「無」、対象をリポジトリ名へ）
  - 内容列の書き方ルール4項目と記入例セクションを追加
- 次の一手: なし。完了

### /api/daily 連打対策（PR #50・完了・停止機能のみ作動未確認）

- 状態: PR #50 マージ済み・ブランチ削除済み・ENV_CHANGELOG.md 追記済み。
  8/27 夜に Mac の Chrome DevTools で効果確認を実施し、再連射の停止を確認
- 触るファイル: なし
- 次の一手: 場中（9:00〜15:30）に同じ手順で再確認できれば、429 検知時の
  一時停止（DAILY_PAUSED_UNTIL）の作動まで見られる。ただし 429 を意図的に
  誘発する必要はなく、機会があれば見る程度でよい

### /api/daily の呼び出し量そのものの削減（PR2）

- 状態: 未着手。未投入（PR番号なし）。8/27 の実測を受けて優先度を引き下げた
- 触るファイル: src/App.js の fillDayNightFor と visibilitychange のリスナ登録部分
- 次の一手: 当面は観測のみ。表示銘柄数が増えて 429 が再発したときに着手する。
  案D（fillDayNightFor の直列ループに400ms程度の待機＋失敗銘柄への再試行抑止マーク）
  と案E（visibilitychange 経由の再開に最小間隔）。
  案F（pmFetchRecentDaily の cache no-store を force 時のみに限定）は効果の切り分けのため単独PR。
  案C（daily 専用キュー）は PM_FETCH_CONCURRENCY と二重制御になるため最終手段

優先度を下げた理由:

- 429 が0件で、転送量も最大14.1kB と軽い。今すぐ直さないと壊れる状態ではない
- ただし「表示銘柄数ぶんのリクエストが一度に飛ぶ」構造は変わっていない。
  PREMARKET_CODES と違って上限が無く、銘柄数に連動して増えるため、見送りではなく後回し

## 次にやること

上から順に:

1. Upstash 上限への対処。管理画面で使用量内訳とリセット日を確認し、
   案1〜案3のどれを採るか決める。月曜 8:45 が期限
2. Phase 2B 段B＋段C。気配ベース予想の実装と PM_HIST_MAX の引き上げ。
   Upstash の件が片付いてから着手
3. PR2 の着手判断。429 が再発するか、表示銘柄数が大きく増えた時点で再評価
4. parts.ranking の修正着手判断。組み立ての予算超過が観測された時点で再評価
5. Phase 2B 段D（画面表示）。段B の実績が貯まってから

## 未決の判断

- Upstash の対処として案1（有料化）・案2（回復待ち）・案3（書き込み削減）の
  どれを採るか。管理画面の確認結果次第
- 段B の確信度をどの水準から始めるか。4営業日・457サンプルしかないため
  控えめに置くのが前提だが、具体値は未定
- .env.example を作るかどうか。PR #18 の調査で、同ファイルが一度もコミットされたことが
  ないと判明した（git log --all が空）。.gitignore の .env.* により追跡できない状態のため、
  作るなら .gitignore に例外行が必要。環境変数21個は README の表に載ったので実害は薄いが、
  新しい環境で立ち上げ直すときの手間として残る
- TACHIBANA_ENV が現在デモと本番のどちらで稼働しているか。コードからは判別できず
  （config.js は production 指定時のみ本番、それ以外はすべてデモ扱い）、
  README にも書いていない。運用上は把握しておくべき情報のため
  docs/OPERATIONS.md への記載が候補
- Claude Code への「食い違えば実装せず報告のみで止める」指示の書き方。PR #18 で
  .env.example 不在という前提相違があったが、Claude Code 側が「停止して往復するより
  完成させる方が有益」と判断して続行した。今回は結果として無害だったが、
  判断の余地を残すと本当に止めてほしい場面でも同じ理屈で走る。
  「食い違いの内容にかかわらず止めること」への文言変更を検討。
  恒久ルールなので本来の記載先は CLAUDE.md か docs/OPERATIONS.md。
  なお PR #53 ではこの文言を指示文に入れており、前提確認3点は正しく報告された
- 暫定保持に残る項目（premarketLogger.js の9前提、修正時に巻き込む恐れのある箇所7件、
  組み立ての起動経路、PREMARKET_CODES 158件固定）の移設先。docs/OPERATIONS.md が候補だが未着手
- ENV_CHANGELOG.md の記録範囲は決着済み。CLAUDE.md など通常のドキュメント変更は
  記録しない（同ファイルは「環境変数の変更履歴」でありドキュメント修正は対象外）。
  8/25 の PR#48、8/26 の CLAUDE.md 修正、8/28 の PR #18、8/29 の PR #53 はこの扱い。
  ただし ENV_CHANGELOG.md 自身の書式・ルール変更だけは例外として1行記録する
  （ルールの適用開始日が追えなくなるため）。8/28 の PR #52 がこの例外の初適用。
  恒久ルールなので、記載先を CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- ENV_CHANGELOG.md の書き方ルール本体（内容列4項目・記入例）は現在
  ENV_CHANGELOG.md 内に置いている。CLAUDE.md と docs/OPERATIONS.md の
  どちらへ移すかは未決。上の記録範囲ルールと同じ移設先にするのが自然
- daytrade-simulator 側の README を作るかどうか。現在は存在せず、GitHub のトップが
  ファイル一覧のみになる。PR #18 で tachibana-server 側から Vercel 側の構成記述を
  除いたため、相方の説明先が無い状態。作るなら10〜15行の入口に徹する方針。
  優先度は低い（無いだけで害は無い）
- Mac mini で DevTools が使えることが判明した。開発端末は iPad という前提で書かれている
  docs/OPERATIONS.md の記述を更新するかどうか

## 未確認の仮説

- Upstash の枠を消費している主因は立花のリアルタイム中継（tachibana-quote）という推測。
  銘柄を選択している間、値が届くたびにライブ用（30秒）とスナップショット用（3日）の
  2か所へ書き込むため、場中の稼働で1日数万回に達し、月20営業日で50万に届く規模になる。
  管理画面の内訳は未確認
- 段A の集計対象が1日あたり約120行（8/25=119 / 8/26=116 / 8/27=109 / 8/28=113）しかない。
  158銘柄を収集しているはずなので、毎日40銘柄前後が1行も現れていないことになる。
  除外内訳（noGap 16 / noRatio 8 / totalRows 480）とは別枠で、
  そもそも銘柄として集計に登場していない。板が立たない銘柄なのか収集側で落ちているのか未確認
- 買い比率の日平均が「その日の地合い」の代理指標になり得る可能性。
  4営業日とも、買い比率の日平均が高い日ほど実測ギャップの日平均も高く、順序が崩れていない
  （48.9→-0.251 / 53.4→+0.056 / 58.5→+0.893 / 48.3→-0.384）。
  現在は米国株と先物から marketBias を推定しているが、板の平均がその代替または補強に
  なり得る。ただし4日では偶然でも説明がつくため仮説にとどめる
- PR #18 で README に載せた起動ログのサンプル4行（watcher のポーリング間隔・
  webapi のポート・scan のバッチサイズ・premarket の収集時刻）が、実際のログ文言と
  一字一句一致しているかは未検証。ログ文言はコピペして Railway で検索する使われ方を
  するため、言い換えになっていると検索に引っかからない
- PR #18 後の README で、見出しの番号付けが混在している可能性がある。
  「4つの役割」「ファイル一覧」「環境変数」「Railway での運用」は番号なしだが、
  「5. 動作確認」に番号が残っているように見える。未確認。
  直すなら全部落とすか全部振り直すかのどちらか
- 組み立ての時間予算 BUILD_BUDGET_MS 8000ms に対し、8/24 の totalMs は 6754ms で84%。
  立花側が普段より遅い日は fetchJsonWithin が先に切れる可能性がある。実際に切れた観測は無い
- 分岐C（/api/ranking へのフォールバック）の fetchJsonWithin には try/catch が無く、
  例外が buildUniverse の外へ出る。コード上の事実だが、発生した観測は無い
- 組み立て失敗時、scan:universe:built のマークは組み立ての前に立つため、
  そのスロットは再試行されない可能性がある。未検証
- 内側のタイムアウト（/api/ipo が8秒、/ranking-data が15秒）より外側の予算8秒が
  先に切れる逆転構造になっている。内側の値は実質的に効いていないとみられる
- tachibana-server の getRankingData には topix にある取得中Promise共有（inflight）が無い。
  同時に2本来ると全銘柄取得が二重に走る可能性がある。発生の観測は無い
- scan:universe:meta の source が「ranking(業種で取れなかったため代替)」の場合、
  /api/sector が例外だったのか200で0件だったのかは meta からは区別できない。
  所要時間が大きく違うため、切り分けが必要になったら記録の追加を検討する
- ENV_CHANGELOG.md の「### 記入例」の表は本表と同じ形式のため、将来スクリプトで
  縦棒区切り行を機械的に数えると 2026-01-01 の1行が混入する。
  機械処理を導入する場合は「## 記録」見出し以降のみを対象にする必要がある
- 8/18 の気配データが 3.2KB と小さいのは初日の部分取得と見られるが、未確認。
  8/19・8/20 が 27KB で一致しているため、12件時の定常値は 27KB
- readBody() の {gz:"..."} 受信パスはどの送信側からも使われていない死んだ経路。害はないが未整理
- 101〜158番目の銘柄が小型で板が薄いためデータ量が小さい、という POST サイズ予測外れの解釈。
  銘柄ごとの内訳では未検証（推論のみ）
- api/premarket.js の cache 変数直上のコメント「3分キャッシュ（サーバーレス関数の
  インスタンスが生きている間だけ有効）」は内容は正確だが「Vercel側」の語がない。
  PR #49 では対応不要と判断。用語統一が必要になったときの候補
- validCount は寄り付き時刻の代理指標の可能性（0/57/69/81 の4値・12刻み）。
  validCount=81 と open=null が完全一致。例外は 5242
- iPad の Web エディタでの貼り付けは、複数行の Markdown で改行が失われることがある。
  8/26 に1回発生（CLAUDE.md）。再発するならドキュメント修正も Claude Code 経由に統一する

## 直近の実測値

### Phase 2B 段A・買い比率とギャップの関係（8/25〜8/28・4営業日・有効457行）

| 変種 | n | r | rRank | slope | intercept | dirRate |
|---|---|---|---|---|---|---|
| first | 457 | 0.166 | 0.339 | 0.039 | -0.008 | 0.608 |
| last | 457 | 0.211 | 0.494 | 0.058 | -0.105 | 0.643 |
| min | 457 | 0.196 | 0.420 | 0.050 | 0.335 | 0.717 |
| max | 457 | 0.206 | 0.433 | 0.053 | -0.443 | 0.568 |
| avg | 457 | 0.200 | 0.427 | 0.051 | -0.046 | 0.626 |

日別内訳:

| 日付 | n | 買い比率avgの平均 | 実測ギャップの平均 | その日の相関 |
|---|---|---|---|---|
| 8/25 | 119 | 48.924 | -0.251% | 0.458 |
| 8/26 | 116 | 53.435 | +0.056% | 0.313 |
| 8/27 | 109 | 58.500 | +0.893% | 0.411 |
| 8/28 | 113 | 48.335 | -0.384% | 0.159 |

除外内訳: noGap 16 / noRatio 8 / totalRows 480

- 段B 着手の水準（rRank 0.3以上）を満たした。最良は buyRatioLast の rRank 0.494
- buyRatioMin は dirRate 0.717 と方向の的中率が突出。関係の強さでは last に劣るが
  「21分間で一度も売りに傾かなかった」という厳しい条件のぶん信頼度が高い。
  予想の大きさは last、確信度は min という役割分担が候補
- slope は現実的な水準。last の 0.058 なら買い比率75%で予想ギャップ +1.35% 前後。
  少数の外れ値に引きずられた形跡はない
- 8/28 だけ相関 0.159 と低い。4日のうち1日は当たらない日がある
- 「buyRatioLast が buyRatioAvg より有効かもしれない」という9銘柄時点の仮説は
  457サンプルで裏付けが取れたため、未確認の仮説から削除した
- 8/18〜8/24 の範囲は Upstash 上限により未取得

### /api/daily 連打対策の効果確認（8/27 夜・Mac の Chrome DevTools）

| 項目 | 実測 | 判定 |
|---|---|---|
| daily リクエスト件数 | 157件（表示銘柄 157件と一致） | 通過（重複取得なし） |
| ステータス | 200 と 304 のみ。429 は0件 | 通過 |
| 裏表の往復による追加リクエスト | 0件 | 通過（再連射なし） |
| 転送量 | 2.0KB / 12.1KB / 14.1KB（3回分） | 通過 |

- 429 が発生しなかったため、ネガティブキャッシュと2分停止は未発火。作動は未確認
- 20:24 の回は大半が disk cache（1ms）、20:32 と 21:25 の回は大半が 304（0.1KB）。
  キャッシュ期限切れ後も再検証だけで済んでおり、本文の再送は起きていない
- 確認したURLはプレビュー用ドメイン。コンソールに出る Vercel の sso-api への
  CORS ブロックと ERR_FAILED はプレビュー認証の副作用で、アプリとは無関係。
  本番ドメインでは出ない
- apple-mobile-web-app-capable の非推奨警告が1件。表示・動作への影響なし

### PREMARKET_MAX=158（8/25朝・全項目通過）

| 項目 | 実測 | 判定 |
|---|---|---|
| tick失敗 | 0件 | 通過 |
| エラー | 0件 | 通過 |
| POST サイズ | 2352KB | 通過（WARN 3500KB まで 1148KB の余裕） |
| レコード件数 | 84件 | 通過 |
| tick 所要 | 439〜645ms / 平均493ms | 通過 |
| 起動ログ 対象 | 158件 / 158件 / 158件 | 通過 |

- 158 で確定。ロールバック先は 100
- 収集時間 1260秒＝21分ちょうど。84ティック × 15秒と完全一致。窓の取りこぼしなし
- 8:35 JST の日次再ログイン成功。メンテ明けの関門を通過
- 8:50 スキャン 203件 / 4分9秒 / 失敗0件
- 4:29・4:39 JST の watcher 読み込み失敗2件はメンテ帯（3:00〜8:30）内で想定内

### ペイロード線形性の前提が更新された

- 予測 3093KB（19.58KB/銘柄 × 158）に対し実測 2352KB（14.89KB/銘柄）。マイナス741KB、予測比76%
- 19.58KB/銘柄は上位100銘柄に限った値であり、全件には適用できない。
  PREMARKET_CODES は 7203 / 8306 / 9984 と大型株から並んでおり、
  101番目以降は板が薄く1銘柄あたりのデータ量が小さい
- Vercel 上限 4.5MB に対して 52%。当面の余裕は想定以上にある

### 気配データの日次サイズ（Upstash premarket:log:<日付>、gzip 後）

| 日付 | Size | 銘柄数 | 1銘柄あたり |
|---|---|---|---|
| 8/18 | 3.2KB | 12 | — |
| 8/19 | 27KB | 12 | — |
| 8/20 | 26.7KB | 12 | — |
| 8/21 | 91.5KB | 40 | 2.29KB |
| 8/24 | 201KB | 100 | 2.01KB |

## 暫定保持（移設先が決まり次第、順次ここから削除する）

premarketLogger.js の壊してはいけない前提:

1. warn() ヘルパーの内部は console.log。console.warn に戻さない（Railway で [err] 分類される）
2. running の解除は .finally() のみ
3. lastSessionDate ガードは維持
4. errorCount と tickErrorCount を混ぜない
5. PREMARKET_CODES のパースは /^[0-9A-Z]{4}$/
6. PREMARKET_MAX は起動時に一度だけ確定。変更には再起動が必須
7. 1ティック＝立花への POST 1回。ボトルネックは Vercel へのペイロードサイズ
8. 検証窓は平日 8:45〜9:06 の21分のみ。失敗の検知は翌営業日。
   実体は START_MINUTE（8:45）/ END_MINUTE（9:06）
9. POSTサイズ閾値は POST_SIZE_WARN_KB=3500 / POST_SIZE_ERROR_KB=4000 / POST_SIZE_LIMIT_KB=4500。
   error() は console.error のまま維持する

tick失敗 と エラー は別カウンタ。必ず tick失敗 を先に見る。

修正時に巻き込む恐れのある箇所:

- api/sync.js — lastSectors 未送信時に既存値を維持
- api/_scan.js — 組み立てはスロット先頭の1回だけ。マークが先に立つ
- api/_scan.js の saveUniverse — source が "ranking" 以外だと保存拒否
- src/lib/analyze.js — App.js と api/_scan.js の共有。変更すると自動スキャンの保存スコアも変わる
- src/App.js の PUSH_SYNC — 触るとお気に入りが巻き戻る
- src/App.js の applySyncedData — SyncPanel の ID 切り替え時に last_sectors を上書き
- api/sector.js の sectorCache と分岐 — 触ると AI 呼び出しが復活

組み立ての起動経路:

- 唯一の入口: POST /api/sync?resource=scan-run → runScanBatch → buildUniverse
- フロントの手動スキャンでは走らない（buildStockUniverse はブラウザ内組み立て）
- offset:0 かつ scan:universe:built のマークが無いときだけ組み立て
- 組み立て回はスキャンせず done:0 / nextOffset:0 で即返す

固定事項:

- PREMARKET_CODES は158件固定の定点観測（scan:universe と非連動）。
  PREMARKET_MAX=158 により現在は全件を収集している
