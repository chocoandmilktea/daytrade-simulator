暫定保持：0件（docs/HOLD.md）／最終棚卸し 2026-09-01

# 作業引き継ぎ（最終更新: 2026-09-03）

このファイルは「今どうなっているか」だけを書く。
恒久的な制約は CLAUDE.md（実装向け）と docs/OPERATIONS.md（運用向け）に書き、
ここには重複させない。更新は GitHub Web エディタで main 直コミット。

進行中の案件には必ず PR 番号を書く。番号が無いものは未投入。
「指示文作成済み」だけの記述は、投入済みかどうかが読めないため使わない。

## 現在の課題

- 寄り前収集の安定稼働が確定した。8/31・9/1・9/2・9/3 の4営業日連続で、
  tick失敗0 / 件数不一致0 / 全ティック158銘柄 / missing 0件 を通過。
  「端末を開かなくても毎朝158銘柄の寄り予想が自動で貯まる」状態は再現性を持って動いている
- 予想誤差の主因は「地合い」だと 9/3 に確定した。同日の予想の平均は −0.02% だったが
  実測の平均は +0.58% で、152銘柄そろって +0.60% ぶん上に外している。
  quote 予想の式には地合いを表す項が無く、切片（−0.105）が固定値でその役目を代行している。
  実測の日別平均ギャップは −1.33%〜+0.58% と1.9ポイント振れており、
  固定の切片では追随できない。ベータ推定側には marketBias があるが quote 側には無い
- 買い比率が極端な銘柄を大きく外している。9/3 に予想の絶対値が1.5%以上だった13件は
  平均絶対誤差 1.79% で、残り139件の 0.80% の2倍以上。
  内訳は 7707（予想 −2.92 / 実測 −0.47）、7717（−2.76 / +0.75）、
  7743（−2.54 / 0.00）、7776（−2.22 / 0.00）、256A（−1.98 / +0.51）など、
  売り気配に偏っていた銘柄がほとんど下げずに寄る形。
  板が薄く気配数量が数枚しか出ない銘柄で比率が振り切れることが原因と見られる
- validCount による群の差は 9/3（上げ相場）では再現しなかった。
  60の群が平均誤差 +0.60%、61の群が +0.66%、61以上をまとめても +0.61% で差は0.01%。
  下げ相場3営業日で見えていた「約1%の開き」は地合いの影響だったと考えられる
- 群の境界を61で引いていたのが粗すぎた。61ちょうどは60よりティック1回ぶん長く
  両側の気配が取れただけで、寄りが遅れたとは呼べない。
  9/3 は61ちょうどが77件・62以上が7件で、84（9:06まで寄らず）は0件だった。
  下げた3日の「61以上」は84付近を多く含んでおり、日によって別の集団を指していた。
  区切るなら62以上にすべき
- validCount が60未満の銘柄が出る日と出ない日がある。9/2 は 11・33・34・49 の4件、
  9/3 は0件。quoteCount は揃っているため収集の取りこぼしではない
- noOpen の件数は日によって大きく振れる。8/25 が4件、8/31 が16件、
  9/1 が5件、9/2 が4件。大きく下げて寄った日に増える。
  「答え合わせできたデータは動きの小さい銘柄に偏る」という歪みは構造的に残る
- 低位株が較正データを壊す。7709 は 9/1 に +15.87%、9/2 に +14.71%。
  前日終値が63円・68円の価格帯で、1円の動きが1.6%になる。
  ただし 9/3 の 7709 は予想 −2.23 に対し実測 −2.94 で、極端な気配の群の中では
  当たっているほうだった。問題の本体は価格帯ではなく板の薄さの可能性がある
- 較正係数が2箇所に分かれている。サーバー側は環境変数（既定値）、ブラウザ側は直書き。
  移行期間中の一時的な状態として受け入れた。当面は環境変数を設定しないこと。
  PR #64 で CLAUDE.md の環境変数表にもこの注意を明記した
- 段A の係数（傾き0.058・切片−0.105）は先頭120銘柄由来。158件全件の4営業日で
  mode=calib を取り直したところ buyRatioLast は傾き0.045・切片−0.405 だった。
  ただし切片は期間の地合いを吸い込むため、この値をそのまま採用してはいけない。
  係数の見直しは2箇所管理を解消してからにする
- 環境変数の未設定が黙って進む経路が2種類ある（PR #64 の調査で判明）。
  UPSTASH_REDIS_REST_URL / _TOKEN は依存が 1.38.2 になった結果、未設定でも起動は続き
  最初の Redis 呼び出しで初めて失敗する。PUSHOVER_TOKEN / PUSHOVER_USER は
  未設定チェックが無く、API は 200 を返すが通知だけが届かない。
  どちらも「設定漏れにすぐ気付ける」という前提が成立しない
- PR #50 の3点のうち、ネガティブキャッシュと2分停止は 429 が発生しなかったため未発火。
  再連射が止まったことは確認できたが、停止機能そのものの作動は未確認のまま
- parts.ranking の調査は完了。原因はほぼ特定できたが、修正は見送りとした
- PR #50〜#57・#60〜#66（daytrade-simulator）と
  PR #18〜#24（tachibana-server）はすべてマージ済み

## 進行中の案件

### validCount の定義訂正（PR #65・#66・完了）

- 状態: PR #65 マージ済み（内容に誤りあり）。PR #66 マージ済み・ブランチ削除済み。完了
- 触るファイル: なし
- 経緯:
  - 9/1 の Railway ログで「総ティック数84 ＝ validCount の最大値84」
    「60回目のティックが 08:59:54 JST」という一致を見て、通し番号だと結論づけた。
    コードを読まずに観測値の一致だけで断定したのが誤りだった
  - 9/1 の時点で 21 という値を観測していたが、
    「21 は9:00より前に寄ったことになり成立しない」という矛盾に気付かず通過した
  - 9/2 に 11・33・34・49 が出て矛盾が表面化し、コード調査で誤りが確定した
- 調査で確定した内容:
  - validCount は api/sync.js:290 の ratios.length。加算処理は存在せず配列長を取るだけ
  - カウント条件は api/sync.js:247-255 の3つ。pAAV が数値化できること、
    pABV が数値化できること、その合計が0より大きいこと。片側が空文字なら除外、
    片側が数値の0なら算入される
  - 上限は quoteCount（api/sync.js:289 の list.length）
  - 寄り付き後を除外する明示的な判定は無い。寄ると気配が空文字になり
    条件で落ちるため結果的に除外されているだけ
  - noOpen は row.open == null（api/sync.js:597）で判定しており validCount を見ていない。
    両者が一致するのは現象上の相関であって同値関係ではない
- 次の一手: なし。完了

### 較正係数の再確認と群別の検証（完了・コード変更なし）

- 状態: 9/3 に実施。未投入（PR番号なし）。読み取りのみのため PR は無い
- 触るファイル: なし
- 確認方法: premarket-summary の mode=calib（from=2026-08-31・to=2026-09-03）と、
  premarket-prediction の GET（date=2026-09-03）をブラウザで開いて突き合わせた
- 結論:
  - 傾き・切片とも変更しない。切片は期間の地合いを吸い込むため固定値の更新に向かない。
    傾きの実測 0.045 は現行 0.058 と差があるが、2箇所管理のままでは変更を入れられない
  - 本番が使っている buyRatioLast は、5変種のうち rRank（日内順位相関）が最も高かった。
    選択は妥当だったことが確認できた
  - buyRatioMin が50超の群の dirRate が 0.602（全体は 0.517）。
    朝じゅう片側に張り付いた銘柄を加点する PM_Q_CONF_ONESIDE に実測の裏付けが取れた
  - validCount の群差は上げ相場では消えた。確信度への反映は見送る
- 次の一手: なし。完了

### 寄り前収集の安定確認（完了・コード変更なし）

- 状態: 完了。未投入（PR番号なし）。確認作業のみのため PR は無い
- 触るファイル: なし
- 確認方法: premarket-summary の mode=coverage と premarket-prediction を
  ブラウザで開き、Railway Logs で tick失敗・件数不一致を突き合わせた
- 結果: 8/31・9/1・9/2 の3営業日とも全項目通過。
  9/1 は Railway ログで「収集終了: 84件（エラー0件）1260秒」「件数不一致 0ティック」
  「POST 158銘柄 / 3095KB / 84レコード」「保存しました: 2026-09-01 84件」
  「予想を生成しました: 2026-09-01 154件（見送り4件）」を確認
- 次の一手: なし。完了

### 環境変数の棚卸しと一覧表の新設（PR #64・完了）

- 状態: PR #64 マージ済み。完了
- 触るファイル: なし
- 実施した内容:
  - daytrade-simulator の CLAUDE.md に「環境変数」の節を新設。
    ファイル早見表の直後に挿入。表は Vercel Functions（15件）と
    GitHub Actions（3件）の2つに分け、いずれも3列
    （変数名・必須任意・用途）。既定値は用途欄に書く。
    tachibana-server の README と列構成を揃えた
  - docs/ENV_AUDIT.md は 2026-08-20 時点の調査記録として凍結。
    冒頭に注記のみ追加し、既存本文は無変更
- 表に載せず注記にしたもの:
  - KV_REST_API_URL / KV_REST_API_TOKEN … コードには無いが
    @upstash/redis が代替名として読む。Vercel の Upstash 連携が注入しうる
  - TACHIBANA_WATCH_API / TACHIBANA_QUOTE_API … src/App.js の同名は直書き定数で
    環境変数ではない
  - GITHUB_TOKEN … forecast.yml の push は actions/checkout の既定トークンに依存
  - JQUANTS_API_KEY … 実行コードの参照ゼロのため載せない
- 調査で確定した内容:
  - 較正係数は PM_Q_SLOPE（既定 0.058）と PM_Q_INTERCEPT（既定 -0.105）の2つのみ。
    他の PM_Q_ 系（EXP_LIMIT・CONF_BASE・CONF_ONESIDE・CONF_RANGE_PENALTY・
    CONF_MIN・CONF_MAX・SRC）はすべて直書き定数で環境変数化されていない
  - PM_HIST_MAX は環境変数ではない。src/App.js の直書き定数（値180）で、
    localStorage の pm_<ticker> に残す件数を決める。両リポジトリに
    process.env 参照は無い
  - TACHIBANA_RELAY_SECRET は「任意」ではなく実質必須。認証の判定は
    isAuthed() に集約され、未設定だと認証が要る5経路すべてが 401 になる
  - tachibana-server 側の環境変数が daytrade-simulator の実行コードに
    混入している事実は無い
- ENV_CHANGELOG.md への記録: 不要と判断（ドキュメントのみの変更で、
  設定値の変更も再デプロイも伴わないため）
- 次の一手: なし。完了

### 暫定保持28件の移設（PR #21・#60・#61・#62・完了）

- 状態: 4本ともマージ済み。docs/HOLD.md は0件の受け皿として存置。完了
- 触るファイル: なし
- 内訳:
  - PR #21（tachibana-server）… CLAUDE.md を新設。premarketLogger.js の前提11件と
    銘柄リストの決まり方。README は人間向けの入口として無変更
  - PR #60（daytrade-simulator）… CLAUDE.md へ12件を追記。
    組み立ての起動経路・summarizePremarketDate の共有・寄り予想の2系統・
    PUSH_SYNC / applySyncedData / sectorCache の注意
  - PR #61（daytrade-simulator）… docs/OPERATIONS.md へ3件を追記。
    寄り前収集のログの読み方・対象銘柄の確認場所・Upstash の料金プラン
  - PR #62（daytrade-simulator）… docs/HOLD.md を空の受け皿へ置き換え
  - 重複のため移設せず削除: analyze.js の共有（既にファイル早見表にあった）
  - HANDOFF.md へ一本化: 較正係数が2箇所にある件
- 調査で判明し、記述を訂正した3点:
  - 組み立ての条件は「マークが無いとき」ではなく
    「マークの値が 日付:スロット と一致しないとき」だった。
    CLAUDE.md の Redis 表下にあった既存記述も同じ緩さがあり、PR #60 で併せて修正した
  - summarizePremarketDate の依存は3箇所ではなく、本体を含めて4箇所だった
  - api/sector.js の AI 呼び出しは「呼ばない実装」ではなく、
    ?sectors= が付かない経路では現役で呼ばれる。
    フロントは前回の業種があれば付けるため通常運用では入らないが、業種が空のときは入る
  - PREMARKET_CODES の158件はコードには存在せず Railway の環境変数側の値。
    実装向けには書けないため、運用側（docs/OPERATIONS.md）へ分けた
- 次の一手: なし。完了

### 気配収集の120件上限の解消（PR #19・完了）

- 状態: PR #19 マージ済み・Railway 再デプロイ済み・3営業日の実測で全項目通過。完了
- 触るファイル: なし
- 実測結果: 8/31・9/1・9/2 とも件数不一致0ティック / tick失敗0件 /
  84ティック全てが158銘柄 / missing 0件。38銘柄の取りこぼしは解消した
- 確定した原因と実施内容は CLAUDE.md（tachibana-server）へ移設済み
- 次の一手: なし。完了

### 寄り予想のサーバー側移行（PR #56・#20・#57・完了）

- 状態: 3本ともマージ済み。4営業日連続で自動生成が成功。完了
- 触るファイル: なし
- 実施した内容:
  - PR #56 … api/sync.js に resource「premarket-prediction」を新設。
    POST は認証必須で生成・保存、GET は無認証の読み取り専用。
    保存先は premarket:pred:<日付>。較正係数のみ環境変数化
  - PR #20 … 生ログの保存成功時のみ、Railway 側から生成APIを1回叩く。
    タイムアウト30秒・リトライなし。失敗しても収集の成否に影響させない
  - PR #57 … CLAUDE.md に新 resource を追記
- 移植の検証: 11,983ケースで移植元と出力が完全一致。不一致0件
- 実測結果: 8/31 は158件・見送り0件、9/1 は154件・見送り4件、
  9/2 は155件・見送り3件、9/3 は152件
- 次の一手: なし。完了

### tachibana-server README の実装追随（PR #22・#23・#24・完了）

- 状態: 3本ともマージ済み。完了
- 触るファイル: なし
- 実施した内容:
  - PR #22 … premarketLogger の節にチャンク分割（PR #19）と
    予想生成の呼び出し（PR #20）を反映。環境変数表に PREMARKET_CHUNK_SIZE を追加し、
    PREMARKET_MAX の用途欄に「1回の取得要求の件数を決めるものではない」と明記
  - PR #23 … 起動ログのサンプルの premarket 行に「宛先:」を補い、
    対象・受領・上限・分割の件数行を追加
  - PR #24 … サンプルを実出力順に並べ替え、例示の数値を切り捨てが起きていない状態
    （対象158 / 受領158 / 上限158 / 分割80）に修正。窓外の行を追加
- 調査で確定した内容:
  - 予想生成は収集終了後ではなく、生ログの保存が成功したときだけ1回呼ばれる
  - 起動ログの実出力順は premarket件数 → watcher → scan → premarket起動 →
    premarket窓外 → webapi。webapi が最後になるのは listen のコールバックのため
  - README の見出しに番号付けの混在は無かった
- 次の一手: なし。完了。この表の3列構成は PR #64 で daytrade-simulator 側にも揃えた

### PM_Q_MIN_TICKS の撤去（PR #63・完了）

- 状態: PR #63 マージ済み。完了
- 触るファイル: なし
- 実施した内容:
  - 定数 PM_Q_MIN_TICKS（値5）を api/sync.js と src/App.js の両方から削除
  - 判定を「validCount に値が入っていること」に置き換え。
    値が無い行を弾く守りは残した
  - handlePremarketPrediction 内の「5回未満」という説明コメントも実装に合わせた
- 訂正: PM_Q_MIN_TICKS は環境変数ではなく、両ファイルに直書きされた定数だった。
  Vercel・Railway の設定画面に対応する変数は無い
- 訂正: 撤去時の根拠として書いた「実測の最小値は60」は誤りだった。
  8/31 に 50、9/1 に 21、9/2 に 11 が観測されている。
  ただし 11 も 5 より大きいため、5未満で弾かれる行が存在しないという結論は変わらない
- 次の一手: なし。完了

### Phase 2B 段D・画面表示

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: src/App.js
- 内容: 予想パネルに2手法を並べて出す。的中率パネルの bySrc の見せ方。
  答え合わせ表示がベータ側を隠してしまう問題もここで解消する
- 作業項目から外したもの: reasons のラベル「観測回数」の修正。
  PR #66 の調査でラベルは実装と整合していることが確認できたため不要になった
- 前提の変更: サーバー側移行により読み出し元が変わった。
  ローカルの pm_<ticker> ではなく premarket-prediction の GET を見る設計になる
- 着手を止めていた前提は外れた。validCount を確信度に反映させるかは
  9/3 の検証で「当面は反映させない」と決着したため、この判断待ちは解消している
- 次の一手: 着手前に現在の予想パネル・的中率パネルを読み取り専用で調べる調査を挟む

### 地合いの項を予想式に入れる（新規・未着手）

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: 着手時は api/sync.js の predictGapByQuote と
  src/App.js の pmPredictGapByQuote が候補。ただし2箇所管理の解消が先
- 内容: quote 予想には地合いを表す項が無く、切片が固定値でその役目を代行している。
  9/3 の実測で誤差の主因がここだと確定した。
  その日の全銘柄の平均買い比率を地合いの代理指標として使える可能性がある
- 次の一手: 営業日10日ぶんほど貯めてから、日別の平均買い比率と
  日別の平均ギャップの関係を mode=calib の daily で見直す

### 極端な買い比率の扱いの見直し（新規・未着手）

- 状態: 未着手。未投入（PR番号なし）
- 触るファイル: 着手時は api/sync.js と src/App.js の PM_Q_EXP_LIMIT 周辺が候補。
  ただし2箇所管理の解消が先
- 内容: 買い比率が極端な銘柄で予想が過剰になる。9/3 は予想の絶対値1.5%以上の13件が
  平均絶対誤差 1.79% で、残り139件の2倍以上だった
- 次の一手: 数日ぶんで同じ傾向が出るかを確認してから対処方針を決める。
  上限で丸めるのか、板の薄い銘柄を予想対象から外すのか、確信度を下げるのかは未定

### /api/daily 連打対策（PR #50・完了・停止機能のみ作動未確認）

- 状態: PR #50 マージ済み・ブランチ削除済み・ENV_CHANGELOG.md 追記済み
- 触るファイル: なし
- 次の一手: 場中（9:00〜15:30）に同じ手順で再確認できれば、
  429 検知時の一時停止の作動まで見られる。機会があれば見る程度でよい

### /api/daily の呼び出し量そのものの削減（PR2）

- 状態: 未着手。未投入（PR番号なし）。8/27 の実測を受けて優先度を引き下げた
- 触るファイル: src/App.js の fillDayNightFor と visibilitychange のリスナ登録部分
- 次の一手: 当面は観測のみ。表示銘柄数が増えて 429 が再発したときに着手する。
  案D（直列ループに400ms程度の待機＋失敗銘柄への再試行抑止マーク）と
  案E（visibilitychange 経由の再開に最小間隔）。
  案F（cache no-store を force 時のみに限定）は効果の切り分けのため単独PR。
  案C（daily 専用キュー）は二重制御になるため最終手段

優先度を下げた理由:

- 429 が0件で、転送量も最大14.1kB と軽い。今すぐ直さないと壊れる状態ではない
- ただし「表示銘柄数ぶんのリクエストが一度に飛ぶ」構造は変わっていない

### parts.ranking の原因調査（調査完了・修正は見送り）

- 状態: 8/28 に Claude Code で調査実施（変更なし）。未投入（PR番号なし）
- 触るファイル: なし（修正に着手する場合は api/ranking.js と api/_scan.js が候補）
- 調査で確定した内容: 6016ms の主因は tachibana-server の getRankingData。
  全銘柄価格を120件ずつ・並列5で取得する処理
- 見送りの理由: 組み立て回はスキャンせず即返す仕様のため、6秒は体感に出ない
- 次の一手: 予算超過が実際に観測された時点で再評価する

### Upstash 月間リクエスト上限への対処（完了・コード変更なし）

- 状態: 完了。未投入（PR番号なし）。コード変更を伴わない運用対処のため PR は無い
- 触るファイル: なし
- 実施した内容: 8/29 未明に無料枠50万コマンドへ到達し停止。
  Vercel Dashboard 経由で Pay As You Go へ移行。Max Budget 20ドル
- 内訳は Reads 808,406 / Writes 76,390。読み取りが91%で主因
- 次の一手: なし。月に一度 Usage を見て金額を確認する（docs/OPERATIONS.md へ記載済み）

## 次にやること

上から順に:

1. Phase 2B 段D（画面表示）。読み出し元がサーバー側に変わった前提で設計。
   着手前に予想パネル・的中率パネルの読み取り専用の調査を挟む
2. ブラウザ側の予想生成をいつ外すか。較正係数の2箇所管理を解消しないと
   地合いの項も極端気配の扱いも手を付けられないため、優先度が上がった
3. 群別の誤差の継続観測。62以上の群は 9/3 が7件しかなく判断できない。
   営業日10日ぶんを目安に貯めてから精度指標としての採用を再評価する
4. 地合いの項の検討。日別の平均買い比率が代理指標になるかを 10営業日ぶんで確認
5. 極端な買い比率の扱いの検討。数日ぶんで同じ傾向が出るかを先に確認
6. PR2 の着手判断。429 が再発するか、表示銘柄数が大きく増えた時点で再評価
7. parts.ranking の修正着手判断。予算超過が観測された時点で再評価

## 未決の判断

- validCount を確信度に反映させるかどうか。方向の減点として使う案は取り下げる。
  上げ相場の 9/3 で群差が消えたため、下げ相場3営業日の観測は地合いの影響と判断した。
  精度の指標として使う案は保留。絶対誤差は 0.80 → 0.95 → 1.08 と
  60・61・62以上の順に大きくなるが、60と61の差0.15に対して
  ばらつきから出る幅が±0.11あり、有意とは言えない。
  区切るなら61ではなく62。営業日10日ぶんを目安に再評価する
- 低位株を較正データから除外するか、重み付けで扱うか。
  7709 は 9/1 に +15.87%、9/2 に +14.71% を記録している。
  ただし 9/3 の実測を見ると、問題の本体は価格帯ではなく板の薄さの可能性がある。
  極端な買い比率の扱いと一体で考えたほうがよい
- 158件という収集規模を維持するか。120件上限が解消され全件取得が実現したが、
  そもそも何件を定点観測するのが適切かという問いは残っている。
  ペイロードの余裕が約400KB（20銘柄ぶん相当）しかないため、増やす方向には制約がある
- 段A の係数を158件の母集団で作り直すかどうか。mode=calib の4営業日集計では
  buyRatioLast が傾き0.045・切片−0.405 だった。当面は据え置きとする。
  切片は期間の地合いを吸い込むため固定値の更新に使えず、
  傾きも2箇所管理のままでは変更を入れられないため
- 較正データに入る偏りをどう扱うか。9:06 までに寄らない銘柄は答え合わせから外れるため、
  大きく動いた銘柄ほどサンプルから抜け落ちる
- ブラウザ側の予想生成（src/App.js）をいつ外すか。
  外すときに較正係数の直書きも削除し、2箇所管理を解消する
- 既存のローカル記録 pm_<ticker> をサーバー側へ統合するのか、いったん切り捨てるのか。
  的中率集計がローカルを見ているため、切り替え方を決めないと過去の集計が断絶する
- Redis の読み取り回数を削減するかどうか。常時ポーリングが1日1.5万コマンド
  （月45万相当）を占めるが、8/29 時点では削減しない判断とした。
  Phase 2B で書き込みが増えて金額が跳ねた場合は再評価する
- quote 予想の確信度の上限60%をいつ引き上げるか。
  根拠が実質4営業日457サンプルのため控えめに置いた。
  ベータ推定側の上限は95%のままなので、当面は横並びで比較できない
- CLAUDE.md の「TTL は用途ごとに5種類」という文言。値の種類としては5で正しいが、
  用途の数と読める曖昧さがある。次に CLAUDE.md を触るときに
  「値としては5種類」と書き換えるのが本筋
- .env.example を作るかどうか。git log --all が空で、一度もコミットされたことがない。
  .gitignore の .env* により追跡できないため、作るなら例外行が必要。
  PR #64 で CLAUDE.md に一覧表ができたため、必要性は下がった
- TACHIBANA_ENV が現在デモと本番のどちらで稼働しているか。
  コードからは判別できず README にも書いていない。Railway の Variables で確認できる
- Claude Code への「食い違えば実装せず報告のみで止める」指示の書き方。
  PR #53 以降はこの文言を入れており、前提確認は毎回正しく報告されている。
  恒久ルールとして CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- Claude Code が完了報告に添える「CI・レビューコメントを監視して自動対応する」という
  申し出をどう扱うか。マージ判断は人間が行う方針のため受け入れていない。
  指示文の禁止事項に毎回書いているが、恒久ルールとして移すのが本来の筋
- ENV_CHANGELOG.md の記録範囲は決着済み。通常のドキュメント変更は記録しない。
  ただし ENV_CHANGELOG.md 自身の書式・ルール変更だけは例外として1行記録する。
  恒久ルールなので記載先を CLAUDE.md か docs/OPERATIONS.md へ移すのが本来の筋
- ENV_CHANGELOG.md の書き方ルール本体の移設先。CLAUDE.md と OPERATIONS.md のどちらか
- daytrade-simulator 側の README を作るかどうか。作るなら10〜15行の入口に徹する。
  優先度は低い
- Mac mini で DevTools が使えることが判明した。
  開発端末は iPad という前提で書かれている docs/OPERATIONS.md を更新するかどうか
- api/premarket.js（今朝の地合い）と premarket-prediction（銘柄ごとの寄り予想）の
  名前の紛らわしさ。実害は出ていないが PR #57 で指摘として挙がった

## 未確認の仮説

- validCount が60未満になる原因。9/2 の4件（7702・5242・255A・7723）は
  いずれも買い比率が極端（7〜8%台、86〜93%台など）だった。
  売り一色・買い一色で片側の気配数量が空になるためと考えているが、
  立花APIがどういう場合に気配数量を空で返すかは仕様を確認していない
- 62以上の群が地合いに対して下振れするかどうか。9/3 は地合い（+0.584%）を
  差し引くと 62以上7件の平均が −0.59% だったが、8136（予想 +0.05 / 実測 −2.30）の
  1件を除くと −0.20% まで縮む。7件では判断できない
- 買い比率が極端になる銘柄は板が薄いという推測。9/3 に大きく外した
  7707・7717・7743・7776 はいずれも比率が1桁台まで振れていた。
  気配数量の絶対値そのものは記録していないため未確認
- 立花APIの120件という上限が、仕様上の固定値なのか、
  応答サイズやカラム数に依存する可変の値なのかは未確認。
  80件分割にしたのは、この不確かさに余裕を持たせるため
- 始値が取れない銘柄に共通する事情。8/31 の noOpen 16件は
  268A・285A・3436・3851・5016・5801・6227・6323・6324・6525・6834・6920・
  6963・7743・8035・9984。大型株を含むため板の薄さでは説明がつかず、
  下げ幅の大きい日の特別気配と見られるが未確認
- Upstash の常時ぶん1日1.5万コマンドの発生源が Railway の watcher による
  ポーリングであるという推測。0.2コマンド/秒は5秒に1回の GET と一致するが未確認
- 買い比率の日平均が「その日の地合い」の代理指標になり得る可能性。
  8/31〜9/3 の4営業日は順序が完全に一致した（38.1→−1.33、45.7→−0.80、
  46.3→−0.49、50.5→+0.58）。8/25〜8/28 と 8/18〜8/24 でも同傾向。
  日をまたぐ感応度は1ポイントあたり約0.16%で、銘柄間の傾き0.045の3倍以上になるが、
  4点で引いた線のため仮説にとどめる
- 気配ベース予想がベータ推定より当たるかどうか。段A の相関は成立しているが、
  実際の予想としての優劣は quote の記録が貯まるまで判断できない
- Claude Code のセッションが認識する日付が JST と1日ずれる場合がある。
  実行環境が UTC のためと見られる。PR #62 で 8/31 と報告されたが実際は 9/1 だった
- UPSTASH_REDIS_REST_URL / _TOKEN が未設定のときの挙動が変わった理由。
  ENV_AUDIT.md が参照した 1.22.0 では即例外と記載されているが、
  現行の 1.38.2 では警告のみで処理が続く。
  1.22.0 が当時本当に例外を投げていたのか、ライブラリ側の仕様変更なのかは未検証
- KV_REST_API_URL / KV_REST_API_TOKEN が Vercel 側に実際に注入されているかは未確認。
  Upstash 連携がこの名前を入れることがあり、入っていればコードを grep しても
  出てこない経路で Redis 接続が成立しうる。Vercel の Environment Variables で確認できる

## 直近の実測値

- 寄り前収集（8/31・9/1・9/2）: いずれも84ティック / 全ティック158銘柄 /
  件数不一致0 / tick失敗0 / missing 0件 / appearedCount 158
- 予想生成: 8/31 は158件・見送り0件、9/1 は154件・見送り4件、
  9/2 は155件・見送り3件、9/3 は152件
- noOpen: 8/25 は4件、8/31 は16件、9/1 は5件、9/2 は4件。9/3 は未確認
- noBuyRatio: 8/31 は0件、9/1 は4件（255A・5187・7749・7781）、
  9/2 は3件（5187・7722・7782）。9/3 は未確認
- validCount の実測値: 8/31 は 50・60・61・62・64・66・69・72・77・82・84、
  9/1 は 21・60・61・62・64・66・72・75・79・80・84、
  9/2 は 11・33・34・49・60・61・62・65・71・72・73・76・84、
  9/3 は 60・61・62・65・71・72。9/2 は quoteCount が全158銘柄で84
- mode=calib（from 2026-08-31・to 2026-09-03・有効595件・全632行）:
  buyRatioLast は r 0.419 / rRank 0.466 / 傾き0.045 / 切片−0.405 / dirRate 0.517。
  buyRatioAvg は r 0.483 / rRank 0.433 / 傾き0.051 / 切片−0.255 / dirRate 0.557。
  buyRatioMin は dirRate 0.602 で5変種中で最も高い。
  rRank が r を上回るのは last だけ。除外は noGap 25件 / noRatio 13件
- mode=calib の日別（avg変種）: 8/31 は142件・平均買い比率45.70・平均ギャップ−0.798・r 0.622、
  9/1 は150件・46.34・−0.489・r 0.251、9/2 は151件・38.14・−1.331・r 0.445、
  9/3 は152件・50.46・+0.584・r 0.468
- 予想誤差（実測−予想）: 8/31 は validCount=60 の群118件が平均 −0.19%、
  61〜82 の群24件が平均 −1.53%。9/2 は 60 の群105件が平均 −0.65%、
  61以上の群45件が平均 −1.72%
- 9/3（上げ相場）の群別: 全152件は平均予想 −0.02% / 平均実測 +0.58% /
  平均誤差 +0.60% / 平均絶対誤差 0.89% / 方向一致 0.671。
  validCount=60 は68件で誤差 +0.60% / 絶対誤差 0.80%、
  61 は77件で +0.66% / 0.95%、62以上は7件で −0.01% / 1.08%。
  62以上の内訳は 5401(62)・6506(65)・6890(65)・7211(71)・1605(72)・4684(72)・8136(72)
- 9/3 の極端な気配: 予想の絶対値1.5%以上が13件で平均絶対誤差 1.79%、
  残り139件は 0.80%。外した代表は 7707（予想−2.92 / 実測−0.47）、
  7717（−2.76 / +0.75）、7743（−2.54 / 0.00）、7776（−2.22 / 0.00）、
  256A（−1.98 / +0.51）、4424（+1.83 / −2.31）
- 段A の較正（上位120銘柄・457サンプル）: buyRatioLast の rRank 0.494〜0.515、
  buyRatioMin の dirRate 0.717〜0.833。採用した係数は傾き0.058・切片−0.105
- Upstash 8月分: Reads 808,406 / Writes 76,390。常時 0.15〜0.2 コマンド/秒
- parts.ranking: 6016ms（totalMs 6754 の89%）
- 気配ログのペイロード: 9/1 は 158銘柄で 3095KB。Vercel 上限 4.5MB
- 環境変数の件数（PR #64 時点）: Vercel Functions 15件 / GitHub Actions 3件。
  ほかに注記扱いが4件（KV_REST_API_URL・KV_REST_API_TOKEN・GITHUB_TOKEN・
  JQUANTS_API_KEY）
